# Sprint 2 RAG « Multi-docs / comparaisons » — plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `baikal-retrieval` v2.2.0 : les questions qui nomment un ou plusieurs documents obtiennent des extraits de CHAQUE document nommé (comparaison déterministe, sans boucle agentique), la boucle agentique a son propre budget et ne génère qu'une fois, ARPET montre les étapes et propose « Approfondir » (lecture intégrale des documents nommés), et l'EF ne répond plus qu'à un utilisateur authentifié membre du projet.

**Architecture:** Le code établit les faits, le modèle juge (principe du Sprint 1). La résolution des documents nommés (`routing/named-documents.ts`) fournit désormais les identifiants de fichiers ; un nouveau module `search/targeted.ts` lance une recherche restreinte par document nommé en parallèle de la recherche globale et fusionne en garantissant la présence de chaque document. L'identité vient du jeton (`auth.ts`) et l'appartenance est vérifiée par une fonction SQL au prédicat identique à la RLS de `core.projects`. La boucle agentique (`agentic/orchestrator.ts`) reste le repli quand la gate le décide.

**Tech Stack:** Deno / TypeScript (Edge Function Supabase), Postgres (plpgsql, pgvector), React + TypeScript (ARPET), Gemini 2.5 Flash / Flash-Lite, OpenAI text-embedding-3-small et gpt-4o-mini, harnais d'éval Deno (`eval/`).

**Spec:** `C:\Dev\Frontend-ARPET\docs\SPEC_RAG_OPTIM_V1.md` §5 Sprint 2 (S2.1–S2.5), §6, §7.3 ; prompt de liaison `docs/superpowers/prompts/2026-09-17-sprint2-rag.md` (reports R-S1a → R-S1e). Décisions du brainstorming du 2026-09-17/19 (Eric) : sécurité en tâche 1 ; recherche ciblée sur **chaque** document nommé (pas seulement les comparaisons) ; comparaisons sur extraits par défaut, lecture intégrale **à la demande** via un bouton « Approfondir » (première marche d'un futur wizard : choix des documents et de l'angle, hors sprint).

## Faits établis avant le plan (vérifiés en base de production le 2026-09-17)

1. `rag.get_agent_context` ne vérifie aucune appartenance (`COALESCE(p_org_id, profil)`, `p_project_id` pris tel quel, `user_id` jamais confronté au jeton) ; `rag.match_documents_v15` est SECURITY DEFINER et exécutable par `anon` ; le schéma `rag` est exposé par PostgREST (`pgrst.db_schemas`). Le harnais d'éval appelle l'EF avec la clé anon et des UUID arbitraires — c'est ainsi que la baseline v2.1.0 a été produite.
2. `buildFallbackAnalysis` appelle `safeRequiresSearch(query, false, intent)` qui remplace l'intent par `factual` dès que la question contient un mot interrogatif ou finit par « ? » : journaux 30 jours = 559 `factual`, 12 `synthesis`, 5 `citation`, **0 `comparison`** (37 questions portent pourtant un motif de comparaison).
3. Sur C3-001, SC3-006, SC3-007, SC3-010, les deux fichiers attendus figurent dans les sources remontées ; la réponse n'en exploite qu'un. Le problème est la couverture par document dans le top-k, pas le rappel.
4. SC3-006 : le projet Citroën (marché privé) possède son propre fichier « 2024 06 20 CCAG CITROEN.pdf » ; la règle de prompt « marché privé → le CCAG ne s'applique pas » fait refuser à tort.
5. Couche application : 2 fichiers (`CCAG.pdf` id `5ba512af-d68a-41f3-9578-aeb17a231a55`, `Norme NFP03-001.pdf`), `layer='app'`, `project_id` NULL, `app_id='arpet'`. Aucun DTU.
6. R-S1c : une seule question distincte avec « plan » en 90 jours (« un plan d'exécution en retard sur CMP »), faux positif. `plan` / `notice` restent hors extraction. Aucune tâche.
7. RLS de `core.projects` (SELECT) : `rls_is_super_admin(uid) OR (rls_is_org_admin(uid) AND org_id = rls_get_user_org_id(uid)) OR id = ANY(rls_get_user_project_ids(uid))`. Ces quatre fonctions existent dans `core`, SECURITY DEFINER, `search_path=core`.
8. Aucun wrapper `public.*` ni `arpet.*` n'appelle les fonctions `rag.*` de recherche ; tous leurs appelants sont des Edge Functions en `service_role` (baikal-retrieval, baikal-brain-v3, baikal-librarian-v3/v4, generate-document). ARPET n'appelle en direct que `rag.delete_conversation` et `rag.close_conversation` (non touchées).

## Global Constraints

- Branche `main` des deux dépôts, **sans worktree** (décision d'Eric du 2026-09-15). Baikal : `C:\Dev\Frontend-Baikal`. ARPET : `C:\Dev\Frontend-ARPET` (branche `main` ; `master` déclenche Vercel — ne pas y pousser).
- **Jamais `git add -A`** : chemins explicites uniquement. Fichiers non commités d'Eric à ne jamais toucher ni ajouter : Baikal `src/contexts/AppContext.jsx` ; ARPET `.claude/settings.local.json`, `src/components/layout/Sidebar.tsx`, `src/components/layout/sidebar/SidebarNavigation.tsx`, `src/components/meeting/MeetingRecordModal.tsx`, `src/components/ui/ConnectorsModal.tsx` (supprimé), `src/services/meeting.service.ts`.
- Tests Deno : `*.test.ts` à côté du module, `https://deno.land/std@0.224.0/assert/mod.ts`, TDD (test écrit et vu échouer avant l'implémentation). Commande : `deno test supabase/functions/baikal-retrieval/` depuis la racine Baikal. Suite de départ : 7 fichiers, 68 tests.
- `deno check supabase/functions/baikal-retrieval/index.ts` : **0 erreur** à partir de la Tâche 1 (les 11 erreurs préexistantes de `routing/analyzer.ts` disparaissent avec le code mort).
- Texte fixe des prompts **sans accents** (convention du fichier `generation/prompt.ts` : « sans accents dans le texte fixe, comme le reste du prompt ») ; commentaires et messages SSE en français accentué. Apostrophe typographique U+2019 conservée là où elle existe (`[’']`).
- Version cible : **v2.2.0** (en-tête et log de `index.ts`).
- Portes : **G1** toute migration SQL est écrite dans `supabase/migrations/` et soumise à Eric ; appliquée via MCP `apply_migration` sur son « ok », **avant** tout déploiement. **G2** déploiement uniquement sur « ok » : `npx supabase functions deploy baikal-retrieval --project-ref odspcxgafcqxjzrarsqf` depuis la racine Baikal. **Aucun push GitHub sans accord.**
- Modèles des sous-agents : `sonnet` pour toutes les tâches (elles contiennent toutes du texte non ASCII) ; `opus` pour la revue finale de branche et les vagues de correction. `haiku` : aucune tâche.
- Éval (racine Baikal ; `eval/.env` et `eval/config.json` présents, gitignorés). Après la Tâche 2, le harnais exige `SUPABASE_SERVICE_ROLE_KEY` dans `eval/.env` (Eric la colle lui-même, jamais dans le chat) ; le juge exige `GEMINI_API_KEY` (idem, toujours vide au 2026-09-17).
  ```
  deno run -A eval/run-eval.ts --tag s2-v2.2.0 --baseline eval/reports/baseline-v2.1.0.json
  deno run -A eval/run-eval.ts --golden eval/golden-set.synthetic.json --tag s2-v2.2.0-synth --baseline eval/reports/baseline-v2.1.0-synth.json
  ```
  Variance inter-runs de 1 à 2 questions sur le set réel : juger par classe et par lecture des échecs.
- Pièges payés au Sprint 1 : `out_project_identity` sans clé `name` (nom du projet lu dans `core.projects`) ; CCAG hors projet (`layer='app'`, `project_id` NULL) ; `\m`/`\M` Postgres ne coupent pas sur `_` (bornes `[^a-z0-9]`) ; dans une chaîne `.or()` PostgREST ni virgule ni parenthèse, opérateur regex `imatch` ; `REFUSAL_PATTERNS` du harnais à compléter si une nouvelle formulation de refus est voulue ; les conversations d'éval restent dans `rag.conversations` (purge sur demande d'Eric seulement).
- Hors périmètre explicite : durcissement `SET search_path` sur `match_documents_v14/v15` (les opérateurs pgvector `<=>` vivent dans `extensions` et se résolvent par le search_path : un `SET search_path` restrictif casserait la fonction — à traiter dans une migration dédiée avec test) ; Cohere ; changement de modèle de génération (Sprint 3) ; wizard complet (documents + angle).

---

## Carte des fichiers

| Fichier | Rôle dans ce sprint |
|---|---|
| `supabase/functions/baikal-retrieval/routing/analyzer.ts` | T1 — code mort retiré, intent conservé |
| `supabase/functions/baikal-retrieval/routing/safety.ts` | T1 — `safeRequiresSearch` / `containsRealQuestion` retirés |
| `supabase/functions/baikal-retrieval/routing/analyzer.test.ts` | T1 — nouveau |
| `supabase/migrations/20260919100000_rag_acces_retrieval.sql` | T2 — `rag.resolve_access`, `get_agent_context` (conversation possédée), REVOKE/GRANT |
| `supabase/functions/baikal-retrieval/auth.ts` + `auth.test.ts` | T2 — nouveau : identité du jeton, décision d'accès |
| `supabase/functions/baikal-retrieval/index.ts` | T1, T2, T5, T6, T7 — câblage |
| `eval/run-eval.ts`, `eval/.env.example` | T2 — Bearer service_role ; T9 — critères alternatifs, `source_docs_all` |
| `supabase/functions/baikal-retrieval/routing/condenser.ts` + test | T3 — prompt, garde anti-recopie |
| `supabase/functions/baikal-retrieval/types.ts` | T4 (candidats avec `fileId`/`layer`), T5 (`targeted_chunks`, compteur), T7 (`GateReason`) |
| `supabase/functions/baikal-retrieval/routing/named-documents.ts` + test | T4 — ids de fichiers, `ccag`, repli couche application |
| `supabase/functions/baikal-retrieval/generation/prompt.ts` + test | T4 — règle marché privé affinée |
| `supabase/functions/baikal-retrieval/search/retrieval.ts` | T5 — `buildFileInfos` et `mapChunks` exportés |
| `supabase/functions/baikal-retrieval/search/targeted.ts` + test | T5 — nouveau : recherche ciblée par document nommé, fusion |
| `supabase/functions/baikal-retrieval/agentic/orchestrator.ts` + test | T6 — budget dédié, génération unique |
| `supabase/functions/baikal-retrieval/agentic/gemini-agent.ts` + test | T6 — timeout borné, `splitForStreaming` |
| `supabase/functions/baikal-retrieval/search/named-files.ts` + test | T7 — nouveau : fichiers des documents nommés pour la lecture intégrale |
| ARPET `src/services/chat/chat-types.ts`, `chat-mapping.ts`, `chat-sse.ts` | T8 — payload `agentic` / `named_documents`, `generation_mode` explicite |
| ARPET `src/types/chat.types.ts`, `src/pages/Dashboard.tsx`, `src/components/chat/MessageBubble.tsx`, `StreamingBubble.tsx`, `AssistantMessage/AssistantMessage.tsx`, `AssistantMessage/DeepenButton.tsx` (nouveau), `AssistantMessage/index.ts` | T8 — étapes agentiques, résumé, bouton « Approfondir » |
| `eval/golden-set.json`, `eval/golden-set.synthetic.json`, `eval/GOLDEN_SET_VALIDATION.md`, `eval/rescore-both-docs.ts` (nouveau) | T9 — critères revus (validés par Eric), métrique « les deux documents cités » |
| `CLAUDE.md` (Baikal), `.claude/proposed-updates.md` | T1 (ligne « Brain »), T10 |
| ARPET `docs/SPEC_RAG_OPTIM_V1.md` §7.4, ARPET `CLAUDE.md` | T10 |

Ordre : T1 → T2 → T3 → T4 → T5 → T6 → T7 → T8 → T9 → T10. T3, T6 et T9 ne dépendent d'aucune autre tâche de code ; T5 et T7 dépendent de T4 ; T8 dépend des payloads de T5, T6, T7.

---

### Task 1: Analyse heuristique — code mort supprimé, intent conservé

**Files:**
- Modify: `supabase/functions/baikal-retrieval/routing/analyzer.ts` (fichier entier réécrit, 217 → ~70 lignes)
- Modify: `supabase/functions/baikal-retrieval/routing/safety.ts:35-101` (retirer `QUESTION_WORDS`, `QUESTION_PATTERNS`, `containsRealQuestion`, `safeRequiresSearch`)
- Modify: `supabase/functions/baikal-retrieval/types.ts` (retirer l'interface `SafeAnalysisOverride` si plus référencée)
- Modify: `supabase/functions/baikal-retrieval/index.ts:2,97-100,130` (en-tête v2.2.0, branche `comparison` de `resolveGenerationMode` retirée, log de version)
- Modify: `CLAUDE.md` (Baikal) ligne « Brain (intent detection + query rewriting, integrated) » ; `.claude/proposed-updates.md` entrée « Le « Brain » LLM (analyzeQuery) n'est jamais appelé en production » → RESOLU
- Test: `supabase/functions/baikal-retrieval/routing/analyzer.test.ts` (nouveau)

**Interfaces:**
- Consumes: `detectIntentByKeywords(query): Intent`, `extractKeywords(query): string[]` (safety.ts), `detectCrossRef(query)` (cross-ref.ts) — inchangés.
- Produces: `buildFallbackAnalysis(query: string, documentsCles: DocumentCle[]): AnalysisResult` — même signature, mais `intent` = intent détecté par mots-clés (`comparison`, `synthesis`, `citation`, `factual`, `conversational`) et `requires_search = intent !== 'conversational'`. `resolveGenerationMode` ne renvoie plus `'gemini'` pour `comparison` (les comparaisons restent en mode extraits ; T5 garantit les extraits de chaque document nommé).

- [ ] **Step 1: Écrire le test qui échoue**

Créer `supabase/functions/baikal-retrieval/routing/analyzer.test.ts` :

```ts
import { assertEquals } from "https://deno.land/std@0.224.0/assert/mod.ts"
import { buildFallbackAnalysis } from "./analyzer.ts"

const DOCS = [{ slug: 'cctp', label: 'CCTP' }, { slug: 'ccap', label: 'CCAP' }]

Deno.test("question de comparaison avec « ? » → intent comparison conservé, format tableau", () => {
  const a = buildFallbackAnalysis("Est-ce qu'il y a des incohérences entre le mémoire technique et le CCTP ?", DOCS)
  assertEquals(a.intent, 'comparison')
  assertEquals(a.requires_search, true)
  assertEquals(a.answer_format, 'table')
})

Deno.test("impératif « Compare … » → comparison (QUESTION_PATTERNS ne rétrograde plus)", () => {
  assertEquals(buildFallbackAnalysis("Compare la garantie entre le CCAP et la NFP 03-001", DOCS).intent, 'comparison')
})

Deno.test("synthèse et citation conservées", () => {
  assertEquals(buildFallbackAnalysis("Peux-tu me résumer le CCTP ?", DOCS).intent, 'synthesis')
  const c = buildFallbackAnalysis("Cite l'article sur les pénalités de retard", DOCS)
  assertEquals(c.intent, 'citation')
  assertEquals(c.answer_format, 'quote')
})

Deno.test("question factuelle → factual", () => {
  assertEquals(buildFallbackAnalysis("Où se trouve le terrain de pétanque ?", DOCS).intent, 'factual')
})

Deno.test("salutation → conversational sans recherche", () => {
  const s = buildFallbackAnalysis("Bonjour", DOCS)
  assertEquals(s.intent, 'conversational')
  assertEquals(s.requires_search, false)
})

Deno.test("documents clés détectés et cross-ref fusionnés", () => {
  const a = buildFallbackAnalysis("Vérifier la conformité du CCTP au DTU 43.1", DOCS)
  assertEquals(a.detected_documents.includes('CCTP'), true)
  assertEquals(a.cross_ref?.is_cross_ref, true)
  assertEquals(a.requires_search, true)
})
```

- [ ] **Step 2: Vérifier qu'il échoue**

Run: `deno test supabase/functions/baikal-retrieval/routing/analyzer.test.ts`
Expected: FAIL — le premier test obtient `factual` au lieu de `comparison` (et « Compare … » aussi).

- [ ] **Step 3: Réécrire `routing/analyzer.ts`**

Remplacer le fichier entier par :

```ts
// ============================================================================
// baikal-retrieval - Routing: Analyse heuristique de la question (Sprint 2, T1)
// ============================================================================
// L'analyse LLM (analyzeQuery, OpenAI) n'était plus appelée depuis la v2.0 :
// supprimée. Ne reste que l'analyse par mots-clés, synchrone et sans I/O.
// L'intent détecté est CONSERVÉ : jusqu'à la v2.1.0, safeRequiresSearch le
// remplaçait par « factual » dès que la question contenait un mot interrogatif
// ou un « ? » — aucune comparaison n'atteignait la production (0 sur 576 requêtes
// journalisées en 30 jours). Les questions de suivi sont condensées en amont
// (routing/condenser.ts) avant d'arriver ici.
// ============================================================================

import type { AnalysisResult, AnswerFormat, DocumentCle } from "../types.ts"
import { detectIntentByKeywords, extractKeywords } from "./safety.ts"
import { detectCrossRef } from "./cross-ref.ts"

const SEARCH_CONFIGS: Record<string, AnalysisResult['search_config']> = {
  factual: { scope: 'narrow', max_files: 2, min_similarity: 0.42, boost_documents: [], file_filter: null },
  synthesis: { scope: 'broad', max_files: 5, min_similarity: 0.35, boost_documents: [], file_filter: null },
  comparison: { scope: 'broad', max_files: 5, min_similarity: 0.35, boost_documents: [], file_filter: null },
  citation: { scope: 'narrow', max_files: 1, min_similarity: 0.6, boost_documents: [], file_filter: null },
  conversational: { scope: 'narrow', max_files: 0, min_similarity: 0.5, boost_documents: [], file_filter: null },
}

const FORMATS: Record<string, AnswerFormat> = {
  factual: 'paragraph', synthesis: 'paragraph', comparison: 'table', citation: 'quote', conversational: 'paragraph',
}

export function buildFallbackAnalysis(
  query: string,
  documentsCles: DocumentCle[],
): AnalysisResult {
  const intent = detectIntentByKeywords(query)
  const requiresSearch = intent !== 'conversational'

  const q = query.toLowerCase()
  const detectedDocs = documentsCles
    .filter(d => q.includes(d.slug.toLowerCase()) || q.includes(d.label.toLowerCase()))
    .map(d => d.label)

  // Cross-ref heuristique (synchrone, 0 ms — aucun I/O)
  const crossRef = detectCrossRef(query)

  const result: AnalysisResult = {
    intent,
    requires_search: requiresSearch,
    rewritten_query: query,
    detected_documents: detectedDocs,
    search_config: SEARCH_CONFIGS[intent] || SEARCH_CONFIGS.factual,
    answer_format: FORMATS[intent] || 'paragraph',
    key_concepts: extractKeywords(query),
    reasoning: 'Analyse par mots-cles',
  }

  if (crossRef.is_cross_ref) {
    result.cross_ref = crossRef
    result.detected_documents = [...new Set([...result.detected_documents, ...crossRef.detected_documents])]
    result.requires_search = true
    console.log(`[retrieval] Cross-ref detected (heuristic): norms=[${crossRef.detected_norms.join(', ')}], docs=[${crossRef.detected_documents.join(', ')}], lot=${crossRef.detected_lot}`)
  }

  return result
}
```

- [ ] **Step 4: Retirer le code devenu mort dans `routing/safety.ts` et `types.ts`**

Dans `safety.ts` : supprimer les sections « QUESTION DETECTION » (`QUESTION_WORDS`, `QUESTION_PATTERNS`, `containsRealQuestion`) et « SAFE OVERRIDE » (`safeRequiresSearch`) ; l'import devient `import type { Intent } from "../types.ts"`. Vérifier qu'aucun autre module ne les référence :

Run: `grep -rn "safeRequiresSearch\|containsRealQuestion\|SafeAnalysisOverride\|analyzeQuery\|parseAnalysisJSON" supabase/functions/baikal-retrieval`
Expected: seule l'interface `SafeAnalysisOverride` dans `types.ts` → la supprimer (bloc `export interface SafeAnalysisOverride { requires_search: boolean; intent: Intent; was_overridden: boolean }`).

- [ ] **Step 5: `index.ts` — version et routage de génération**

Ligne 2 : `// baikal-retrieval v2.2.0 - "Agentic RAG" + Sprint 2 (documents nommés cibles, budget agentique, acces verifie)`.
Ligne 130 : `console.log(\`[retrieval] === v2.2.0 Sprint 2 === Query: "${query.substring(0, 60)}..."\`)`.
Dans `resolveGenerationMode`, supprimer le bloc :

```ts
  if (analysis.intent === 'comparison' && analysis.detected_documents.length > 0) {
    console.log(`[retrieval] 📄 generation_mode: 'gemini' (comparison avec docs=[${analysis.detected_documents.join(', ')}])`)
    return 'gemini'
  }
```

et ajouter au-dessus du `return 'auto'` le commentaire : `// Sprint 2 : les comparaisons restent en mode extraits (recherche ciblée par document nommé, T5) ; la lecture intégrale est demandée explicitement par le frontend (« Approfondir », T7).`

- [ ] **Step 6: Tests et type-check**

Run: `deno test supabase/functions/baikal-retrieval/` puis `deno check supabase/functions/baikal-retrieval/index.ts`
Expected: 74 tests OK (68 + 6) ; `deno check` : **0 erreur** (les 11 erreurs de `analyzer.ts:62-78` ont disparu avec le code).

- [ ] **Step 7: Documentation (accord d'Eric donné au plan)**

Dans `CLAUDE.md` (Baikal), section « RAG Pipeline (baikal-retrieval v2.0) », remplacer la ligne `  ├── Brain (intent detection + query rewriting, integrated)` par `  ├── Analyse heuristique (intent par mots-clés, routing/analyzer.ts) + condensation des suivis (routing/condenser.ts, Gemini flash-lite)`. Dans `.claude/proposed-updates.md`, l'entrée « Le « Brain » LLM (analyzeQuery) n'est jamais appelé en production » passe à `**Statut** : RESOLU (Sprint 2 T1 : code mort supprimé, ligne du pipeline mise à jour — 2026-09-19)`.

- [ ] **Step 8: Commit**

```bash
git add supabase/functions/baikal-retrieval/routing/analyzer.ts supabase/functions/baikal-retrieval/routing/analyzer.test.ts supabase/functions/baikal-retrieval/routing/safety.ts supabase/functions/baikal-retrieval/types.ts supabase/functions/baikal-retrieval/index.ts CLAUDE.md .claude/proposed-updates.md
git commit -m "feat(rag): analyse heuristique — code mort analyzeQuery supprimé, intent conservé (comparison/synthesis/citation)"
```

---

### Task 2: Sécurité — identité du jeton, appartenance vérifiée, fonctions rag fermées à anon

**Files:**
- Create: `supabase/migrations/20260919100000_rag_acces_retrieval.sql`
- Create: `supabase/functions/baikal-retrieval/auth.ts`
- Test: `supabase/functions/baikal-retrieval/auth.test.ts`
- Modify: `supabase/functions/baikal-retrieval/index.ts:61-64,116-134` (env `SUPABASE_ANON_KEY`, contrôle avant le flux SSE, `timings.auth`)
- Modify: `eval/run-eval.ts:188-206,442-446` (Bearer = clé service_role si présente) ; `eval/.env.example`

**Interfaces:**
- Produces (SQL) : `rag.resolve_access(p_user_id uuid, p_project_id uuid, p_org_id uuid) RETURNS TABLE(allowed boolean, effective_org_id uuid, reason text)` — `reason` ∈ `project_member | not_project_member | project_not_found | org_member | not_org_member | no_scope`.
- Produces (TS) : `bearerToken(req: Request): string | null` ; `resolveCaller(token, env, getUserId): Promise<Caller>` avec `Caller = { kind: 'service' } | { kind: 'user'; userId: string } | { kind: 'anonymous'; reason: 'missing_token' | 'anon_key' | 'invalid_token' }` ; `resolveAccess(supabase, userId, projectId, orgId): Promise<AccessDecision>` avec `AccessDecision = { allowed: boolean; effectiveOrgId: string | null; reason: string }`.
- Règle : rôle `service_role` (jeton strictement égal à `SUPABASE_SERVICE_ROLE_KEY`) → le corps de la requête est pris tel quel (harnais d'éval, appels internes). Jeton utilisateur valide → `user_id` = `sub` du jeton (le corps est ignoré), `org_id` = celui décidé par `resolve_access`. Clé anon, jeton absent ou invalide → **401**. Utilisateur non membre → **403**. Les deux refus sont des réponses HTTP JSON (`errorResponse`), jamais des événements SSE.

- [ ] **Step 1: Écrire la migration**

Créer `supabase/migrations/20260919100000_rag_acces_retrieval.sql` :

```sql
-- ============================================================================
-- Sprint 2 (T2) — Contrôle d'accès de baikal-retrieval
-- ============================================================================
-- 1. rag.resolve_access : même prédicat que la RLS SELECT de core.projects.
-- 2. rag.get_agent_context (surcharge avec p_conversation_id) : une conversation
--    fournie n'est reprise que si elle appartient à l'utilisateur.
-- 3. Les fonctions de recherche et de contexte, SECURITY DEFINER et appelées
--    uniquement par des Edge Functions en service_role, ne sont plus exécutables
--    par anon / authenticated (le schéma rag est exposé par PostgREST).
-- ============================================================================

-- 1. Décision d'accès -------------------------------------------------------
CREATE OR REPLACE FUNCTION rag.resolve_access(
  p_user_id uuid,
  p_project_id uuid DEFAULT NULL,
  p_org_id uuid DEFAULT NULL
)
RETURNS TABLE(allowed boolean, effective_org_id uuid, reason text)
LANGUAGE plpgsql
STABLE
SECURITY DEFINER
SET search_path = ''
AS $$
DECLARE
  v_profile_org uuid;
  v_project_org uuid;
BEGIN
  SELECT p.org_id INTO v_profile_org FROM core.profiles p WHERE p.id = p_user_id;

  IF p_project_id IS NOT NULL THEN
    SELECT pr.org_id INTO v_project_org FROM core.projects pr WHERE pr.id = p_project_id;
    IF v_project_org IS NULL THEN
      RETURN QUERY SELECT false, NULL::uuid, 'project_not_found'::text;
      RETURN;
    END IF;
    -- Parité avec la policy projects_select_secure
    IF core.rls_is_super_admin(p_user_id)
       OR (core.rls_is_org_admin(p_user_id) AND v_project_org = v_profile_org)
       OR p_project_id = ANY(core.rls_get_user_project_ids(p_user_id)) THEN
      RETURN QUERY SELECT true, v_project_org, 'project_member'::text;
      RETURN;
    END IF;
    RETURN QUERY SELECT false, NULL::uuid, 'not_project_member'::text;
    RETURN;
  END IF;

  IF p_org_id IS NOT NULL THEN
    -- Parité avec la policy org_members_select_own_org
    IF core.rls_is_super_admin(p_user_id) OR p_org_id = v_profile_org THEN
      RETURN QUERY SELECT true, p_org_id, 'org_member'::text;
      RETURN;
    END IF;
    RETURN QUERY SELECT false, NULL::uuid, 'not_org_member'::text;
    RETURN;
  END IF;

  RETURN QUERY SELECT true, v_profile_org, 'no_scope'::text;
END;
$$;

-- 2. get_agent_context : conversation fournie → doit appartenir à l'utilisateur
CREATE OR REPLACE FUNCTION rag.get_agent_context(
  p_user_id uuid,
  p_org_id uuid DEFAULT NULL::uuid,
  p_project_id uuid DEFAULT NULL::uuid,
  p_app_id text DEFAULT NULL::text,
  p_agent_type text DEFAULT 'librarian'::text,
  p_conversation_id uuid DEFAULT NULL::uuid,
  p_conversation_timeout_minutes integer DEFAULT 30,
  p_context_messages_count integer DEFAULT 4
)
RETURNS TABLE(out_effective_org_id uuid, out_effective_app_id text, out_system_prompt text, out_gemini_system_prompt text, out_parameters jsonb, out_config_source text, out_project_identity jsonb, out_conversation_id uuid, out_conversation_summary text, out_conversation_first_message text, out_recent_messages jsonb, out_message_count integer, out_previous_source_file_ids uuid[], out_documents_cles jsonb)
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path TO 'rag', 'core', 'config', 'public'
AS $function$
DECLARE
    v_effective_org_id uuid;
    v_effective_app_id text;
    v_system_prompt text;
    v_gemini_system_prompt text;
    v_parameters jsonb;
    v_config_source text := 'fallback';
    v_project_identity jsonb;
    v_conversation_id uuid;
    v_conversation_summary text;
    v_conversation_first_message text;
    v_recent_messages jsonb;
    v_message_count int := 0;
    v_previous_source_file_ids uuid[];
    v_documents_cles jsonb;
    v_parent_concept_id uuid;
    v_timeout_threshold timestamptz;
    v_last_assistant_sources jsonb;
BEGIN
    -- 1. RÉSOUDRE PROFILE
    SELECT
        COALESCE(p_org_id, p.org_id),
        COALESCE(p_app_id, p.app_id, 'arpet')
    INTO v_effective_org_id, v_effective_app_id
    FROM core.profiles p
    WHERE p.id = p_user_id;

    IF v_effective_app_id IS NULL THEN
        v_effective_app_id := COALESCE(p_app_id, 'arpet');
    END IF;

    -- 2. RÉCUPÉRER CONFIG AGENT (avec p_agent_type dynamique)
    IF v_effective_org_id IS NOT NULL THEN
        SELECT ap.system_prompt, ap.gemini_system_prompt, ap.parameters, 'org'
        INTO v_system_prompt, v_gemini_system_prompt, v_parameters, v_config_source
        FROM config.agent_prompts ap
        WHERE ap.agent_type = p_agent_type
          AND ap.is_active = true
          AND ap.org_id = v_effective_org_id
        LIMIT 1;
    END IF;

    IF v_system_prompt IS NULL THEN
        SELECT ap.system_prompt, ap.gemini_system_prompt, ap.parameters, 'app'
        INTO v_system_prompt, v_gemini_system_prompt, v_parameters, v_config_source
        FROM config.agent_prompts ap
        WHERE ap.agent_type = p_agent_type
          AND ap.is_active = true
          AND ap.app_id = v_effective_app_id
          AND ap.org_id IS NULL
        LIMIT 1;
    END IF;

    IF v_system_prompt IS NULL THEN
        SELECT ap.system_prompt, ap.gemini_system_prompt, ap.parameters, 'global'
        INTO v_system_prompt, v_gemini_system_prompt, v_parameters, v_config_source
        FROM config.agent_prompts ap
        WHERE ap.agent_type = p_agent_type
          AND ap.is_active = true
          AND ap.app_id IS NULL
          AND ap.org_id IS NULL
        LIMIT 1;
    END IF;

    IF v_parameters IS NULL THEN
        v_parameters := '{}'::jsonb;
        v_config_source := 'fallback';
    END IF;

    -- 3. RÉCUPÉRER IDENTITÉ PROJET
    IF p_project_id IS NOT NULL THEN
        SELECT p.identity
        INTO v_project_identity
        FROM core.projects p
        WHERE p.id = p_project_id;
    END IF;

    -- 4. FIND OR CREATE CONVERSATION
    -- v2.2 (Sprint 2, sécurité) : une conversation fournie n'est reprise que si elle
    -- appartient à l'utilisateur ; sinon elle est ignorée (find-or-create ci-dessous).
    IF p_conversation_id IS NOT NULL THEN
        SELECT c.id, c.summary
        INTO v_conversation_id, v_conversation_summary
        FROM rag.conversations c
        WHERE c.id = p_conversation_id
          AND c.user_id = p_user_id;

        IF v_conversation_id IS NULL
           AND NOT EXISTS (SELECT 1 FROM rag.conversations c WHERE c.id = p_conversation_id) THEN
            INSERT INTO rag.conversations (id, user_id, org_id, project_id, app_id)
            VALUES (p_conversation_id, p_user_id, v_effective_org_id, p_project_id, v_effective_app_id)
            RETURNING id INTO v_conversation_id;
        END IF;
    END IF;

    IF v_conversation_id IS NULL THEN
        v_timeout_threshold := NOW() - (p_conversation_timeout_minutes || ' minutes')::interval;

        SELECT c.id, c.summary
        INTO v_conversation_id, v_conversation_summary
        FROM rag.conversations c
        WHERE c.user_id = p_user_id
          AND c.app_id = v_effective_app_id
          AND (c.org_id = v_effective_org_id OR (c.org_id IS NULL AND v_effective_org_id IS NULL))
          AND (c.project_id = p_project_id OR (c.project_id IS NULL AND p_project_id IS NULL))
          AND c.updated_at > v_timeout_threshold
        ORDER BY c.updated_at DESC
        LIMIT 1;

        IF v_conversation_id IS NULL THEN
            INSERT INTO rag.conversations (user_id, org_id, project_id, app_id)
            VALUES (p_user_id, v_effective_org_id, p_project_id, v_effective_app_id)
            RETURNING id INTO v_conversation_id;
        END IF;
    END IF;

    -- Contexte conversation
    SELECT
        (SELECT content FROM rag.messages WHERE conversation_id = v_conversation_id AND role = 'user' ORDER BY created_at ASC LIMIT 1),
        COUNT(*)::int
    INTO v_conversation_first_message, v_message_count
    FROM rag.messages
    WHERE conversation_id = v_conversation_id;

    SELECT jsonb_agg(
        jsonb_build_object(
            'role', m.role,
            'content', m.content,
            'created_at', m.created_at,
            'sources', m.sources
        ) ORDER BY m.created_at DESC
    )
    INTO v_recent_messages
    FROM (
        SELECT role, content, created_at, sources
        FROM rag.messages
        WHERE conversation_id = v_conversation_id
        ORDER BY created_at DESC
        LIMIT p_context_messages_count
    ) m;

    -- 4b. SOURCE_FILE_ID DU DERNIER MESSAGE ASSISTANT
    SELECT m.sources
    INTO v_last_assistant_sources
    FROM rag.messages m
    WHERE m.conversation_id = v_conversation_id
      AND m.role = 'assistant'
      AND m.sources IS NOT NULL
    ORDER BY m.created_at DESC
    LIMIT 1;

    IF v_last_assistant_sources IS NOT NULL THEN
        WITH parsed_sources AS (
            SELECT
                CASE
                    WHEN jsonb_typeof(v_last_assistant_sources) = 'array' THEN v_last_assistant_sources
                    WHEN jsonb_typeof(v_last_assistant_sources) = 'string' THEN (v_last_assistant_sources #>> '{}')::jsonb
                    ELSE '[]'::jsonb
                END AS sources_array
        ),
        extracted_ids AS (
            SELECT DISTINCT (elem->>'source_file_id')::uuid AS file_id
            FROM parsed_sources ps
            CROSS JOIN LATERAL jsonb_array_elements(ps.sources_array) AS elem
            WHERE elem->>'source_file_id' IS NOT NULL
        )
        SELECT ARRAY_AGG(file_id)
        INTO v_previous_source_file_ids
        FROM extracted_ids;
    END IF;

    -- 5. DOCUMENTS CLÉS
    SELECT c.id
    INTO v_parent_concept_id
    FROM config.concepts c
    WHERE c.slug = 'documents_cles'
      AND c.status = 'active'
      AND v_effective_app_id = ANY(c.target_apps)
    LIMIT 1;

    IF v_parent_concept_id IS NOT NULL THEN
        SELECT jsonb_agg(
            jsonb_build_object('slug', c.slug, 'label', c.label)
        )
        INTO v_documents_cles
        FROM config.concepts c
        WHERE c.parent_id = v_parent_concept_id
          AND c.status = 'active';
    END IF;

    RETURN QUERY SELECT
        v_effective_org_id,
        v_effective_app_id,
        v_system_prompt,
        v_gemini_system_prompt,
        v_parameters,
        v_config_source,
        v_project_identity,
        v_conversation_id,
        v_conversation_summary,
        v_conversation_first_message,
        COALESCE(v_recent_messages, '[]'::jsonb),
        v_message_count,
        COALESCE(v_previous_source_file_ids, ARRAY[]::uuid[]),
        COALESCE(v_documents_cles, '[]'::jsonb);
END;
$function$;

-- 3. Fermeture à anon / authenticated des fonctions internes aux Edge Functions -----
-- Toutes appelées en service_role (baikal-retrieval, baikal-brain-v3, baikal-librarian-v3/v4,
-- generate-document). Aucun wrapper public/arpet ne les appelle ; ARPET n'appelle en direct que
-- rag.delete_conversation et rag.close_conversation, non touchées.
REVOKE EXECUTE ON FUNCTION
  rag.resolve_access(uuid, uuid, uuid),
  rag.get_agent_context(uuid, uuid, uuid, text, text, integer, integer),
  rag.get_agent_context(uuid, uuid, uuid, text, text, uuid, integer, integer),
  rag.find_or_create_conversation(uuid, uuid, uuid, text, integer),
  rag.get_conversation_context(uuid, integer),
  rag.search_qa_memory(vector, uuid, uuid, double precision, integer),
  rag.add_message(uuid, text, text, jsonb, text, integer),
  rag.increment_qa_usage(uuid),
  rag.match_documents_v12(vector, text, uuid, uuid, uuid, text, integer, double precision, boolean, boolean, boolean, boolean, text[], uuid[], text[], boolean, integer, double precision),
  rag.match_documents_v13(vector, text, uuid, uuid, uuid, text, integer, double precision, boolean, boolean, boolean, boolean, text[], uuid[], text[], boolean, integer, double precision, integer[], boolean),
  rag.match_documents_v14(vector, text, uuid, uuid, uuid, text, integer, double precision, boolean, boolean, boolean, boolean, text[], uuid[], text[], boolean, integer, double precision, integer[], boolean),
  rag.match_documents_v15(vector, text, uuid, uuid, uuid, text, integer, double precision, boolean, boolean, boolean, boolean, text[], uuid[], text[], boolean, integer, double precision, integer[], boolean, double precision, integer),
  rag.match_files_v1(vector, double precision, integer, text, uuid, uuid, uuid, boolean, boolean, boolean, boolean),
  rag.match_files_v2(vector, double precision, integer, text, uuid, uuid, uuid, boolean, boolean, boolean, boolean),
  rag.match_documents_orphans_v1(vector, double precision, integer, text, uuid, uuid, uuid, boolean, boolean, boolean, boolean)
FROM PUBLIC, anon, authenticated;

GRANT EXECUTE ON FUNCTION
  rag.resolve_access(uuid, uuid, uuid),
  rag.get_agent_context(uuid, uuid, uuid, text, text, integer, integer),
  rag.get_agent_context(uuid, uuid, uuid, text, text, uuid, integer, integer),
  rag.find_or_create_conversation(uuid, uuid, uuid, text, integer),
  rag.get_conversation_context(uuid, integer),
  rag.search_qa_memory(vector, uuid, uuid, double precision, integer),
  rag.add_message(uuid, text, text, jsonb, text, integer),
  rag.increment_qa_usage(uuid),
  rag.match_documents_v12(vector, text, uuid, uuid, uuid, text, integer, double precision, boolean, boolean, boolean, boolean, text[], uuid[], text[], boolean, integer, double precision),
  rag.match_documents_v13(vector, text, uuid, uuid, uuid, text, integer, double precision, boolean, boolean, boolean, boolean, text[], uuid[], text[], boolean, integer, double precision, integer[], boolean),
  rag.match_documents_v14(vector, text, uuid, uuid, uuid, text, integer, double precision, boolean, boolean, boolean, boolean, text[], uuid[], text[], boolean, integer, double precision, integer[], boolean),
  rag.match_documents_v15(vector, text, uuid, uuid, uuid, text, integer, double precision, boolean, boolean, boolean, boolean, text[], uuid[], text[], boolean, integer, double precision, integer[], boolean, double precision, integer),
  rag.match_files_v1(vector, double precision, integer, text, uuid, uuid, uuid, boolean, boolean, boolean, boolean),
  rag.match_files_v2(vector, double precision, integer, text, uuid, uuid, uuid, boolean, boolean, boolean, boolean),
  rag.match_documents_orphans_v1(vector, double precision, integer, text, uuid, uuid, uuid, boolean, boolean, boolean, boolean)
TO service_role;
```

La migration n'est **pas** appliquée par l'implémenteur (porte G1). Le contrôleur, avant de la soumettre, vérifie via MCP n8n que les workflows FLUX qui touchent Supabase utilisent un credential service_role (aucun n'appelle ces fonctions d'après le code, mais la vérification est documentée dans le registre SDD).

- [ ] **Step 2: Écrire le test de `auth.ts` qui échoue**

Créer `supabase/functions/baikal-retrieval/auth.test.ts` :

```ts
import { assertEquals } from "https://deno.land/std@0.224.0/assert/mod.ts"
import { bearerToken, resolveCaller } from "./auth.ts"

const ENV = { serviceRoleKey: 'service-secret', anonKey: 'anon-public' }
const userLookup = (users: Record<string, string>) => async (jwt: string) => users[jwt] ? { id: users[jwt] } : null

Deno.test("bearerToken lit l'en-tête Authorization, insensible à la casse", () => {
  assertEquals(bearerToken(new Request('http://x', { headers: { Authorization: 'Bearer abc.def' } })), 'abc.def')
  assertEquals(bearerToken(new Request('http://x', { headers: { authorization: 'bearer abc' } })), 'abc')
  assertEquals(bearerToken(new Request('http://x')), null)
  assertEquals(bearerToken(new Request('http://x', { headers: { Authorization: 'Basic abc' } })), null)
})

Deno.test("clé service_role → service, corps de requête de confiance", async () => {
  assertEquals(await resolveCaller('service-secret', ENV, userLookup({})), { kind: 'service' })
})

Deno.test("clé anon ou jeton absent → anonymous (401)", async () => {
  assertEquals(await resolveCaller('anon-public', ENV, userLookup({})), { kind: 'anonymous', reason: 'anon_key' })
  assertEquals(await resolveCaller(null, ENV, userLookup({})), { kind: 'anonymous', reason: 'missing_token' })
})

Deno.test("jeton utilisateur valide → user avec le sub ; invalide → anonymous", async () => {
  const lookup = userLookup({ 'jwt-eric': 'u-eric' })
  assertEquals(await resolveCaller('jwt-eric', ENV, lookup), { kind: 'user', userId: 'u-eric' })
  assertEquals(await resolveCaller('jwt-forge', ENV, lookup), { kind: 'anonymous', reason: 'invalid_token' })
})

Deno.test("un getUser qui lève ne fait pas tomber la requête : anonymous", async () => {
  const boom = async () => { throw new Error('auth down') }
  assertEquals(await resolveCaller('jwt-x', ENV, boom), { kind: 'anonymous', reason: 'invalid_token' })
})
```

- [ ] **Step 3: Vérifier qu'il échoue**

Run: `deno test supabase/functions/baikal-retrieval/auth.test.ts`
Expected: FAIL — `Module not found "./auth.ts"`.

- [ ] **Step 4: Écrire `auth.ts`**

```ts
// ============================================================================
// baikal-retrieval - Auth : identité du jeton et décision d'accès (Sprint 2, T2)
// ============================================================================
// L'EF tourne en service_role : jusqu'à la v2.1.0, user_id / org_id / project_id
// venaient du corps de la requête et n'étaient jamais confrontés au jeton — la clé
// anon publique suffisait pour lire les documents de n'importe quel projet.
// Désormais : jeton service_role → corps de confiance (harnais d'éval, appels
// internes) ; jeton utilisateur → user_id = sub, appartenance vérifiée par
// rag.resolve_access (même prédicat que la RLS de core.projects) ; clé anon,
// jeton absent ou invalide → 401.
// ============================================================================

import type { Supabase } from "./types.ts"

export type Caller =
  | { kind: 'service' }
  | { kind: 'user'; userId: string }
  | { kind: 'anonymous'; reason: 'missing_token' | 'anon_key' | 'invalid_token' }

export interface AccessDecision {
  allowed: boolean
  effectiveOrgId: string | null
  reason: string
}

export function bearerToken(req: Request): string | null {
  const header = req.headers.get('authorization')
  if (!header) return null
  const m = header.match(/^Bearer\s+(.+)$/i)
  return m ? m[1].trim() : null
}

/**
 * `getUserId` valide le jeton auprès de GoTrue (supabase.auth.getUser) ; il est injecté
 * pour rester testable sans réseau. Toute erreur de validation vaut jeton invalide.
 */
export async function resolveCaller(
  token: string | null,
  env: { serviceRoleKey: string; anonKey: string },
  getUserId: (jwt: string) => Promise<{ id: string } | null>,
): Promise<Caller> {
  if (!token) return { kind: 'anonymous', reason: 'missing_token' }
  if (token === env.serviceRoleKey) return { kind: 'service' }
  if (token === env.anonKey) return { kind: 'anonymous', reason: 'anon_key' }
  try {
    const user = await getUserId(token)
    if (!user?.id) return { kind: 'anonymous', reason: 'invalid_token' }
    return { kind: 'user', userId: user.id }
  } catch (err) {
    console.warn('[auth] validation du jeton impossible :', err instanceof Error ? err.message : err)
    return { kind: 'anonymous', reason: 'invalid_token' }
  }
}

export async function getUserIdFromJwt(supabase: Supabase, jwt: string): Promise<{ id: string } | null> {
  const { data, error } = await supabase.auth.getUser(jwt)
  if (error || !data?.user) return null
  return { id: data.user.id }
}

export async function resolveAccess(
  supabase: Supabase,
  userId: string,
  projectId: string | undefined,
  orgId: string | undefined,
): Promise<AccessDecision> {
  const { data, error } = await supabase.schema('rag').rpc('resolve_access', {
    p_user_id: userId,
    p_project_id: projectId || null,
    p_org_id: orgId || null,
  })
  if (error) throw new Error(`Access check error: ${error.message}`)
  const row = Array.isArray(data) ? data[0] : data
  return {
    allowed: row?.allowed === true,
    effectiveOrgId: (row?.effective_org_id as string | null) ?? null,
    reason: (row?.reason as string) ?? 'unknown',
  }
}
```

- [ ] **Step 5: Vérifier que le test passe**

Run: `deno test supabase/functions/baikal-retrieval/auth.test.ts`
Expected: 5 tests OK.

- [ ] **Step 6: Câbler dans `index.ts`**

Ajouter l'import `import { bearerToken, resolveCaller, getUserIdFromJwt, resolveAccess } from "./auth.ts"` et l'env `const SUPABASE_ANON_KEY = Deno.env.get("SUPABASE_ANON_KEY")!` (après `SUPABASE_SERVICE_ROLE_KEY`). Remplacer le bloc de lecture du corps (lignes 116-134) par :

```ts
    const body: RequestBody = await req.json()
    const {
      query, user_id: bodyUserId, org_id: bodyOrgId, project_id,
      app_id = 'arpet', conversation_id,
      generation_mode = 'auto', stream = true,
      include_app_layer = true, include_org_layer = true,
      include_project_layer = true, include_user_layer = false,
      filter_source_types,
      enable_suggestions = false,
    } = body

    if (!query?.trim()) return errorResponse("Query is required")

    const supabase = createClient(SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY)
    const timer = createTimer()

    // Sprint 2 (T2) — identité et appartenance AVANT d'ouvrir le flux SSE :
    // un refus est une réponse HTTP (401 / 403), jamais un événement.
    const caller = await resolveCaller(
      bearerToken(req),
      { serviceRoleKey: SUPABASE_SERVICE_ROLE_KEY, anonKey: SUPABASE_ANON_KEY },
      (jwt) => getUserIdFromJwt(supabase, jwt),
    )
    if (caller.kind === 'anonymous') {
      console.warn(`[auth] 401 (${caller.reason})`)
      return errorResponse("Authentification requise", 401)
    }
    let org_id: string | undefined = bodyOrgId
    if (caller.kind === 'user') {
      const access = await resolveAccess(supabase, caller.userId, project_id, bodyOrgId)
      if (!access.allowed) {
        console.warn(`[auth] 403 (${access.reason}) user=${caller.userId} project=${project_id ?? '-'} org=${bodyOrgId ?? '-'}`)
        return errorResponse("Acces refuse", 403)
      }
      org_id = access.effectiveOrgId ?? undefined
    }
    // `const` de type string : un `let` perdrait son rétrécissement de type dans la closure start(controller)
    const user_id: string = caller.kind === 'user' ? caller.userId : (bodyUserId ?? '')
    if (!user_id) return errorResponse("user_id is required")
    const authMs = timer.mark('auth')
    console.log(`[auth] ${caller.kind} en ${authMs}ms`)

    console.log(`[retrieval] === v2.2.0 Sprint 2 === Query: "${query.substring(0, 60)}..."`)
    const layerFlags = { app: include_app_layer, org: include_org_layer, project: include_project_layer, user: include_user_layer }
```

Dans `start(controller)`, juste après la création de `metrics`, ajouter `metrics.timings.auth = authMs`. Le reste du handler continue d'utiliser `user_id` et `org_id` (désormais les valeurs vérifiées). Vérifier que `RequestBody.user_id` reste `string` (le corps peut l'omettre pour un utilisateur : passer le champ en `user_id?: string`).

- [ ] **Step 7: Harnais d'éval en service_role**

Dans `eval/run-eval.ts` : renommer le paramètre `anonKey` de `callRetrieval` et `evalEntry` en `bearer` ; l'en-tête devient `'Authorization': \`Bearer ${bearer}\`` et `'apikey': anonKey` reste la clé anon (lue une fois au niveau module : `let ANON_KEY = ''`, affectée dans `main`). Dans `main` :

```ts
  const anonKey = env.SUPABASE_ANON_KEY
  if (!anonKey) {
    console.error('❌ SUPABASE_ANON_KEY manquant (eval/.env — voir eval/.env.example)')
    Deno.exit(1)
  }
  ANON_KEY = anonKey
  // Sprint 2 (T2) : baikal-retrieval refuse la clé anon (401) ; le banc s'authentifie en service_role
  // et l'EF prend alors user_id / org_id / project_id du corps (contextes d'eval/config.json).
  const bearer = env.SUPABASE_SERVICE_ROLE_KEY || anonKey
  if (!env.SUPABASE_SERVICE_ROLE_KEY) {
    console.warn('⚠️  SUPABASE_SERVICE_ROLE_KEY absente de eval/.env : depuis v2.2.0 l\'EF répondra 401 avec la clé anon')
  }
```

et passer `bearer` aux appels (`--smoke` compris). Dans `eval/.env.example`, remplacer le commentaire de `SUPABASE_SERVICE_ROLE_KEY` par : `# Requise depuis v2.2.0 pour le banc (baikal-retrieval refuse la clé anon) et pour le juge (contenu complet des chunks). NE JAMAIS COMMITER.`

- [ ] **Step 8: Tests, type-check, commit**

Run: `deno test supabase/functions/baikal-retrieval/ && deno check supabase/functions/baikal-retrieval/index.ts && deno check eval/run-eval.ts`
Expected: 79 tests OK ; 0 erreur.

```bash
git add supabase/migrations/20260919100000_rag_acces_retrieval.sql supabase/functions/baikal-retrieval/auth.ts supabase/functions/baikal-retrieval/auth.test.ts supabase/functions/baikal-retrieval/index.ts supabase/functions/baikal-retrieval/types.ts eval/run-eval.ts eval/.env.example
git commit -m "feat(rag): accès vérifié — identité du jeton, rag.resolve_access (parité RLS projets), fonctions rag fermées à anon"
```

---

### Task 3: Condenser — la question réécrite ne recopie jamais la réponse précédente (R-S1a)

**Files:**
- Modify: `supabase/functions/baikal-retrieval/routing/condenser.ts:33-64,92-100`
- Test: `supabase/functions/baikal-retrieval/routing/condenser.test.ts`

**Interfaces:**
- Produces: `copiesAssistant(candidate: string, recent: ConversationMessage[]): boolean` (pur) — vrai si la réécriture contient 8 mots consécutifs d'un message assistant de l'historique. `condenseQuery` conserve la question brute dans ce cas.

- [ ] **Step 1: Écrire les tests qui échouent**

Ajouter à `routing/condenser.test.ts` :

```ts
import { copiesAssistant } from "./condenser.ts"

Deno.test("prompt : interdit la recopie des réponses de l'assistant, avec exemple", () => {
  const p = buildCondensePrompt("dans le ccap ?", [
    { role: "assistant", content: "Le projet de décompte final doit intégrer la formule de révision de l'article 18.2." },
    { role: "user", content: "Le marché est actualisable ou révisable ?" },
  ] as never)
  assert(/n['’]y recopie jamais/i.test(p))
  assert(p.includes("Le marché est-il actualisable ou révisable d'après le CCAP ?"))
})

Deno.test("copiesAssistant : 8 mots consécutifs d'une réponse → recopie détectée", () => {
  const recent = [
    { role: "assistant", content: "Le projet de décompte final doit intégrer l'application de la formule de révision figurant à l'article 18.2 si le marché est concerné." },
    { role: "user", content: "Le marché est actualisable ou révisable ?" },
  ] as never
  assertEquals(copiesAssistant("Le projet de décompte final doit intégrer l'application de la formule de révision dans le CCAP ?", recent), true)
  assertEquals(copiesAssistant("Le marché est-il actualisable ou révisable d'après le CCAP ?", recent), false)
})

Deno.test("copiesAssistant : les mots de l'utilisateur ne comptent pas, seuls ceux de l'assistant", () => {
  const recent = [
    { role: "user", content: "Quel est le délai global d'exécution des travaux d'après le mémoire technique du marché ?" },
  ] as never
  assertEquals(copiesAssistant("Quel est le délai global d'exécution des travaux d'après le mémoire technique du marché si on le dépasse ?", recent), false)
})
```

- [ ] **Step 2: Vérifier qu'ils échouent**

Run: `deno test supabase/functions/baikal-retrieval/routing/condenser.test.ts`
Expected: FAIL — `copiesAssistant` n'est pas exporté ; le prompt ne contient pas la règle.

- [ ] **Step 3: Implémenter**

Dans `buildCondensePrompt`, remplacer le tableau de lignes par :

```ts
  return [
    "Tu réécris la dernière question d'un conducteur de travaux en une question autonome et complète,",
    "en résolvant les références implicites (« la », « il », « et pour… ») à partir de l'historique.",
    "Conserve les termes techniques, les numéros d'article et les noms de documents tels quels.",
    "La question réécrite reformule UNIQUEMENT ce que l'utilisateur demande : n'y recopie jamais une phrase,",
    "un chiffre ou une conclusion des réponses de l'ASSISTANT — l'historique sert à retrouver le sujet, pas à répondre.",
    "Exemple : USER « Le marché est actualisable ou révisable ? » / ASSISTANT « … formule de révision à l'article 18.2 … » /",
    "DERNIÈRE QUESTION « dans le ccap ? » → « Le marché est-il actualisable ou révisable d'après le CCAP ? »",
    "Réponds uniquement par la question réécrite, sans commentaire ni guillemets.",
    "Si la question est déjà autonome, renvoie-la telle quelle.",
    "",
    "HISTORIQUE :",
    history,
    "",
    "DERNIÈRE QUESTION :",
    query.trim(),
  ].join('\n')
```

Ajouter après `looksSane` :

```ts
const COPY_WINDOW_WORDS = 8

function contentWords(s: string): string[] {
  return s.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '')
    .replace(/[^a-z0-9]+/g, ' ').trim().split(/\s+/).filter(Boolean)
}

/**
 * Garde-fou (C5-002, C5-003) : le modèle recopie parfois une phrase de la réponse précédente
 * dans la question réécrite. Toute fenêtre de 8 mots consécutifs d'un message ASSISTANT
 * retrouvée dans la réécriture vaut recopie → la question brute est conservée.
 */
export function copiesAssistant(candidate: string, recent: ConversationMessage[]): boolean {
  const cand = ' ' + contentWords(candidate).join(' ') + ' '
  for (const m of recent) {
    if (m.role !== 'assistant') continue
    const w = contentWords(m.content || '')
    for (let i = 0; i + COPY_WINDOW_WORDS <= w.length; i++) {
      if (cand.includes(' ' + w.slice(i, i + COPY_WINDOW_WORDS).join(' ') + ' ')) return true
    }
  }
  return false
}
```

Dans `condenseQuery`, après le contrôle `looksSane` :

```ts
    if (copiesAssistant(candidate, recent)) {
      console.warn(`[condenser] réécriture qui recopie la réponse précédente, question brute conservée: "${candidate.substring(0, 80)}"`)
      return query
    }
```

- [ ] **Step 4: Tests, commit**

Run: `deno test supabase/functions/baikal-retrieval/routing/condenser.test.ts`
Expected: 8 tests OK.

```bash
git add supabase/functions/baikal-retrieval/routing/condenser.ts supabase/functions/baikal-retrieval/routing/condenser.test.ts
git commit -m "fix(rag): condenser — règle et garde contre la recopie de la réponse précédente (C5-002, C5-003)"
```

---

### Task 4: Documents nommés — identifiants de fichiers, `ccag`, repli couche application (R-S1b)

**Files:**
- Modify: `supabase/functions/baikal-retrieval/types.ts:171-203` (`NamedCandidate`, `NamedDocumentResolution`, `CandidatesByType`)
- Modify: `supabase/functions/baikal-retrieval/routing/named-documents.ts:24-47,216-272,316-376`
- Modify: `supabase/functions/baikal-retrieval/generation/prompt.ts:207-232` (`formatProjectIdentity`)
- Modify: `supabase/functions/baikal-retrieval/index.ts:172,194` (`fetchNamedDocumentCandidates` reçoit `app_id`)
- Test: `routing/named-documents.test.ts`, `generation/prompt.test.ts`

**Interfaces:**
- Produces (types) :
  ```ts
  export type NamedDocumentLayer = 'project' | 'app'
  export interface NamedCandidate { fileId: string; name: string; searchText: string; layer: NamedDocumentLayer }
  export interface NamedDocumentResolution {
    phrase: string; type: NamedDocumentType; qualifiers: string[]
    found: string[]; found_file_ids: string[]      // alignés, même longueur (max MAX_LISTED)
    similar: string[]; status: NamedDocumentStatus; total: number; truncated: boolean
    layer: NamedDocumentLayer | null               // couche des candidats (null si aucun)
    targeted_chunks?: number                       // posé par T5
  }
  ```
- Produces : `fetchNamedDocumentCandidates(supabase, projectId, appId, named)` — projet d'abord ; si aucun fichier du type dans le projet, repli sur la couche application (`layer='app'`, `app_id`, `project_id` NULL). `formatNamedDocumentsBlock` écrit « document de reference de la couche application » pour `layer='app'`.
- Consumed by T5 (`found_file_ids`, `layer`) et T7.

- [ ] **Step 1: Écrire les tests qui échouent**

Ajouter à `routing/named-documents.test.ts` (les fixtures de candidats existantes passent de `{ name, searchText }` à `{ fileId, name, searchText, layer: 'project' }` — mettre à jour les fixtures existantes avec `fileId: 'f-' + name` et `layer: 'project'` sans changer les assertions) :

```ts
Deno.test("extraction : CCAG est de nouveau un type nommé", () => {
  const r = extractNamedDocuments("Quelles informations sont dans l'article 2 Définitions du CCAG ?")
  assertEquals(r.map(n => n.type), ['ccag'])
})

Deno.test("appariement : found_file_ids alignés sur found", () => {
  const cands = [
    { fileId: 'f1', name: 'CCTP - Lot N°02 ETANCHEITE.pdf', searchText: 'cctp - lot n°02 etancheite.pdf', layer: 'project' as const },
    { fileId: 'f2', name: 'CCTP - Lot N°07 PLÂTRERIE.pdf', searchText: 'cctp - lot n°07 platrerie.pdf', layer: 'project' as const },
  ]
  const r = matchNamedDocument({ type: 'cctp', phrase: 'CCTP du lot 07', qualifiers: ['lot', '07'] }, cands)
  assertEquals(r.found, ['CCTP - Lot N°07 PLÂTRERIE.pdf'])
  assertEquals(r.found_file_ids, ['f2'])
  assertEquals(r.layer, 'project')
  const all = matchNamedDocument({ type: 'cctp', phrase: 'CCTP', qualifiers: [] }, cands)
  assertEquals(all.found_file_ids, ['f1', 'f2'])
})

Deno.test("bloc : un document de la couche application est nommé comme référence commune", () => {
  const block = formatNamedDocumentsBlock([{
    phrase: 'CCAG', type: 'ccag', qualifiers: [], found: ['CCAG'], found_file_ids: ['5ba512af'], similar: [],
    status: 'found', total: 1, truncated: false, layer: 'app',
  }])
  assert(block!.includes('« CCAG » → CCAG (document de reference de la couche application, commun a tous les projets)'))
})

Deno.test("bloc : aucun candidat → AUCUN, layer null", () => {
  const block = formatNamedDocumentsBlock([{
    phrase: 'DOE', type: 'doe', qualifiers: [], found: [], found_file_ids: [], similar: [],
    status: 'no_candidate', total: 0, truncated: false, layer: null,
  }])
  assert(block!.includes('AUCUN fichier de ce type dans le projet'))
})
```

Ajouter à `generation/prompt.test.ts` :

```ts
Deno.test("marché privé : le CCAG de référence ne s'applique pas, mais un CCAG propre au projet fait foi", () => {
  const p = buildSystemPrompt(null, { ...CTX, projectIdentity: { market_type: 'prive' } } as never, [], 'factual', 'paragraph', [], false, FEATURES)
  assert(p.includes("REGLE MARCHE PRIVE"))
  assert(p.includes("Si le projet contient son propre document CCAG"))
})
```

(`CTX` et `FEATURES` : réutiliser les fixtures déjà définies dans ce fichier de test ; si elles portent d'autres noms, prendre ceux-là.)

- [ ] **Step 2: Vérifier qu'ils échouent**

Run: `deno test supabase/functions/baikal-retrieval/routing/named-documents.test.ts supabase/functions/baikal-retrieval/generation/prompt.test.ts`
Expected: FAIL (type `ccag` non extrait, `found_file_ids` undefined, texte du bloc, règle absente).

- [ ] **Step 3: Types**

Dans `types.ts`, remplacer les définitions de `NamedCandidate`, `CandidatesByType` et `NamedDocumentResolution` par :

```ts
export type NamedDocumentLayer = 'project' | 'app'

/** Un fichier candidat : identifiant, nom montré au modèle, texte d'appariement, couche. */
export interface NamedCandidate {
  fileId: string
  name: string            // display_name || original_filename (nom présenté)
  searchText: string      // normalizeName(original_filename + " " + display_name) : les deux noms
  layer: NamedDocumentLayer
}

/** Candidats par type, résultat du réseau. `null` = requête en erreur → statut unknown. */
export type CandidatesByType = Map<NamedDocumentType, { candidates: NamedCandidate[]; truncated: boolean } | null>

/** Résolution d'une mention contre les fichiers du projet (ou de la couche application). */
export interface NamedDocumentResolution {
  phrase: string
  type: NamedDocumentType
  qualifiers: string[]      // qualifiants effectifs (mots du nom du projet retirés) — affichés dans le bloc
  found: string[]           // fichiers dont le nom contient tous les qualifiants (max MAX_LISTED)
  found_file_ids: string[]  // identifiants sources.files alignés sur found (Sprint 2 : recherche ciblée, lecture intégrale)
  similar: string[]         // à défaut : fichiers du même type (max MAX_LISTED)
  status: NamedDocumentStatus
  total: number             // nombre réel de fichiers derrière found (si found) ou similar, avant troncature
  truncated: boolean        // la liste des candidats a atteint MAX_CANDIDATES : elle ne prouve aucune absence
  layer: NamedDocumentLayer | null   // couche des candidats : projet, ou repli couche application (CCAG, normes)
  targeted_chunks?: number  // Sprint 2 (T5) : extraits ramenés par la recherche ciblée sur ces fichiers
}
```

- [ ] **Step 4: `named-documents.ts`**

(a) Dans `DOC_TYPE_PATTERNS`, après la ligne `ccap`, ajouter `{ type: 'ccag',            regex: /\bCCAG\b/gi },` et réduire le commentaire de fin de tableau à la seule mention de `plan`/`notice` (R7) : « ccag résolu depuis le Sprint 2 par le repli couche application ».

(b) `matchNamedDocument` : les listes `found`/`similar` sont construites sur les candidats, pas sur les noms :

```ts
export function matchNamedDocument(
  named: NamedDocument,
  candidates: NamedCandidate[],
  ignoreTokens: string[] = [],
): NamedDocumentResolution {
  const ignore = new Set(ignoreTokens)
  const qualifiers = named.qualifiers.filter(q => !ignore.has(normalizeName(q)))
  const layer = candidates[0]?.layer ?? null
  const base = { phrase: named.phrase, type: named.type, qualifiers, truncated: false, layer }
  if (candidates.length === 0) {
    return { ...base, found: [], found_file_ids: [], similar: [], status: 'no_candidate', total: 0 }
  }
  if (qualifiers.length === 0) {
    const listed = candidates.slice(0, MAX_LISTED)
    return { ...base, found: listed.map(c => c.name), found_file_ids: listed.map(c => c.fileId), similar: [], status: 'found', total: candidates.length }
  }
  const matched = candidates.filter(c => qualifiers.every(q => qualifierMatches(q, c.searchText)))
  if (matched.length > 0) {
    const listed = matched.slice(0, MAX_LISTED)
    return { ...base, found: listed.map(c => c.name), found_file_ids: listed.map(c => c.fileId), similar: [], status: 'found', total: matched.length }
  }
  return { ...base, found: [], found_file_ids: [], similar: candidates.slice(0, MAX_LISTED).map(c => c.name), status: 'not_found', total: candidates.length }
}
```

(c) `formatNamedDocumentsBlock` : la branche `found` devient

```ts
    if (r.status === 'found') {
      const suffix = r.layer === 'app' ? ' (document de reference de la couche application, commun a tous les projets)' : ''
      lines.push(`- « ${r.phrase} » → ${withRemainder(r.found, r.total)}${suffix}${partial}`)
    }
```

et la branche `not_found` écrit `fichiers ${r.type} ${r.layer === 'app' ? 'de la couche application' : 'du projet'} : …`. `unknownResolution` renvoie aussi `found_file_ids: []` et `layer: null`.

(d) Résolution réseau : `fetchCandidates` sélectionne `id, original_filename, display_name`, prend la couche en paramètre et construit les candidats avec `fileId: f.id` et `layer` ; puis le repli :

```ts
type CandidateList = { candidates: NamedCandidate[]; truncated: boolean }

async function queryCandidates(
  supabase: Supabase,
  type: NamedDocumentType,
  layer: NamedDocumentLayer,
  scope: { projectId: string } | { appId: string },
): Promise<CandidateList | null> {
  let q = supabase
    .schema('sources')
    .from('files')
    .select('id, original_filename, display_name')
    .eq('processing_status', 'completed')
    .or(buildFilenameFilter(type))
  q = 'projectId' in scope
    ? q.eq('project_id', scope.projectId)
    : q.eq('layer', 'app').eq('app_id', scope.appId).is('project_id', null)
  const { data, error } = await q.order('original_filename').limit(MAX_CANDIDATES)
  if (error) {
    console.warn(`[named-documents] ${type} (${layer}):`, error.message)
    return null
  }
  const rows = data || []
  const candidates: NamedCandidate[] = []
  const seen = new Set<string>()
  for (const f of rows) {
    const original = typeof f.original_filename === 'string' ? f.original_filename : ''
    const display = typeof f.display_name === 'string' ? f.display_name : ''
    const name = (display || original).trim()
    if (name === '' || seen.has(name) || typeof f.id !== 'string') continue
    seen.add(name)
    candidates.push({ fileId: f.id, name, searchText: normalizeName(`${original} ${display}`), layer })
  }
  return { candidates, truncated: rows.length >= MAX_CANDIDATES }
}

/**
 * Fichiers du projet d'un type donné ; si le projet n'en a aucun, repli sur la couche
 * application (CCAG, normes : layer='app', project_id NULL, app_id). null = erreur → unknown.
 */
async function fetchCandidates(
  supabase: Supabase,
  projectId: string,
  appId: string,
  type: NamedDocumentType,
): Promise<CandidateList | null> {
  const project = await queryCandidates(supabase, type, 'project', { projectId })
  if (project === null) return null
  if (project.candidates.length > 0) return project
  return await queryCandidates(supabase, type, 'app', { appId })
}
```

`fetchNamedDocumentCandidates(supabase, projectId, appId, named)` passe `appId` à `fetchCandidates`. Dans `index.ts`, les deux appels deviennent `fetchNamedDocumentCandidates(supabase, project_id, app_id, …)`.

(e) `generation/prompt.ts`, `formatProjectIdentity`, branche `prive` :

```ts
    rules.push(
      `REGLE MARCHE PRIVE : Ce projet est un marche PRIVE. Le CCAG des marches publics (document de reference de la couche application) ne s'applique PAS a ce projet.`,
      `Si l'utilisateur t'interroge sur le CCAG et que le projet ne contient AUCUN document CCAG propre, precise que le CCAG de reference concerne les marches publics et reponds a partir des documents contractuels du projet (CCAP, CCTP, Charte).`,
      `Si le projet contient son propre document CCAG (present dans DOCUMENTS NOMMES ou dans les chunks fournis), c'est lui qui fait foi : reponds a partir de lui.`,
    )
```

- [ ] **Step 5: Tests, type-check**

Run: `deno test supabase/functions/baikal-retrieval/ && deno check supabase/functions/baikal-retrieval/index.ts`
Expected: tous les tests OK (les 39 existants de named-documents avec fixtures mises à jour + 4 nouveaux + 1 prompt) ; 0 erreur.

- [ ] **Step 6: Preuve réseau (contrôleur, lecture seule)**

Le contrôleur exécute via MCP, avant la revue : `select id, original_filename from sources.files where layer='app' and app_id='arpet' and project_id is null and processing_status='completed' and (original_filename ~* 'ccag' or display_name ~* 'ccag');` → attendu : `5ba512af-d68a-41f3-9578-aeb17a231a55 | CCAG.pdf`. Consigné dans le registre SDD.

- [ ] **Step 7: Commit**

```bash
git add supabase/functions/baikal-retrieval/types.ts supabase/functions/baikal-retrieval/routing/named-documents.ts supabase/functions/baikal-retrieval/routing/named-documents.test.ts supabase/functions/baikal-retrieval/generation/prompt.ts supabase/functions/baikal-retrieval/generation/prompt.test.ts supabase/functions/baikal-retrieval/index.ts
git commit -m "feat(rag): documents nommés — identifiants de fichiers, CCAG résolu par la couche application, règle marché privé affinée"
```

---

### Task 5: Recherche ciblée par document nommé (S2.3 généralisé)

**Files:**
- Create: `supabase/functions/baikal-retrieval/search/targeted.ts`
- Test: `supabase/functions/baikal-retrieval/search/targeted.test.ts`
- Modify: `supabase/functions/baikal-retrieval/search/retrieval.ts:117,147` (`export function mapChunks`, `export function buildFileInfos`)
- Modify: `supabase/functions/baikal-retrieval/types.ts` (`PipelineMetrics.counts.targeted_chunks`)
- Modify: `supabase/functions/baikal-retrieval/index.ts:290-312` (bloc A6), `:395-409` et `:591-606` (payloads `sources` : `named_documents`)

**Interfaces:**
- Consumes: `NamedDocumentResolution.found_file_ids`, `.layer` (T4) ; `MATCH_DOCUMENTS_FN`, `toFtsQuery`, `mapChunks`, `buildFileInfos` (retrieval.ts).
- Produces:
  ```ts
  export interface NamedTarget { phrase: string; names: string[]; fileIds: string[]; layer: NamedDocumentLayer }
  export interface TargetedResult { target: NamedTarget; chunks: ChunkResult[] }
  export const TARGETED_CHUNKS_PER_DOCUMENT = 6
  export const TARGETED_SIMILARITY_THRESHOLD = 0.25
  export function buildNamedTargets(resolutions: NamedDocumentResolution[]): NamedTarget[]
  export function targetLabel(t: NamedTarget): string
  export async function executeTargetedSearches(supabase, queryEmbedding, queryText, userId, effectiveOrgId, projectId, effectiveAppId, config, filterSourceTypes, intentStrategy, targets): Promise<TargetedResult[]>
  export function mergeTargeted(global: SearchResult, targeted: TargetedResult[], config: LibrarianConfig, searchConfig: SearchConfig): SearchResult
  export function annotateTargeted(resolutions: NamedDocumentResolution[], targeted: TargetedResult[]): void
  export function slimNamedDocuments(resolutions: NamedDocumentResolution[]): Array<{ phrase: string; status: string; found: string[]; file_ids: string[]; layer: string | null; targeted_chunks: number }>
  ```
- Règle de fusion : les extraits des documents nommés d'abord (dans l'ordre des mentions), puis les extraits globaux non déjà présents ; dédoublonnage par `chunk_id` ; `files` recalculés sur l'ensemble ; `filterApplied: true`. Aucune requête ciblée ne fait échouer la question (erreur → liste vide + warn).

- [ ] **Step 1: Écrire les tests qui échouent**

Créer `search/targeted.test.ts` :

```ts
import { assertEquals } from "https://deno.land/std@0.224.0/assert/mod.ts"
import { buildNamedTargets, mergeTargeted, targetLabel, annotateTargeted } from "./targeted.ts"
import type { ChunkResult, SearchResult, LibrarianConfig, SearchConfig, NamedDocumentResolution } from "../types.ts"

function chunk(id: number, file: string, sim = 0.5): ChunkResult {
  return {
    chunk_id: id, content: `c${id}`, similarity: sim, metadata: { page_start: 1 }, layer: 'project',
    source_file_id: file, matched_concepts: [], rank_score: sim, match_source: 'vector', filter_applied: false,
    file_storage_path: `${file}.pdf`, file_storage_bucket: 'documents', file_original_filename: `${file}.pdf`,
    file_mime_type: 'application/pdf', file_total_pages: 10, file_max_similarity: null, file_chunk_count: null,
    hierarchy_level: 1, parent_chunk_id: null, retrieval_role: 'primary', section_title: null,
  }
}
function resolution(over: Partial<NamedDocumentResolution>): NamedDocumentResolution {
  return { phrase: 'CCAP', type: 'ccap', qualifiers: [], found: ['CCAP.pdf'], found_file_ids: ['fa'], similar: [],
    status: 'found', total: 1, truncated: false, layer: 'project', ...over }
}
const CONFIG = { boost_on_mention: 1, boost_factor: 1, gemini_max_files: 5 } as unknown as LibrarianConfig
const SEARCH: SearchConfig = { scope: 'broad', max_files: 5, min_similarity: 0.35, boost_documents: [], file_filter: null }
const GLOBAL: SearchResult = { chunks: [chunk(1, 'fz', 0.9), chunk(2, 'fa', 0.8)], files: [], meetingChunks: [], totalPages: 0, filterApplied: false, fallbackUsed: false, reranked: false }

Deno.test("buildNamedTargets : seuls les found avec ids, dédoublonnés par ensemble de fichiers", () => {
  const t = buildNamedTargets([
    resolution({}),
    resolution({ phrase: 'ccap', found: ['CCAP.pdf'], found_file_ids: ['fa'] }),
    resolution({ phrase: 'CCTP du lot 07', type: 'cctp', found: ['Lot07.pdf'], found_file_ids: ['fb'] }),
    resolution({ phrase: 'DOE', type: 'doe', status: 'no_candidate', found: [], found_file_ids: [] }),
  ])
  assertEquals(t.map(x => x.fileIds), [['fa'], ['fb']])
  assertEquals(targetLabel(t[0]), 'CCAP.pdf')
  assertEquals(targetLabel({ phrase: 'CCTP', names: ['a', 'b'], fileIds: ['1', '2'], layer: 'project' }), 'CCTP')
})

Deno.test("mergeTargeted : extraits ciblés d'abord, globaux ensuite, sans doublon, fichiers recalculés", () => {
  const targeted = [{ target: { phrase: 'CCAP', names: ['CCAP.pdf'], fileIds: ['fa'], layer: 'project' as const }, chunks: [chunk(2, 'fa', 0.4), chunk(3, 'fa', 0.3)] }]
  const m = mergeTargeted(GLOBAL, targeted, CONFIG, SEARCH)
  assertEquals(m.chunks.map(c => c.chunk_id), [2, 3, 1])
  assertEquals(m.filterApplied, true)
  assertEquals(m.files.map(f => f.file_id).sort(), ['fa', 'fz'])
  assertEquals(m.totalPages, 20)
})

Deno.test("mergeTargeted sans résultat ciblé rend les extraits globaux inchangés", () => {
  const m = mergeTargeted(GLOBAL, [{ target: { phrase: 'CCAP', names: ['CCAP.pdf'], fileIds: ['fa'], layer: 'project' }, chunks: [] }], CONFIG, SEARCH)
  assertEquals(m.chunks.map(c => c.chunk_id), [1, 2])
})

Deno.test("annotateTargeted pose targeted_chunks sur la résolution correspondante", () => {
  const r = [resolution({})]
  annotateTargeted(r, [{ target: { phrase: 'CCAP', names: ['CCAP.pdf'], fileIds: ['fa'], layer: 'project' }, chunks: [chunk(2, 'fa')] }])
  assertEquals(r[0].targeted_chunks, 1)
})
```

- [ ] **Step 2: Vérifier qu'ils échouent**

Run: `deno test supabase/functions/baikal-retrieval/search/targeted.test.ts`
Expected: FAIL — module absent.

- [ ] **Step 3: Exporter `mapChunks` et `buildFileInfos` dans `retrieval.ts`**

Préfixer les deux déclarations par `export`. Aucun autre changement.

- [ ] **Step 4: Écrire `search/targeted.ts`**

```ts
// ============================================================================
// baikal-retrieval - Search: Recherche ciblée par document nommé (Sprint 2, T5)
// ============================================================================
// Quand la question nomme un document résolu sur les fichiers ingérés (T4),
// une recherche restreinte à ce fichier (filter_file_ids) tourne en parallèle
// de la recherche globale ; la fusion garantit des extraits de CHAQUE document
// nommé. Deux documents nommés = comparaison déterministe (S2.3) sans boucle
// agentique ; un seul = « dans le CCAP ? », « d'après le CR 41 ». Aucun LLM.
// ============================================================================

import type {
  Supabase, LibrarianConfig, IntentStrategy, SearchConfig, ChunkResult, SearchResult,
  NamedDocumentResolution, NamedDocumentLayer,
} from "../types.ts"
import { MATCH_DOCUMENTS_FN } from "../config.ts"
import { toFtsQuery, mapChunks, buildFileInfos } from "./retrieval.ts"

export interface NamedTarget {
  phrase: string
  names: string[]
  fileIds: string[]
  layer: NamedDocumentLayer
}

export interface TargetedResult {
  target: NamedTarget
  chunks: ChunkResult[]
}

// Constantes (pas de config DB : S2.5 est le seul réglage prévu ce sprint)
export const TARGETED_CHUNKS_PER_DOCUMENT = 6
export const TARGETED_SIMILARITY_THRESHOLD = 0.25   // seuil bas : le document est déjà désigné, comme search_in_file

export function buildNamedTargets(resolutions: NamedDocumentResolution[]): NamedTarget[] {
  const targets: NamedTarget[] = []
  const seen = new Set<string>()
  for (const r of resolutions) {
    if (r.status !== 'found' || r.found_file_ids.length === 0) continue
    const key = [...r.found_file_ids].sort().join(',')
    if (seen.has(key)) continue
    seen.add(key)
    targets.push({ phrase: r.phrase, names: r.found, fileIds: r.found_file_ids, layer: r.layer ?? 'project' })
  }
  return targets
}

/** Libellé des étapes SSE : le nom du fichier s'il est unique, sinon la mention de l'utilisateur. */
export function targetLabel(t: NamedTarget): string {
  return t.names.length === 1 ? t.names[0] : t.phrase
}

export async function executeTargetedSearches(
  supabase: Supabase,
  queryEmbedding: number[],
  queryText: string,
  userId: string,
  effectiveOrgId: string | null,
  projectId: string | undefined,
  effectiveAppId: string,
  config: LibrarianConfig,
  filterSourceTypes: string[] | undefined,
  intentStrategy: IntentStrategy,
  targets: NamedTarget[],
): Promise<TargetedResult[]> {
  if (targets.length === 0) return []
  const ftsQuery = toFtsQuery(queryText)
  return await Promise.all(targets.map(async (target): Promise<TargetedResult> => {
    try {
      const { data, error } = await supabase.schema('rag').rpc(MATCH_DOCUMENTS_FN, {
        query_embedding: queryEmbedding,
        query_text: ftsQuery,
        p_user_id: userId,
        p_org_id: effectiveOrgId,
        p_project_id: projectId || null,
        p_app_id: effectiveAppId,
        match_count: TARGETED_CHUNKS_PER_DOCUMENT,
        similarity_threshold: TARGETED_SIMILARITY_THRESHOLD,
        include_app_layer: true,
        include_org_layer: true,
        include_project_layer: true,
        include_user_layer: false,
        filter_source_types: filterSourceTypes || null,
        filter_file_ids: target.fileIds,
        filter_filenames: null,
        enable_concept_expansion: config.enable_concept_expansion,
        p_hierarchy_levels: intentStrategy.hierarchy_levels,
        p_include_children: intentStrategy.include_children,
        p_app_layer_weight: 1.0,          // le document est désigné : aucune pénalité de couche
        p_children_per_parent: 3,
      })
      if (error) {
        console.warn(`[targeted] ${targetLabel(target)}: ${error.message}`)
        return { target, chunks: [] }
      }
      const chunks = mapChunks(data || [])
      console.log(`[targeted] ${targetLabel(target)}: ${chunks.length} extraits`)
      return { target, chunks }
    } catch (err) {
      console.warn(`[targeted] ${targetLabel(target)} error:`, err)
      return { target, chunks: [] }
    }
  }))
}

/** Extraits ciblés d'abord (ordre des mentions), puis extraits globaux non déjà présents. */
export function mergeTargeted(
  global: SearchResult,
  targeted: TargetedResult[],
  config: LibrarianConfig,
  searchConfig: SearchConfig,
): SearchResult {
  const seen = new Set<number>()
  const merged: ChunkResult[] = []
  const push = (c: ChunkResult) => { if (!seen.has(c.chunk_id)) { seen.add(c.chunk_id); merged.push(c) } }
  for (const t of targeted) for (const c of t.chunks) push(c)
  for (const c of global.chunks) push(c)

  const meetingChunks = merged.filter(c => c.metadata?.source_type === 'meeting_transcript')
  const fileChunks = merged.filter(c => c.metadata?.source_type !== 'meeting_transcript')
  const files = buildFileInfos(fileChunks, searchConfig.boost_documents, config)
  const maxFiles = searchConfig.max_files || config.gemini_max_files
  const filteredFiles = files.slice(0, maxFiles)
  return {
    ...global,
    chunks: merged,
    files: filteredFiles,
    meetingChunks,
    totalPages: filteredFiles.reduce((sum, f) => sum + f.total_pages, 0),
    filterApplied: true,
  }
}

/** Trace par mention : combien d'extraits la recherche ciblée a ramenés (rag.query_logs.named_documents). */
export function annotateTargeted(resolutions: NamedDocumentResolution[], targeted: TargetedResult[]): void {
  for (const r of resolutions) {
    if (r.status !== 'found') continue
    const key = [...r.found_file_ids].sort().join(',')
    const hit = targeted.find(t => [...t.target.fileIds].sort().join(',') === key)
    if (hit) r.targeted_chunks = hit.chunks.length
  }
}

/** Forme envoyée au frontend dans l'événement `sources` (bouton « Approfondir », T8). */
export function slimNamedDocuments(resolutions: NamedDocumentResolution[]) {
  return resolutions
    .filter(r => r.status !== 'unknown')
    .map(r => ({
      phrase: r.phrase, status: r.status, found: r.found, file_ids: r.found_file_ids,
      layer: r.layer, targeted_chunks: r.targeted_chunks ?? 0,
    }))
}
```

- [ ] **Step 5: Vérifier que les tests passent**

Run: `deno test supabase/functions/baikal-retrieval/search/targeted.test.ts`
Expected: 4 tests OK.

- [ ] **Step 6: Câbler dans `index.ts`**

Imports : `import { buildNamedTargets, targetLabel, executeTargetedSearches, mergeTargeted, annotateTargeted, slimNamedDocuments } from "./search/targeted.ts"`. Dans `types.ts`, ajouter `targeted_chunks: number` à `PipelineMetrics.counts` et l'initialiser à `0` dans `metrics.counts` (index.ts). Remplacer le bloc A6 (de `sendSSE(controller, 'step', { step: 'search', …})` jusqu'à `metrics.timings.search = timer.mark('search')`) par :

```ts
          // A6. SEARCH — recherche globale + recherche ciblée par document nommé (Sprint 2, T5), en parallèle
          sendSSE(controller, 'step', { step: 'search', message: 'Recherche documentaire...' })
          const targets = buildNamedTargets(context.namedDocuments)
          for (const t of targets) {
            sendSSE(controller, 'step', { step: 'search_named', message: `🔍 Recherche dans ${targetLabel(t)}...` })
          }

          const globalSearch = fastAnalysis.cross_ref?.is_cross_ref
            ? executeCrossRefSearch(
                supabase, queryEmbedding, effectiveQuery, user_id, context.effectiveOrgId,
                project_id, context.effectiveAppId, config.librarian,
                filter_source_types,
                fastAnalysis.search_config, fastAnalysis.intent, intentStrategy,
                fastAnalysis.cross_ref,
              )
            : executeSearch(
                supabase, queryEmbedding, effectiveQuery, user_id, context.effectiveOrgId,
                project_id, context.effectiveAppId, config.librarian,
                layerFlags, filter_source_types,
                fastAnalysis.search_config, fastAnalysis.intent, intentStrategy, config.features,
                fastAnalysis.cross_ref?.detected_norms ?? [],
              )
          if (fastAnalysis.cross_ref?.is_cross_ref) console.log(`[retrieval] Cross-ref search activated`)

          const [globalResult, targetedResults] = await Promise.all([
            globalSearch,
            executeTargetedSearches(
              supabase, queryEmbedding, effectiveQuery, user_id, context.effectiveOrgId,
              project_id, context.effectiveAppId, config.librarian, filter_source_types, intentStrategy, targets,
            ),
          ])
          let searchResult: Awaited<ReturnType<typeof executeSearch>> = targets.length > 0
            ? mergeTargeted(globalResult, targetedResults, config.librarian, fastAnalysis.search_config)
            : globalResult
          annotateTargeted(context.namedDocuments, targetedResults)
          metrics.counts.targeted_chunks = targetedResults.reduce((s, t) => s + t.chunks.length, 0)
          if (targets.length > 0) {
            console.log(`[retrieval] Recherche ciblée: ${targets.length} document(s) nommé(s), ${metrics.counts.targeted_chunks} extraits ajoutés`)
          }
          metrics.timings.search = timer.mark('search')
```

(supprimer la déclaration `let searchResult: …` qui précédait le bloc). Dans les deux événements `sources` (agentique et chemin rapide), ajouter `named_documents: slimNamedDocuments(context.namedDocuments),`. Le `logQuery` continue de recevoir `named_documents: context.namedDocuments` (les résolutions portent désormais `found_file_ids`, `layer`, `targeted_chunks` — colonne jsonb, aucune migration).

- [ ] **Step 7: Tests, type-check, commit**

Run: `deno test supabase/functions/baikal-retrieval/ && deno check supabase/functions/baikal-retrieval/index.ts`
Expected: tous OK ; 0 erreur.

```bash
git add supabase/functions/baikal-retrieval/search/targeted.ts supabase/functions/baikal-retrieval/search/targeted.test.ts supabase/functions/baikal-retrieval/search/retrieval.ts supabase/functions/baikal-retrieval/types.ts supabase/functions/baikal-retrieval/index.ts
git commit -m "feat(rag): recherche ciblée par document nommé — extraits garantis de chaque document, comparaison sans boucle agentique (S2.3)"
```

---

### Task 6: Boucle agentique — budget dédié (S2.1) et génération unique (S2.2)

**Files:**
- Modify: `supabase/functions/baikal-retrieval/agentic/orchestrator.ts:39-47,53-61,99-179`
- Modify: `supabase/functions/baikal-retrieval/agentic/gemini-agent.ts:78-110,274-284`
- Modify: `supabase/functions/baikal-retrieval/index.ts:357-360,404,421-425`
- Test: `agentic/orchestrator.test.ts` (nouveau), `agentic/gemini-agent.test.ts` (nouveau)

**Interfaces:**
- Produces: `remainingBudgetMs(startTime: number, now: number, timeoutMs: number): number` (orchestrator.ts, pur) ; `MIN_ITERATION_BUDGET_MS = 1500` ; `splitForStreaming(text: string, size?: number): string[]` (gemini-agent.ts, pur, coupe sur les espaces, `size` par défaut 24) ; `callGeminiAgent(query, history, cfg, key, opts?: { timeoutMs?: number })` ; `AgenticResult.directAnswer: boolean`.
- Règles : `startTime` passé à `runAgenticLoop` est l'instant du déclenchement de la Phase B (`Date.now()` dans index.ts), plus `timer.startTime` ; chaque itération vérifie le budget restant et borne le timeout HTTP de l'appel Gemini à ce reste ; si Gemini répond en texte dans la boucle, ce texte est streamé tel quel (aucun second appel) ; timeout ou itérations épuisées → génération finale sans outils, comme avant.

- [ ] **Step 1: Écrire les tests qui échouent**

`agentic/orchestrator.test.ts` :

```ts
import { assertEquals } from "https://deno.land/std@0.224.0/assert/mod.ts"
import { remainingBudgetMs, MIN_ITERATION_BUDGET_MS } from "./orchestrator.ts"

Deno.test("le budget agentique se compte depuis le déclenchement de la Phase B, pas depuis la requête", () => {
  assertEquals(remainingBudgetMs(10_000, 12_500, 8000), 5500)
  assertEquals(remainingBudgetMs(10_000, 19_000, 8000), -1000)
  assertEquals(MIN_ITERATION_BUDGET_MS, 1500)
})
```

`agentic/gemini-agent.test.ts` :

```ts
import { assertEquals } from "https://deno.land/std@0.224.0/assert/mod.ts"
import { splitForStreaming } from "./gemini-agent.ts"

Deno.test("splitForStreaming coupe sur les espaces et reconstitue le texte exact", () => {
  const text = "Le CCAP prévoit des pénalités de 100 € par jour calendaire de retard [CCAP, Page 19]."
  const parts = splitForStreaming(text, 24)
  assertEquals(parts.join(''), text)
  assertEquals(parts.every(p => p.length <= 24 + 20), true)
  assertEquals(parts.length > 2, true)
})

Deno.test("splitForStreaming : texte vide → aucun morceau ; mot long → un seul morceau", () => {
  assertEquals(splitForStreaming(""), [])
  assertEquals(splitForStreaming("abcdefghijklmnopqrstuvwxyz0123456789", 8), ["abcdefghijklmnopqrstuvwxyz0123456789"])
})
```

- [ ] **Step 2: Vérifier qu'ils échouent**

Run: `deno test supabase/functions/baikal-retrieval/agentic/`
Expected: FAIL — exports absents.

- [ ] **Step 3: `gemini-agent.ts`**

Signature : `export async function callGeminiAgent(query, conversationHistory, agenticConfig, geminiApiKey, opts: { timeoutMs?: number } = {})` et `signal: AbortSignal.timeout(Math.max(1000, opts.timeoutMs ?? 20_000))`. Ajouter dans HELPERS :

```ts
/** Découpe un texte déjà complet en morceaux streamables (coupure sur un espace, jamais au milieu d'un mot). */
export function splitForStreaming(text: string, size = 24): string[] {
  const parts: string[] = []
  let rest = text
  while (rest.length > 0) {
    if (rest.length <= size) { parts.push(rest); break }
    let cut = rest.indexOf(' ', size)
    if (cut === -1) { parts.push(rest); break }
    cut += 1                                     // l'espace reste avec le morceau courant
    parts.push(rest.slice(0, cut))
    rest = rest.slice(cut)
  }
  return parts
}
```

- [ ] **Step 4: `orchestrator.ts`**

Ajouter `directAnswer: boolean` à `AgenticResult`, puis :

```ts
export const MIN_ITERATION_BUDGET_MS = 1500

/** Budget restant de la Phase B : startTime = déclenchement de la boucle (S2.1), pas début de la requête. */
export function remainingBudgetMs(startTime: number, now: number, timeoutMs: number): number {
  return timeoutMs - (now - startTime)
}
```

Boucle (remplace `while … { const elapsed … }` jusqu'à l'appel Gemini) :

```ts
  let finalText: string | null = null

  while (iterations < agenticConfig.max_iterations) {
    const remaining = remainingBudgetMs(startTime, Date.now(), agenticConfig.timeout_ms)
    if (remaining < MIN_ITERATION_BUDGET_MS) {
      console.log(`[agentic] Budget épuisé (${remaining}ms restants sur ${agenticConfig.timeout_ms}ms) à l'itération ${iterations}`)
      timedOut = true
      break
    }

    iterations++
    console.log(`[agentic] Iteration ${iterations}/${agenticConfig.max_iterations} (budget restant ${remaining}ms)`)
    sendSSE('step', { step: 'agent_thinking', message: `Réflexion (étape ${iterations})...` })

    const turn = await callGeminiAgent(query, history, agenticConfig, geminiApiKey, { timeoutMs: Math.min(20_000, remaining) })

    if (turn.type === 'text') {
      // S2.2 : la réponse texte de la boucle EST la réponse finale — plus de second appel
      finalText = turn.content?.trim() || null
      console.log(`[agentic] Gemini répond en texte à l'itération ${iterations} (${finalText?.length ?? 0} caractères)`)
      break
    }
    // … suite inchangée (tool call, exécution, steps, historique)
  }
```

Génération finale :

```ts
  sendSSE('step', { step: 'generating', message: 'Rédaction de la réponse...' })

  let fullResponse = ''
  const directAnswer = finalText !== null && finalText.length > 0
  if (directAnswer) {
    for (const piece of splitForStreaming(finalText!)) {
      fullResponse += piece
      sendSSE('token', { content: piece })
    }
  } else {
    // Timeout ou itérations épuisées sans réponse texte : génération finale sans outils
    const generator = streamGeminiAgentResponse(history, agenticConfig, geminiApiKey)
    for await (const token of generator) {
      fullResponse += token
      sendSSE('token', { content: token })
    }
  }
```

et `return { …, directAnswer }`. Importer `splitForStreaming` depuis `./gemini-agent.ts`.

- [ ] **Step 5: `index.ts`**

L'appel devient `runAgenticLoop(effectiveQuery, context, config.agentic, toolCtx, GEMINI_API_KEY, sseSender, Date.now())`. Dans le `logQuery` agentique, ajouter `direct_answer: agenticResult.directAnswer` dans l'objet `agentic` (et le champ optionnel `direct_answer?: boolean` dans `QueryLogEntry.agentic`, `logging.ts`). Dans l'événement `sources` agentique : `agentic: { iterations, timed_out, direct_answer: agenticResult.directAnswer, steps }`.

- [ ] **Step 6: Tests, type-check, commit**

Run: `deno test supabase/functions/baikal-retrieval/ && deno check supabase/functions/baikal-retrieval/index.ts`
Expected: tous OK ; 0 erreur.

```bash
git add supabase/functions/baikal-retrieval/agentic/orchestrator.ts supabase/functions/baikal-retrieval/agentic/orchestrator.test.ts supabase/functions/baikal-retrieval/agentic/gemini-agent.ts supabase/functions/baikal-retrieval/agentic/gemini-agent.test.ts supabase/functions/baikal-retrieval/index.ts supabase/functions/baikal-retrieval/logging.ts
git commit -m "feat(rag): boucle agentique — budget compté depuis la Phase B, réponse texte streamée sans second appel (S2.1, S2.2)"
```

---

### Task 7: Lecture intégrale sur les documents nommés (backend du bouton « Approfondir »)

**Files:**
- Create: `supabase/functions/baikal-retrieval/search/named-files.ts`
- Test: `supabase/functions/baikal-retrieval/search/named-files.test.ts`
- Modify: `supabase/functions/baikal-retrieval/agentic/gate.ts:12` (`GateReason` += `'explicit_full_document'`)
- Modify: `supabase/functions/baikal-retrieval/index.ts` (après le bloc A7 rerank, avant la gate)

**Interfaces:**
- Produces: `fetchFileInfosByIds(supabase, fileIds: string[]): Promise<FileInfo[]>` (ordre des ids conservé) ; `selectNamedFiles(files: FileInfo[], maxFiles: number, maxPages: number): FileInfo[]` (pur : garde l'ordre, s'arrête au premier fichier qui ferait dépasser `maxFiles` ou le cumul `maxPages`).
- Règle : `generation_mode === 'gemini'` **explicite** dans la requête ET au moins un document nommé résolu → les fichiers lus en mode intégral sont ceux des documents nommés (pas les mieux classés par la recherche) ; la gate agentique est court-circuitée (`reason: 'explicit_full_document'`). Sans document nommé, comportement inchangé.

- [ ] **Step 1: Écrire le test qui échoue**

`search/named-files.test.ts` :

```ts
import { assertEquals } from "https://deno.land/std@0.224.0/assert/mod.ts"
import { selectNamedFiles } from "./named-files.ts"
import type { FileInfo } from "../types.ts"

function file(id: string, pages: number): FileInfo {
  return { file_id: id, storage_path: `${id}.pdf`, storage_bucket: 'documents', original_filename: `${id}.pdf`,
    mime_type: 'application/pdf', total_pages: pages, max_similarity: 1, avg_similarity: 1, chunk_count: 0,
    layer: 'project', score: 1, is_boosted: true }
}

Deno.test("selectNamedFiles respecte l'ordre, le nombre et le cumul de pages", () => {
  const files = [file('a', 100), file('b', 300), file('c', 200)]
  assertEquals(selectNamedFiles(files, 5, 450).map(f => f.file_id), ['a', 'b'])
  assertEquals(selectNamedFiles(files, 1, 450).map(f => f.file_id), ['a'])
  assertEquals(selectNamedFiles(files, 5, 50).map(f => f.file_id), [])
})
```

- [ ] **Step 2: Vérifier qu'il échoue**

Run: `deno test supabase/functions/baikal-retrieval/search/named-files.test.ts` → FAIL (module absent).

- [ ] **Step 3: Écrire `search/named-files.ts`**

```ts
// ============================================================================
// baikal-retrieval - Search: fichiers des documents nommés (Sprint 2, T7)
// ============================================================================
// Bouton « Approfondir » d'ARPET : la question est renvoyée avec
// generation_mode='gemini'. Les fichiers lus en entier sont alors ceux que la
// question nomme (résolus par T4), pas les mieux classés par la recherche.
// ============================================================================

import type { Supabase, FileInfo } from "../types.ts"

export async function fetchFileInfosByIds(supabase: Supabase, fileIds: string[]): Promise<FileInfo[]> {
  if (fileIds.length === 0) return []
  const { data, error } = await supabase
    .schema('sources')
    .from('files')
    .select('id, storage_path, storage_bucket, original_filename, display_name, mime_type, total_pages, layer')
    .in('id', fileIds)
    .eq('processing_status', 'completed')
  if (error) {
    console.warn('[named-files] sources.files:', error.message)
    return []
  }
  const byId = new Map<string, Record<string, unknown>>()
  for (const row of data || []) byId.set(row.id as string, row as Record<string, unknown>)
  const files: FileInfo[] = []
  for (const id of fileIds) {                    // ordre des mentions conservé
    const f = byId.get(id)
    if (!f || typeof f.storage_path !== 'string') continue
    files.push({
      file_id: id,
      storage_path: f.storage_path,
      storage_bucket: (f.storage_bucket as string) || 'documents',
      original_filename: (f.original_filename as string) || (f.display_name as string) || 'Document',
      mime_type: (f.mime_type as string) || 'application/pdf',
      total_pages: (f.total_pages as number) || 1,
      max_similarity: 1, avg_similarity: 1, chunk_count: 0,
      layer: (f.layer as string) || 'project',
      score: 1, is_boosted: true,
    })
  }
  return files
}

/** Garde l'ordre ; s'arrête au premier fichier qui dépasserait maxFiles ou le cumul maxPages. */
export function selectNamedFiles(files: FileInfo[], maxFiles: number, maxPages: number): FileInfo[] {
  const kept: FileInfo[] = []
  let pages = 0
  for (const f of files) {
    if (kept.length >= maxFiles) break
    if (pages + f.total_pages > maxPages) break
    kept.push(f)
    pages += f.total_pages
  }
  return kept
}
```

- [ ] **Step 4: Câbler dans `index.ts`**

Import `import { fetchFileInfosByIds, selectNamedFiles } from "./search/named-files.ts"` et `import type { FileInfo } from "./types.ts"` (ajouter au `import type` existant). Après `metrics.timings.rerank = timer.mark('rerank')` :

```ts
          // Sprint 2 (T7) — « Approfondir » : lecture intégrale demandée explicitement →
          // les fichiers lus sont ceux que la question nomme, pas les mieux classés.
          let namedFiles: FileInfo[] = []
          if (generation_mode === 'gemini' && GEMINI_API_KEY) {
            const namedIds = [...new Set(context.namedDocuments.filter(r => r.status === 'found').flatMap(r => r.found_file_ids))]
            if (namedIds.length > 0) {
              namedFiles = selectNamedFiles(
                await fetchFileInfosByIds(supabase, namedIds),
                config.librarian.gemini_max_files, config.librarian.gemini_max_pages,
              )
              if (namedFiles.length > 0) {
                searchResult = {
                  ...searchResult, files: namedFiles, filterApplied: true,
                  totalPages: namedFiles.reduce((s, f) => s + f.total_pages, 0),
                }
                console.log(`[retrieval] Lecture intégrale sur documents nommés: ${namedFiles.map(f => f.original_filename).join(', ')}`)
              }
            }
          }
```

Puis la décision : `const useAgentic = gate.trigger && namedFiles.length === 0` et `const gateReason: GateReason = namedFiles.length > 0 && gate.trigger ? 'explicit_full_document' : (gate.trigger && !GEMINI_API_KEY ? 'no_gemini_key' : gate.reason)`. Dans `gate.ts` : `export type GateReason = 'disabled' | 'too_few_vector_chunks' | 'low_max_similarity' | 'fast_path_ok' | 'no_gemini_key' | 'explicit_full_document'`.

- [ ] **Step 5: Tests, type-check, commit**

Run: `deno test supabase/functions/baikal-retrieval/ && deno check supabase/functions/baikal-retrieval/index.ts` → tous OK ; 0 erreur.

```bash
git add supabase/functions/baikal-retrieval/search/named-files.ts supabase/functions/baikal-retrieval/search/named-files.test.ts supabase/functions/baikal-retrieval/agentic/gate.ts supabase/functions/baikal-retrieval/index.ts
git commit -m "feat(rag): lecture intégrale sur les documents nommés quand le frontend la demande (Approfondir)"
```

---

### Task 8: ARPET — étapes agentiques visibles, résumé, bouton « Approfondir » (S2.4)

Dépôt : **`C:\Dev\Frontend-ARPET`**, branche `main`. Vérification : `npm run build` (= `tsc -b && vite build`) doit passer. Aucun test unitaire dans ce dépôt : le contrôleur vérifie dans le navigateur (preview) après déploiement backend.

**Files:**
- Modify: `src/services/chat/chat-types.ts:157-190` (`SSESourcesPayload` += `agentic`, `named_documents`) et `ChatResponse` (mêmes champs)
- Modify: `src/services/chat/chat-mapping.ts:135-156` (`mapSSESourcesPayload` les transmet)
- Modify: `src/types/chat.types.ts:71-93` (`Message` += `agentic?`, `named_documents?` ; nouveaux types `AgenticSummary`, `NamedDocumentRef`)
- Modify: `src/pages/Dashboard.tsx:108,150-158,215-250,316-323` (`handleSendMessage` accepte `generation_mode`, copie des métadonnées, `handleDeepen`)
- Modify: `src/components/chat/MessageBubble.tsx` (prop `onDeepen`)
- Modify: `src/components/chat/AssistantMessage/AssistantMessage.tsx` (résumé agentique + bouton)
- Create: `src/components/chat/AssistantMessage/DeepenButton.tsx` ; export dans `AssistantMessage/index.ts`
- Modify: `src/components/chat/StreamingBubble.tsx:19-73` (style des étapes agentiques et ciblées)

**Interfaces:**
- Consumes (payload `sources` de v2.2.0) : `agentic: { iterations: number; timed_out: boolean; direct_answer?: boolean; steps: Array<{ iteration: number; tool_call: { name: string; args: Record<string, unknown> }; result_summary: string; chunks_found: number; elapsed_ms: number }> } | null` ; `named_documents: Array<{ phrase: string; status: string; found: string[]; file_ids: string[]; layer: string | null; targeted_chunks: number }>` ; étapes SSE `step` : `agentic_start`, `agent_thinking`, `agent_searching`, `agent_found`, `search_named`.
- Produces : `handleSendMessage(content, files?, deepAnalysis?, options?: { generation_mode?: 'gemini' })` ; `onDeepen(message: Message)` renvoie la question du message (`message.vote_context.question`) avec `generation_mode: 'gemini'`.

- [ ] **Step 1: Types**

`src/types/chat.types.ts`, avant `export interface Message` :

```ts
/** Sprint 2 RAG — trace de la boucle agentique (payload `agentic` de l'événement sources) */
export interface AgenticSummary {
  iterations: number;
  timed_out: boolean;
  direct_answer?: boolean;
  steps: Array<{
    iteration: number;
    tool_call: { name: string; args: Record<string, unknown> };
    result_summary: string;
    chunks_found: number;
    elapsed_ms: number;
  }>;
}

/** Sprint 2 RAG — document nommé dans la question, résolu sur les fichiers du projet */
export interface NamedDocumentRef {
  phrase: string;
  status: 'found' | 'not_found' | 'no_candidate' | string;
  found: string[];
  file_ids: string[];
  layer: string | null;
  targeted_chunks: number;
}
```

et dans `Message`, après `cache_status?`, ajouter :

```ts
  // Sprint 2 RAG
  agentic?: AgenticSummary | null;
  named_documents?: NamedDocumentRef[];
```

`src/services/chat/chat-types.ts` : importer `AgenticSummary, NamedDocumentRef` depuis `'../../types'` ; ajouter à `SSESourcesPayload` et à `ChatResponse` : `agentic?: AgenticSummary | null` et `named_documents?: NamedDocumentRef[]`. `chat-mapping.ts`, `mapSSESourcesPayload` : ajouter `agentic: payload.agentic ?? null, named_documents: payload.named_documents ?? [],`.

- [ ] **Step 2: Dashboard**

Signature : `const handleSendMessage = async (content: string, _files?: File[], deepAnalysis?: boolean, options?: { generation_mode?: 'gemini' }) => {`. Dans l'appel `sendMessageStream({ … })`, ajouter `generation_mode: options?.generation_mode ?? 'auto',`. Dans `onComplete`, après `cache_status: receivedMetadata.cache_status,` ajouter `agentic: receivedMetadata.agentic ?? null, named_documents: receivedMetadata.named_documents ?? [],`. Ajouter avant le rendu :

```tsx
  // Sprint 2 RAG — « Approfondir » : même question, lecture intégrale des documents nommés
  const handleDeepen = (message: Message) => {
    const question = message.vote_context?.question
    if (!question || isAgentTyping) return
    handleSendMessage(question, undefined, undefined, { generation_mode: 'gemini' })
  }
```

et passer `onDeepen={handleDeepen}` à `<MessageBubble …>`.

- [ ] **Step 3: MessageBubble et AssistantMessage**

`MessageBubble.tsx` : prop `onDeepen?: (message: Message) => void` dans `MessageBubbleProps`, transmise à `<AssistantMessage … onDeepen={onDeepen} />`.

`AssistantMessage/DeepenButton.tsx` (nouveau) :

```tsx
// ============================================================
// DeepenButton — Sprint 2 RAG
// Relance la question en lecture intégrale des documents nommés.
// ============================================================

import { BookOpen } from 'lucide-react'
import type { NamedDocumentRef } from '../../../types'

interface DeepenButtonProps {
  namedDocuments: NamedDocumentRef[]
  disabled?: boolean
  onClick: () => void
}

export function DeepenButton({ namedDocuments, disabled, onClick }: DeepenButtonProps) {
  const names = [...new Set(namedDocuments.filter(d => d.status === 'found').flatMap(d => d.found))]
  if (names.length === 0) return null
  const label = names.length <= 2 ? names.join(' et ') : `${names.slice(0, 2).join(', ')} et ${names.length - 2} autre${names.length > 3 ? 's' : ''}`
  return (
    <button
      type="button"
      onClick={onClick}
      disabled={disabled}
      title="Relance la question en lisant ces documents en entier (environ 15 secondes)"
      className="mt-3 inline-flex items-center gap-2 text-xs font-medium px-3 py-1.5 rounded-lg border border-stone-200 dark:border-stone-700 text-stone-600 dark:text-stone-300 hover:bg-stone-50 dark:hover:bg-stone-800 disabled:opacity-50"
    >
      <BookOpen className="w-3.5 h-3.5" />
      <span>Approfondir — lecture intégrale de {label} (~15 s)</span>
    </button>
  )
}
```

Exporter dans `AssistantMessage/index.ts` : `export { DeepenButton } from './DeepenButton'`.

`AssistantMessage.tsx` : prop `onDeepen?: (message: Message) => void` (interface + destructuration) ; import `DeepenButton` ; après `<SourcesList … />` :

```tsx
          {/* Sprint 2 RAG — trace de la recherche intelligente */}
          {message.agentic && message.agentic.iterations > 0 && (
            <div className="mt-3 text-[11px] text-stone-400 dark:text-stone-500">
              Recherche intelligente : {message.agentic.steps.length} recherche{message.agentic.steps.length > 1 ? 's' : ''}
              {message.agentic.timed_out ? ', budget épuisé' : ''}
              {message.agentic.steps.length > 0 && (
                <ul className="mt-1 space-y-0.5">
                  {message.agentic.steps.map(s => (
                    <li key={s.iteration}>· {s.result_summary}</li>
                  ))}
                </ul>
              )}
            </div>
          )}

          {/* Sprint 2 RAG — Approfondir : lecture intégrale des documents nommés */}
          {onDeepen && message.generation_mode !== 'gemini' && (message.named_documents?.length ?? 0) > 0 && (
            <DeepenButton namedDocuments={message.named_documents!} onClick={() => onDeepen(message)} />
          )}
```

- [ ] **Step 4: StreamingBubble — étapes agentiques et ciblées**

Dans `StepsIndicator`, avant le `return` :

```tsx
    const isAgentStep = (s: string) => s === 'agentic_start' || s.startsWith('agent_')
```

et sur le `<div key=…>` de chaque étape, ajouter à la classe : `${isAgentStep(step.step) ? 'ml-5' : ''}` ; le point d'état des étapes agentiques passe en violet : remplacer `bg-stone-100 dark:bg-stone-800` (état en cours) par `${isAgentStep(step.step) ? 'bg-violet-50 dark:bg-violet-900/20' : 'bg-stone-100 dark:bg-stone-800'}` et le spinner `border-t-stone-600` par `${isAgentStep(step.step) ? 'border-t-violet-500' : 'border-t-stone-600'}`. Les étapes `search_named` gardent le style standard (leur message porte déjà « 🔍 Recherche dans … »).

- [ ] **Step 5: Build et commit (dépôt ARPET, fichiers explicites)**

Run: `npm run build` dans `C:\Dev\Frontend-ARPET` → Expected: `tsc -b` sans erreur, bundle produit.

```bash
git add src/services/chat/chat-types.ts src/services/chat/chat-mapping.ts src/types/chat.types.ts src/pages/Dashboard.tsx src/components/chat/MessageBubble.tsx src/components/chat/StreamingBubble.tsx src/components/chat/AssistantMessage/AssistantMessage.tsx src/components/chat/AssistantMessage/DeepenButton.tsx src/components/chat/AssistantMessage/index.ts
git commit -m "feat(chat): étapes agentiques visibles, trace de la recherche intelligente, bouton Approfondir (lecture intégrale des documents nommés)"
```

---

### Task 9: Harnais — critères alternatifs, métrique « les deux documents cités », critères du set réel revus (R-S1d)

**Files:**
- Modify: `eval/run-eval.ts:20-30,296-345,360-392,416-425` (types `expected`, scoring, agrégat, rapport)
- Create: `eval/rescore-both-docs.ts`
- Modify: `eval/golden-set.json` (C2-003, C3-001, C4-001, C6-003, C8-003 ; `source_docs_all` sur C3-001..004), `eval/golden-set.synthetic.json` (`source_docs_all` sur SC3-001..010)
- Modify: `eval/GOLDEN_SET_VALIDATION.md` (section « Révision du 2026-09-19 (Sprint 2) »)

**Interfaces:**
- `expected.answer_must_contain: Array<string | string[]>` — un élément tableau = alternatives (une suffit). `expected.source_docs_all?: string[]` — tous ces libellés doivent apparaître dans les `top_k_for_recall` sources ; résultat dans `ok_all_docs: boolean | null` (n'entre PAS dans `ok_criteria`, pour rester comparable à v2.1.0) ; agrégat `all_docs_pct` par classe et global ; `rescore-both-docs.ts` recalcule cette métrique sur un rapport existant (les sources y sont stockées) pour obtenir la valeur v2.1.0.
- Changements de critères **soumis à Eric avec le plan** (aucun n'est appliqué sans son accord ; chaque changement est justifié dans `GOLDEN_SET_VALIDATION.md`) :

| Id | Aujourd'hui | Proposition | Justification |
|---|---|---|---|
| C2-003 | doc `CCAG`, contient `10 %` | doc `CCAP`, contient `150` | Le CCAP du projet fixe 100/150 €/jour ; le plafond 10 % du CCAG est la règle générale, l'utilisateur veut le marché (poids 0,5 de la couche application, décision du Sprint 1) |
| C3-001 | doc `Mémoire Technique`, contient `désamiantage` | `source_docs_all: ["Mémoire Technique", "CCTP"]`, contient `[]` | L'origine dit « réponse de référence à définir par Eric » ; « désamiantage » n'est qu'une des 4 réponses observées. Le critère mesurable de C3 est : les deux documents cités |
| C4-001 | contient `réhabilitation énergétique`, `7 résidences` | contient `[["réhabilitation énergétique", "rénovation énergétique"], "7 résidences"]` | Synonymes du CCTP lui-même |
| C6-003 | contient `travail dissimulé` | contient `[["travail dissimulé", "8221"]]` | La réponse traite les articles L. 8221-3 à 5 ; la formule « travail dissimulé » n'est pas exigible |
| C8-003 | contient `Communication Parties Prenantes` | contient `[["Communication Parties Prenantes", "parties prenantes"]]` | Contenu juste, libellé exact absent ; la mention des parties prenantes suffit à prouver la lecture de l'annexe 2 |

`source_docs_all` ajouté (sans autre changement) : C3-002 `["CCAP", "CHARTE"]`, C3-003 `["CCAP", "PROCES VERBAL"]`, C3-004 `["CCTP", "PGC"]`, SC3-006 `["CCAG", "41"]`, SC3-007 `["CCAP", "ETANCHEITE"]`, SC3-010 `["N°02", "N°07"]` ; pour SC3-001..005, SC3-008, SC3-009 : les deux libellés se lisent dans `origine` / `source_doc_contains` de chaque entrée (l'implémenteur les reporte, Eric relit au diff).

- [ ] **Step 1: Harnais — types et scoring**

Dans `run-eval.ts`, interface `expected` : `answer_must_contain?: Array<string | string[]>` et `source_docs_all?: string[]` ; `EvalResult` += `ok_all_docs: boolean | null` ; `Aggregate` += `all_docs_pct: number | null`. Scoring (remplace le bloc « Critères factuels ») :

```ts
  // Sprint 2 : un élément tableau = alternatives (une suffit)
  const mustContain = exp.answer_must_contain || []
  const missing_facts = mustContain
    .filter(f => Array.isArray(f) ? !f.some(alt => answerNorm.includes(normalize(alt))) : !answerNorm.includes(normalize(f)))
    .map(f => Array.isArray(f) ? f.join(' | ') : f)
  const violations = (exp.answer_must_not_contain || []).filter(f => answerNorm.includes(normalize(f)))
  const refusal_detected = detectRefusal(call.answer)

  // Sprint 2 : C3 — tous les documents attendus dans le top-k (métrique à part, hors ok_criteria)
  let ok_all_docs: boolean | null = null
  if (exp.source_docs_all?.length) {
    ok_all_docs = exp.source_docs_all.every(needle => topK.some(s => normalize(s.document_name || '').includes(normalize(needle))))
  }
```

`ok_all_docs` dans l'objet retourné ; dans `aggregate` : `const withAll = results.filter(r => r.ok_all_docs !== null)` et `all_docs_pct: withAll.length ? Math.round(100 * withAll.filter(r => r.ok_all_docs).length / withAll.length) : null`. Dans le markdown (`buildMarkdown`) et l'affichage console, ajouter la colonne « Tous docs (C3) » avec `fmt(a.all_docs_pct, ' %')`, à côté de « Critères ». Dans la liste des échecs (ligne 416), inclure `r.ok_all_docs === false` avec la raison `documents manquants: …`.

- [ ] **Step 2: `eval/rescore-both-docs.ts`**

```ts
// Recalcule la métrique « tous les documents attendus cités » (source_docs_all) sur un rapport
// existant : les sources y sont stockées. Sert à obtenir la valeur v2.1.0 sans rejouer le banc.
// Usage : deno run -A eval/rescore-both-docs.ts --report eval/reports/baseline-v2.1.0.json --golden eval/golden-set.json
import { parseArgs } from "https://deno.land/std@0.224.0/cli/parse_args.ts"

function normalize(s: string): string {
  return s.normalize('NFD').replace(/[\u0300-\u036f]/g, '').toLowerCase().replace(/[\s-]+/g, '')
}

const args = parseArgs(Deno.args, { string: ['report', 'golden'] })
if (!args.report || !args.golden) {
  console.error('Usage: deno run -A eval/rescore-both-docs.ts --report <rapport.json> --golden <golden-set.json>')
  Deno.exit(1)
}
const report = JSON.parse(await Deno.readTextFile(args.report))
const golden = JSON.parse(await Deno.readTextFile(args.golden))
const topK: number = report.config?.defaults?.top_k_for_recall ?? 10
const expected = new Map<string, string[]>()
for (const e of golden.entries) if (e.expected?.source_docs_all?.length) expected.set(e.id, e.expected.source_docs_all)

let n = 0, ok = 0
const misses: string[] = []
for (const r of report.results) {
  const needles = expected.get(r.id)
  if (!needles) continue
  n++
  const docs = (r.sources || []).slice(0, topK).map((s: { document_name?: string }) => normalize(s.document_name || ''))
  const missing = needles.filter(nd => !docs.some((d: string) => d.includes(normalize(nd))))
  if (missing.length === 0) ok++
  else misses.push(`${r.id}: manque ${missing.join(', ')}`)
}
console.log(`${args.report} — tous docs cités : ${ok}/${n} (${n ? Math.round(100 * ok / n) : 0} %)`)
for (const m of misses) console.log('  ✗ ' + m)
```

(vérifier le nom réel de la clé où le rapport stocke sa config — `report.config` ou autre — en lisant `eval/reports/baseline-v2.1.0.json` ; sinon utiliser 10.)

- [ ] **Step 3: Critères (après l'accord d'Eric sur le tableau ci-dessus)**

Appliquer les changements validés dans `eval/golden-set.json` et les `source_docs_all` dans les deux sets. Dans `eval/GOLDEN_SET_VALIDATION.md`, ajouter la section « Révision du 2026-09-19 (Sprint 2) » avec le tableau (id, avant, après, justification, « validé par Eric le … »).

- [ ] **Step 4: Vérifier, rejouer la métrique sur v2.1.0, commit**

Run: `deno check eval/run-eval.ts eval/rescore-both-docs.ts && deno run -A eval/rescore-both-docs.ts --report eval/reports/baseline-v2.1.0.json --golden eval/golden-set.json && deno run -A eval/rescore-both-docs.ts --report eval/reports/baseline-v2.1.0-synth.json --golden eval/golden-set.synthetic.json`
Expected: deux lignes « tous docs cités : x/y » — les valeurs v2.1.0 de la métrique C3, reportées dans le registre SDD et dans §7.4.

```bash
git add eval/run-eval.ts eval/rescore-both-docs.ts eval/golden-set.json eval/golden-set.synthetic.json eval/GOLDEN_SET_VALIDATION.md
git commit -m "feat(eval): critères alternatifs, métrique « tous les documents cités » (C3), critères du set réel revus (validés)"
```

---

### Task 10: Revue finale, portes, éval, baseline v2.2.0, docs

Exécutée par le contrôleur (pas de sous-agent implémenteur), sauf la revue.

- [ ] **Step 1: Revue finale de branche (opus) AVANT les portes**

Paquet : diff Baikal depuis `f57f8f1` (T1→T7, T9) + diff ARPET depuis `bfc2589` (T8), liste des minors différés des revues par tâche. Constats Critique/Important → vague de correction (opus ou sonnet selon le brief), re-revue ciblée. Minors → registre SDD.

- [ ] **Step 2: Porte G1 — migration**

Soumettre `supabase/migrations/20260919100000_rag_acces_retrieval.sql` à Eric. Sur « ok » : `apply_migration` via MCP (projet `odspcxgafcqxjzrarsqf`). Vérifications immédiates (lecture seule) :
- `select has_function_privilege('anon', 'rag.match_documents_v15(vector, text, uuid, uuid, uuid, text, integer, double precision, boolean, boolean, boolean, boolean, text[], uuid[], text[], boolean, integer, double precision, integer[], boolean, double precision, integer)', 'EXECUTE')` → `false` ; même requête pour `service_role` → `true`.
- `select * from rag.resolve_access('<user_id du contexte bessieres>', '<project_id bessieres>', null)` → `allowed = true, reason = project_member` ; avec un `user_id` d'une autre org → `allowed = false, reason = not_project_member`.

- [ ] **Step 3: Porte G2 — déploiement**

Sur « ok » : `npx supabase functions deploy baikal-retrieval --project-ref odspcxgafcqxjzrarsqf` depuis la racine Baikal. Contrôles :
- `curl` avec la clé anon → HTTP 401 `{"success":false,"error":"Authentification requise"}`.
- Depuis ARPET connecté (preview navigateur, projet Bessières) : « Est-ce qu'il y a des incohérences entre le mémoire technique et le CCTP ? » → étapes « 🔍 Recherche dans Mémoire Technique… » / « … CCTP TCE… », réponse sourcée sur les deux, bouton « Approfondir — lecture intégrale de Mémoire Technique et CCTP TCE » ; clic → badge « Full Document ».
- Question courte de suivi (« et dans le CCAP ? ») → réécriture journalisée sans recopie (`rag.query_logs.rewritten_query`).

- [ ] **Step 4: Éval**

Prérequis d'Eric : `SUPABASE_SERVICE_ROLE_KEY` et `GEMINI_API_KEY` dans `eval/.env`. Lancer les deux campagnes `s2-v2.2.0` / `s2-v2.2.0-synth` (commandes des contraintes globales), puis le juge : `deno run -A eval/judge-groundedness.ts --from-report eval/reports/s2-v2.2.0.json`. Lecture des échecs :

```
node -e "const r=require('./eval/reports/s2-v2.2.0.json'); r.results.filter(x=>!x.ok_criteria||x.ok_all_docs===false).forEach(x=>console.log(x.id, x.question, '\n', x.answer.slice(0,400)))"
```

Grille de lecture : (a) C3 « tous docs cités » vs valeur v2.1.0 recalculée (T9) — critère de succès de la spec ; (b) p50 des comparaisons (intent `comparison`) stable ou réduit ; (c) distribution `agentic.reason` et part `direct_answer` ; (d) effet du T1 sur `synthesis`/`citation` (latence, classes C4/C8) ; (e) `targeted_chunks` par question nommée. Variance ±1-2 questions : juger par classe.

- [ ] **Step 5: S2.5 (sur données)**

Si les comparaisons qui passent encore par la boucle agentique s'arrêtent sur `timed_out` ou `max_iterations`, proposer à Eric `parameters.agentic.max_iterations: 4` (global — la config n'a pas d'override par intent ; un override par intent serait du code, à décider sur chiffres). Sinon, ne rien changer.

- [ ] **Step 6: Baseline v2.2.0**

Rejouer sous les tags `baseline-v2.2.0` / `baseline-v2.2.0-synth` sur le code déployé, commiter `eval/reports/baseline-v2.2.0.*` et `baseline-v2.2.0-synth.*` (suivis via `eval/.gitignore`).

- [ ] **Step 7: Docs**

ARPET `docs/SPEC_RAG_OPTIM_V1.md` : §7.4 « Sprint 2 — résultats » (gabarit de §7.3 : tableaux réel/synthétique, par classe, gains/pertes nominatifs, classement des échecs restants, enseignements, décisions — y compris la métrique « tous docs cités » avant/après et le fait sécurité). ARPET `CLAUDE.md` section « État Courant » (v2.2.0, accès vérifié, bouton Approfondir). Baikal `CLAUDE.md` : ligne du pipeline (recherche ciblée par document nommé, budget agentique, `auth.ts`) et, dans « Known Issues / Tech Debt », remplacer la ligne sur les événements SSE agentiques par : « Les événements SSE agentiques ont un traitement UI dédié dans ARPET depuis v2.2.0 (T8) ; l'événement `analysis` (intent, rewritten_query) reste non consommé côté front ». Commit ARPET séparé (`docs(rag): Sprint 2 clos — …`), commit Baikal `docs(rag): …`. Push sur accord d'Eric uniquement.

---

## Auto-revue du plan

- **Couverture spec** : S2.1 → T6 ; S2.2 → T6 ; S2.3 → T5 (généralisé à tout document nommé, décision Q2) + T4 ; S2.4 → T8 ; S2.5 → T10 étape 5 ; R-S1a → T3 ; R-S1b → T4 ; R-S1c → tranché (fait 6) ; R-S1d → T9 ; R-S1e → T2 ; « Approfondir » (décision Q3) → T7 + T8 ; minor « 11 erreurs deno check » → T1. Non couverts, volontairement : durcissement `search_path` v14/v15, `p_children_per_parent` en dur, pool graphrag ×4, clé Gemini en query string, tri de `similar` — reportés (registre SDD).
- **Placeholders** : aucun « TBD » ; le seul point laissé à la lecture de l'implémenteur est le nom des fixtures `CTX`/`FEATURES` dans `prompt.test.ts` et la clé de config dans le rapport JSON (`rescore-both-docs.ts`), tous deux explicitement signalés.
- **Cohérence des types** : `found_file_ids` / `layer` (T4) consommés à l'identique par T5 (`buildNamedTargets`, `slimNamedDocuments`) et T7 ; `AgenticResult.directAnswer` (T6) → `direct_answer` dans le log et le payload → `AgenticSummary.direct_answer` (T8) ; `GateReason` étendu en T7 ; `NamedTarget.names` utilisé par `targetLabel`.
