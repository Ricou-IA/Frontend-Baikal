# Sprint 4 RAG « Corpus » (et harnais) — plan d'implémentation

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Le corpus ARPET est remis à niveau (7 fichiers ré-ingérés en FLUX 3 v5.1.0 : les 5 documents Bessières sans L0 et les 2 documents de la couche application), le `fts` porte les entités QQOQCCP (normes, lots, localisations) avec des poids, gemini-2.5-flash est re-mesuré avec un budget de réflexion, Cohere est mesuré dès que la clé est posée ; chaque cause a sa campagne d'éval, la version cible du code est **v2.4.0** et une baseline v2.4.0 est figée.

**Architecture:** Deux migrations SQL (fonction/trigger `rag.update_fts` pondérée + backfill ; archivage des chunks legacy en `rejected` avant ré-ingestion), une ré-ingestion pilotée fichier par fichier par le MCP n8n (workflow FLUX 3 « copy 2 » v5.1.0, `SQQlO1oZYBt9EU0r`) avec vérification SQL après chaque fichier, et une évolution de code v2.4.0 limitée à la surcharge d'éval (`eval_overrides` v2 : `llm_model`, `gemini_thinking_budget`, `enable_reranking`, service_role seulement) et au budget de réflexion Gemini (`generation.gemini_thinking_budget`, repli 0). Toutes les mesures se font par le banc `eval/` sans toucher `config.agent_prompts` ; les promotions (flash, Cohere) sont des migrations décidées par Eric sur chiffres.

**Tech Stack:** Postgres (tsvector `french`, `setweight`, triggers, JSONB `config.agent_prompts`), Deno / TypeScript (Edge Function `baikal-retrieval`), n8n FLUX 3 v5.1.0 (LlamaParse + Gemini 2.5 Flash, EF `ingest-documents` v8.1.0 upsert), Supabase Storage (copie d'objet), Gemini `thinkingConfig`, Cohere `v2/rerank` (code prêt), harnais d'éval Deno (`eval/run-eval.ts`, `eval/judge-groundedness.ts`).

**Spec:** `C:\Dev\Frontend-ARPET\docs\SPEC_RAG_OPTIM_V1.md` §5 Sprint 4 (S4.1–S4.4), §6, §7.5 ; prompt de liaison `docs/superpowers/prompts/2026-09-23-sprint4-rag.md` (reports R-S3a, R-S3b, R-S3c, R-S3d). Décisions du brainstorming du 2026-09-24 (Eric) : **5 fichiers Bessières** (le CCAP inclus, objet storage recopié depuis le projet « Test ») ; **ordre FTS → corpus → flash → Cohere → mineurs**, une cause par campagne ; **flash : un seul budget (256) sur échantillon avant toute campagne complète** ; **Cohere : Eric pose `COHERE_API_KEY` pendant le sprint**, seuil d'activation = au moins 2 des 4 questions ciblées réglées, p50 +300 ms max, aucune classe en baisse de plus d'une question.

## Faits établis avant le plan (vérifiés le 2026-09-24 en base, dans le code et dans n8n)

1. **Cinq fichiers du projet OC014 - Bessières (`631734bc-ab38-4cb5-b0b0-0f5e7fe67a28`, org `50c496ef-3af2-4aba-8950-e14680ceb895`) sont en pipeline de janvier** (aucun L0, `chunk_local_id` NULL, `parent_chunk_id` NULL, `qui_lots`/`comment_normes` vides, `metadata` sans `enrichment`) : Acte d'Engagement `50011461-6815-447c-9674-cd591633db65` (42 chunks), CCAP `ae536bdf-4079-4b89-839f-1235e7968138` (107), Mémoire Technique `5cd5db31-b679-4b65-9c32-74579e549a55` (171), PGC `2ae83a91-5efb-4436-93cf-04c8d8e86653` (175), RICT `672c4542-30af-43da-9d4d-86adfd5fd426` (41). Tous : bucket `premium-sources`, `created_by = 2a61b437-8e80-482f-89d7-6913002bd3de`, `app_id = arpet`, `metadata.category_slug = bdf87560-6e1d-4bf9-84e3-620e94ee8b83`, chunks `quality_level = standard`, `target_apps = {arpet}`, `target_projects = {631734bc-…}`.
2. **Le PDF du CCAP est absent du storage** (0 objet à `project/2a61b437-8e80-482f-89d7-6913002bd3de/1768438642018_Bessi_res_CCAP.pdf`). Une copie de même taille (`sources.files.file_size` 660 Ko ; objet 675 931 octets) existe : bucket `user-workspace`, clé `no-org/2a61b437-8e80-482f-89d7-6913002bd3de/1774398790180_Bessi_res_CCAP.pdf` (fichier `2022efc0-0da9-406f-963e-8deb2ddd4788` du projet « Test » `c0dce122-…`, à ne pas toucher).
3. **Couche app** : CCAG `5ba512af-d68a-41f3-9578-aeb17a231a55` (527 chunks : 129 L0, 374 L1, **24 niveaux 2/3** tous à `parent_chunk_id` NULL, articles 8.1.x/9.x/10.x) et Norme NFP03-001 `8644dc88-1ded-4a95-97e8-27cc8e10754b` (544 : 98 L0, 446 L1), pipeline 3.2.0, `chunk_local_id` de la forme `batch_N_sec_…` / `batch_N_chunk_…` — **même forme que v5.x** (`batch_0_sec_1_generalites`, `batch_0_chunk_1_1_note_preliminaire`) : un upsert sans archivage écraserait les ids identiques et laisserait les autres en `approved`. Chemins : `app/arpet/1770018568216_CCAG.pdf`, `app/arpet/1770018578308_Norme_NFP03-001.pdf` (`premium-sources`), `metadata.category_slug = d16e955b-0876-429d-aa31-34914d524fea`, `org_id` NULL, `project_id` NULL, `target_projects` NULL.
4. `rag.documents` : 3 700 lignes (app/approved 1 071, project/approved 2 629) ; `status` enum `draft,pending,approved,rejected` (**pas de `archived`**), défaut `approved` ; index unique `uq_documents_file_chunk_local (source_file_id, chunk_local_id)` ; `chunk_local_id` est une **colonne générée** `(metadata->>'chunk_local_id')` ; aucune clé étrangère ne référence `rag.documents` (`rag.citations` porte `source_id`/`target_id` sans FK).
5. **`fts`** est alimenté par le trigger `trg_documents_fts` (`BEFORE INSERT OR UPDATE OF content`) → `rag.update_fts()` = `to_tsvector('french', COALESCE(NEW.content, ''))` **sans poids** (poids D implicite). `match_documents_v15` note par `ts_rank_cd(d.fts, websearch_to_tsquery('french', query_text))` (poids par défaut D 0,1 / C 0,2 / B 0,4 / A 1,0) et filtre `d.fts @@ websearch_to_tsquery(...)`. Le JSONB `qqoqccp` porte `ou.localisations` (tableau), `ou.batiment/niveau/zone`, `comment.normes`, `qui.lots` ; colonnes dénormalisées `comment_normes text[]`, `qui_lots text[]`. Le CCTP TCE (v5.0.0, 345 chunks) a 86 chunks avec normes et 50 avec lots. `metadata.section_title` existe en v5.x, `metadata.current_section` en pipeline de janvier.
6. **S4.4 est déjà en place depuis le Sprint 1** : `search/layer-weight.ts` (`resolveAppLayerWeight` : 1,0 hors projet, si norme détectée ou motif normatif dans la question, sinon `search.app_layer_weight` borné [0,1 ; 1,0], repli 0,5) → `p_app_layer_weight` de `match_documents_v15` (multiplie le RRF des chunks `layer = 'app'`). En base : `parameters.search.app_layer_weight` = NULL (repli 0,5 du code). Rien à coder ; à re-vérifier après la ré-ingestion (T2).
7. **Ingestion** : `sources.on_file_inserted` (fichier `pending`) → `sources.ingestion_queue` → `sources.send_to_n8n` (pg_net, **timeout 30 s**) → EF `trigger-ingestion` → FLUX 1 `/webhook/ingest` (v1.2.0) → HTTP vers FLUX 3 `/webhook/process-document` (**timeout 5 min**) → LlamaParse → 3.6c chunking v5.1.0 (L0 = chapitres, L1 cible 2 000 caractères) → 3.6h/3.6i QQOQCCP par micro-lots de 10 → EF `ingest-documents` v8.1.0 (upsert `source_file_id, chunk_local_id`, puis `rag.resolve_chunk_hierarchy(file)` qui rattache les L1 aux L0 **par `metadata->>'chunk_local_id'` du même fichier, sans filtre de statut**) → 3.8b `sources.complete_ingestion_job(file_id, success, null, chunk_count)`. Statut de `sources.files` connu pour rester faux sur les gros fichiers (CCTP TCE corrigé à la main le 15/08). Aucune ligne `ingestion_queue` ne subsiste pour ces fichiers.
8. **FLUX 3 « copy 2 » (`SQQlO1oZYBt9EU0r`) est le workflow actif** (v5.1.0 au nœud 3.6c), exécutable par le MCP n8n (`availableInMCP: true`, `execute_workflow` type `webhook`, réponse immédiate avec un id d'exécution, suivi par `get_execution`). Son nœud 3.2 relaie le corps tel quel ; les nœuds 3.4/3.5/3.6 lisent : `trace_id, filename, document_title, category_slug, source_file_id, extension, layer, status, quality_level, user_id, org_id, target_apps, target_projects, filename_clean, storage_bucket, path`. Le téléchargement lit `storage/v1/object/authenticated/{storage_bucket}/{path}`. L'ancien FLUX 3 (`rUCSJCfahZyiQK6O`) est inactif.
9. **`COHERE_API_KEY` est absente** des secrets du projet (`npx supabase secrets list --project-ref odspcxgafcqxjzrarsqf`, vérifié le 2026-09-24) ; `search/reranker.ts` saute le rerank avec un avertissement si la clé manque, et `candidateCount`/`rerankIfEnabled` lisent `features.enable_reranking` (`config.features`, `features` NULL en base → repli `false`, `cohere_top_n` 10, `cohere_candidates` 24).
10. **Gemini sur extraits** : `generation/gemini-chunks.ts` — `thinkingConfigFor(model)` renvoie `{ thinkingBudget: 0 }` sauf `*-pro` (undefined) ; `buildGeminiChunksBody` l'insère dans `generationConfig` ; garde `MAX_REPEAT_RUN = 200`. `generation/usage.ts` additionne déjà `thoughtsTokenCount` aux tokens de sortie. `config.ts` : `FALLBACK_LIBRARIAN.llm_model = 'gpt-4o-mini'`, `parseLibrarianConfig` lit `generation.llm_model` ; `LibrarianConfig` (types.ts) n'a pas de champ de budget.
11. **Surcharges du banc** : `eval-overrides.ts` — `applyEvalOverrides(librarian, overrides, callerKind)` → `{ librarian, applied, ignored }`, seul `llm_model` (regex `^[a-z0-9][a-z0-9.\-]{1,63}$`, `service` seulement) ; appelée dans `index.ts` ligne ~221 après `loadConfig` (`config.librarian = ov.librarian`) ; `types.ts:29` `eval_overrides?: { llm_model?: string }`. `eval/run-eval.ts` : `--llm-model`, `--classes`, `--limit`, `--tag`, `--baseline`, `--golden`, `--smoke` ; `callRetrieval(question, ctx, conversationId, cfg, bearer, llmModel)` envoie `eval_overrides: { llm_model }` ; `meta.llm_model_override`. `eval/judge-groundedness.ts` : `--passes` lu seulement s'il est une chaîne (`--passes` nu = booléen → 1 passage silencieux). Champ `classe` des golden sets = `C1`…`C8` dans les deux fichiers (synthétique : C3 10 questions, C5 8, C6 10).
12. **Baseline v2.3.0** (référence) : réel 28/35, recall 93 %, C3 3/4, MRR 0,805, p50 4,24 s, p95 12,6 s, 0,0021 $/req, agentique 29 %, fidélité 0,632 (n=23), citations 0,884 ; synthétique 50/60, recall 96 %, p50 2,93 s, 0,0016 $/req, fidélité 0,673 (n=44). Variance ±1-2 questions (C4-004, C5-003, C8-003).
13. **Questions du golden réel qui lisent les 7 fichiers** (15) : Mémoire Technique — C1-002, C3-001, C4-002 ; PGC — C1-003 ; CCAP — C1-007, C2-003, C2-005, C3-002, C3-003, C5-002, C5-003, C6-002, C6-003 ; CCAG — C4-004, C8-002. Synthétique : 12 questions CCAP (SC1-001/002/003, SC2-001, SC3-009, SC4-003, SC5-001/002/008, SC8-001/002) et 2 Acte d'Engagement (SC1-008, SC4-002) ; les questions « CCAG CITROEN » et « 2139_CCAP » sont d'autres projets, hors effet. **C3-004** (CCTP TCE, limites de prestations entre lots) n'est pas ré-ingéré : sa cible est le FTS pondéré (T1, `qui_lots` en poids A).
14. Suite de départ : **152 tests EF** (`deno test -A supabase/functions/baikal-retrieval/`), **10 tests banc** (`deno test -A eval/`), `deno check supabase/functions/baikal-retrieval/index.ts` = 0 erreur. `eval/config.json` (contextes, `defaults.request_timeout_ms`) et `eval/.env` (`SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, `GEMINI_API_KEY`) sont gitignorés.
15. Fichiers non commités au 2026-09-24 — Baikal : `deno.lock`, `src/contexts/AppContext.jsx` (Eric). ARPET : `.claude/settings.local.json`, `src/components/layout/Sidebar.tsx`, `src/components/layout/sidebar/SidebarNavigation.tsx`, `src/components/meeting/MeetingRecordModal.tsx`, `src/components/ui/ConnectorsModal.tsx` (supprimé), `src/services/meeting.service.ts`. **Jamais ajoutés, jamais modifiés.** Dernier commit Baikal `59b8774`, ARPET `e3a7c00`.

## Global Constraints

- Branche `main` des deux dépôts, **sans worktree**. Baikal : `C:\Dev\Frontend-Baikal`. ARPET : `C:\Dev\Frontend-ARPET` (`main` ; `master` déclenche Vercel — pousser `main` seulement, `master` = décision d'Eric). Toujours `git -C <dépôt>` explicite.
- **Jamais `git add -A`** : chemins explicites uniquement ; fichiers du fait 15 jamais touchés.
- Tests Deno : `*.test.ts` à côté du module, `https://deno.land/std@0.224.0/assert/mod.ts`, TDD (test écrit et vu échouer avant l'implémentation). `deno check supabase/functions/baikal-retrieval/index.ts` = **0 erreur** ; `deno check eval/run-eval.ts eval/judge-groundedness.ts` = 0 erreur à T3.
- Texte fixe des prompts **sans accents** (convention de `generation/prompt.ts`) ; commentaires, logs et messages SSE en français accentué. Écrire les fichiers avec l'outil Write (un heredoc bash décode `\u0300`).
- Version cible : **v2.4.0** (en-tête et log `=== v2.4.0 Sprint 4 ===` d'index.ts, T3). T1 et T2 se mesurent sur v2.3.0 déployé (aucun changement de code).
- **Portes** : G1 — chaque migration est écrite dans `supabase/migrations/` et soumise à Eric avant `apply_migration` (MCP, projet `odspcxgafcqxjzrarsqf`), sauf régime « go de bout en bout » déclaré par Eric ; G2 — déploiement `npx supabase functions deploy baikal-retrieval --project-ref odspcxgafcqxjzrarsqf` sur « ok » ; G3 — promotion de gemini-2.5-flash (migration) sur chiffres, décision d'Eric ; G4 — activation Cohere en prod (migration) sur chiffres, décision d'Eric ; aucun push sans accord. En régime « go de bout en bout », G1/G2 et le push s'exécutent sans nouvelle question, **G3 et G4 restent des décisions d'Eric** (changement de modèle et de coût en production).
- Modèles des sous-agents : `sonnet` pour toutes les tâches ; `opus` pour la revue finale de branche et les vagues de correction. Registre SDD neuf : `.superpowers/sdd/2026-09-24-sprint4-rag/`.
- Éval (racine Baikal) : une campagne réelle ≈ 5 min, synthétique ≈ 6 min, juge ≈ 2-3 min — **toujours en processus détaché** (`Start-Process powershell` sur un `.ps1` du scratchpad qui journalise dans le scratchpad, outil PowerShell) ; **jamais deux campagnes en parallèle** sur le même projet ; juger **par classe et par question**, jamais au chiffre global seul ; juge à **un seul passage** (déterministe à T=0). Nettoyage `rag.conversations` sur demande d'Eric seulement.
- Le MCP `execute_sql` tourne en lecture seule (`supabase_read_only_user`) : toute écriture passe par `apply_migration` (migration versionnée) ou par l'API REST/Storage avec la clé service_role lue dans `eval/.env` par un script (**valeur jamais dans le chat**).
- Ré-ingestion : **un fichier à la fois**, le plus petit d'abord (canari), vérification SQL après chaque fichier, jamais deux exécutions FLUX 3 en parallèle (quotas LlamaParse/Gemini, latence des campagnes).
- Pièges payés : `\b` JavaScript ne borne pas devant une lettre accentuée (`(?<![a-zà-ÿ])`) ; `eval_overrides` n'est appliqué qu'en `service_role` (campagnes avec `SUPABASE_SERVICE_ROLE_KEY`) ; gemini-2.5-flash sans réflexion boucle sur un caractère dans 3-6 % des réponses (garde `MAX_REPEAT_RUN`, une réponse tronquée = échec) ; `npx supabase secrets list` montre des empreintes, jamais les valeurs ; le Bash de Claude tue une commande de fond après 10 min ; `original_filename` de `sources.files` est à lire en base avant de construire un payload (les chemins storage sont translittérés).
- Hors périmètre (spec §6 + reports) : changement d'embedding ; refonte n8n (FLUX 3 v5.1.0 tel quel) ; UI cross-ref ; mineurs R-S3c qui ne touchent pas les mesures de ce sprint (motif `usageMetadata` dupliqué, `candidateCount` cross-ref/targeted, enfants ciblés orphelins après rerank, buffer SSE final, usage avant throw dans gemini-agent, boucle à cheval sur deux morceaux, `logQuery` Phase B, payload `agentic` du repli sans `steps`) — listés en §7.6 comme reports Sprint 5, avec ceux que l'activation de Cohere en prod rendrait prioritaires (`candidateCount`, enfants orphelins).

---

## Carte des fichiers

| Fichier | Rôle dans ce sprint |
|---|---|
| `supabase/migrations/20260924100000_rag_fts_entites_qqoqccp.sql` | T1 — `rag.fts_entites_a/b`, `rag.update_fts` pondérée, trigger élargi, backfill (G1) |
| `supabase/migrations/20260924110000_rag_archive_chunks_bessieres_sprint4.sql` | T2 — archivage (`rejected`, `chunk_local_id` retiré) des 5 fichiers Bessières (G1) |
| `supabase/migrations/20260924120000_rag_archive_chunks_app_sprint4.sql` | T2 — archivage des 2 fichiers de la couche app (G1) |
| `supabase/functions/baikal-retrieval/types.ts` | T3 — `LibrarianConfig.gemini_thinking_budget`, `RequestBody.eval_overrides` v2 |
| `supabase/functions/baikal-retrieval/config.ts` + `config.test.ts` | T3 — `gemini_thinking_budget` lu en DB (repli 0) |
| `supabase/functions/baikal-retrieval/generation/gemini-chunks.ts` + `gemini-chunks.test.ts` | T3 — `thinkingConfigFor(model, budget)` |
| `supabase/functions/baikal-retrieval/eval-overrides.ts` + `eval-overrides.test.ts` | T3 — surcharges `gemini_thinking_budget`, `enable_reranking` (service_role) |
| `supabase/functions/baikal-retrieval/index.ts` | T3 — en-tête/log v2.4.0, application des surcharges (`librarian` + `features`) |
| `eval/run-eval.ts` + `eval/run-eval.test.ts` | T3 — `--thinking-budget`, `--enable-reranking`, `buildEvalOverrides`, `meta.eval_overrides` |
| `eval/judge-groundedness.ts` | T3 — `--passes` nu signalé (R-S3d) |
| `supabase/migrations/20260924130000_rag_librarian_gemini_flash.sql` | T3 — **seulement si G3 = promotion** |
| `supabase/migrations/20260924140000_rag_features_cohere.sql` | T4 — **seulement si G4 = activation** |
| `eval/reports/baseline-v2.4.0.*`, `baseline-v2.4.0-synth.*` | T5 — baseline figée |
| ARPET `docs/SPEC_RAG_OPTIM_V1.md` (§7 tableau, §7.6), ARPET `CLAUDE.md` (État courant), Baikal `CLAUDE.md` | T5 — docs de fin de sprint |

---

### Task 1 : QQOQCCP → FTS pondéré (S4.3) et campagne « fts »

**Files:**
- Create: `supabase/migrations/20260924100000_rag_fts_entites_qqoqccp.sql`
- Read: `supabase/functions/baikal-retrieval/search/retrieval.ts` (`toFtsQuery`, pour savoir ce que reçoit `query_text` : mots OR-isés)

**Interfaces:**
- Consumes: colonnes `rag.documents.comment_normes text[]`, `qui_lots text[]`, `qqoqccp jsonb`, `metadata jsonb`, `content text` ; `match_documents_v15` (inchangée).
- Produces: `rag.fts_entites_a(text[], text[]) → text`, `rag.fts_entites_b(jsonb, jsonb) → text`, `rag.update_fts()` pondérée, trigger `trg_documents_fts` sur `content, comment_normes, qui_lots, qqoqccp, metadata` ; rapports `eval/reports/s4-v2.3.0-fts.*`.

- [ ] **Step 1 : Écrire la migration** (outil Write)

```sql
-- Sprint 4 (S4.3) : le tsvector `fts` de rag.documents porte, en plus du contenu, les entités
-- clés du QQOQCCP et le titre de section, avec des poids distincts pour ts_rank_cd
-- (poids par défaut de Postgres : A = 1,0 ; B = 0,4 ; C = 0,2 ; D = 0,1).
--   A : normes (comment_normes) et lots (qui_lots)
--   B : localisations (qqoqccp.ou.localisations, batiment, niveau, zone) et titre de section
--   D : contenu (poids implicite d'avant : les rangs des correspondances de contenu sont inchangés)
-- Avant (2026-01) : NEW.fts := to_tsvector('french', COALESCE(NEW.content, ''))
-- Rollback : recréer rag.update_fts() avec la ligne ci-dessus, recréer le trigger sur
--            `content` seul, puis UPDATE rag.documents SET content = content.

CREATE OR REPLACE FUNCTION rag.fts_entites_a(p_comment_normes text[], p_qui_lots text[])
RETURNS text
LANGUAGE sql IMMUTABLE
AS $$
  SELECT NULLIF(concat_ws(' ',
    array_to_string(COALESCE(p_comment_normes, '{}'::text[]), ' '),
    array_to_string(COALESCE(p_qui_lots, '{}'::text[]), ' ')
  ), '')
$$;

CREATE OR REPLACE FUNCTION rag.fts_entites_b(p_qqoqccp jsonb, p_metadata jsonb)
RETURNS text
LANGUAGE sql IMMUTABLE
AS $$
  SELECT NULLIF(concat_ws(' ',
    (SELECT string_agg(x, ' ')
       FROM jsonb_array_elements_text(
         CASE WHEN jsonb_typeof(p_qqoqccp->'ou'->'localisations') = 'array'
              THEN p_qqoqccp->'ou'->'localisations' ELSE '[]'::jsonb END) AS x),
    p_qqoqccp->'ou'->>'batiment',
    p_qqoqccp->'ou'->>'niveau',
    p_qqoqccp->'ou'->>'zone',
    COALESCE(p_metadata->>'section_title', p_metadata->>'current_section')
  ), '')
$$;

CREATE OR REPLACE FUNCTION rag.update_fts()
RETURNS trigger
LANGUAGE plpgsql
AS $$
BEGIN
  NEW.fts :=
       setweight(to_tsvector('french', COALESCE(rag.fts_entites_a(NEW.comment_normes, NEW.qui_lots), '')), 'A')
    || setweight(to_tsvector('french', COALESCE(rag.fts_entites_b(NEW.qqoqccp, NEW.metadata), '')), 'B')
    || setweight(to_tsvector('french', COALESCE(NEW.content, '')), 'D');
  RETURN NEW;
END;
$$;

DROP TRIGGER IF EXISTS trg_documents_fts ON rag.documents;
CREATE TRIGGER trg_documents_fts
  BEFORE INSERT OR UPDATE OF content, comment_normes, qui_lots, qqoqccp, metadata
  ON rag.documents
  FOR EACH ROW EXECUTE FUNCTION rag.update_fts();

-- Backfill (3 700 lignes au 2026-09-24) : `UPDATE OF content` déclenche le trigger même à valeur identique.
UPDATE rag.documents SET content = content;
```

- [ ] **Step 2 : Vérifier à blanc les helpers sur des valeurs connues** (MCP `execute_sql`, lecture seule — les fonctions n'existent pas encore, on vérifie l'expression)

```sql
SELECT
  to_tsvector('french', 'NF P 92-507 (2004) NF EN 15534 Gros oeuvre') AS a_attendu,
  websearch_to_tsquery('french', 'NF P 03-001') AS requete_norme,
  websearch_to_tsquery('french', 'limites de prestations gros oeuvre') AS requete_lots;
```
Attendu : les lexèmes `nf`, `p`, `92`, `-507`, `15534`, `gros`, `oeuvr` (stemming français) ; la requête norme produit `'nf' & 'p' & '03' & '-001'` (ou équivalent) — noter la forme exacte dans le rapport de tâche.

- [ ] **Step 3 : G1 — soumettre la migration à Eric** (chemin du fichier + résumé en 3 lignes : poids A/B/D, trigger élargi, backfill 3 700 lignes). Attendre « ok » (ou régime « go de bout en bout »).

- [ ] **Step 4 : Appliquer** via MCP `apply_migration` (project `odspcxgafcqxjzrarsqf`, name `rag_fts_entites_qqoqccp`, contenu du fichier).

- [ ] **Step 5 : Vérifier en base**

```sql
SELECT count(*) AS total,
       count(*) FILTER (WHERE fts::text ~ ':[0-9,]+A') AS avec_poids_a,
       count(*) FILTER (WHERE fts::text ~ ':[0-9,]+B') AS avec_poids_b,
       count(*) FILTER (WHERE fts IS NULL) AS sans_fts
FROM rag.documents;
-- attendu : total 3700, sans_fts 0, avec_poids_a >= 136 (CCTP TCE seul : 86 normes + 50 lots, recouvrement possible), avec_poids_b > 1000

SELECT d.id, left(f.display_name, 30) AS doc, d.hierarchy_level,
       ts_rank_cd(d.fts, websearch_to_tsquery('french', 'NF P 03-001')) AS rang
FROM rag.documents d JOIN sources.files f ON f.id = d.source_file_id
WHERE d.status = 'approved' AND d.fts @@ websearch_to_tsquery('french', 'NF P 03-001')
ORDER BY rang DESC LIMIT 8;
-- attendu : des chunks portant '03-001' dans comment_normes en tête (rang >= 1,0), le contenu seul en dessous (rang < 0,5)

SELECT tgname, pg_get_triggerdef(t.oid) FROM pg_trigger t WHERE tgname = 'trg_documents_fts';
-- attendu : UPDATE OF content, comment_normes, qui_lots, qqoqccp, metadata
```

- [ ] **Step 6 : Commit de la migration**

```bash
git -C C:/Dev/Frontend-Baikal add supabase/migrations/20260924100000_rag_fts_entites_qqoqccp.sql
git -C C:/Dev/Frontend-Baikal commit -m "feat(rag): fts pondéré — normes et lots en poids A, localisations et titre de section en B, contenu en D (S4.3)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

- [ ] **Step 7 : Campagne « fts »** — écrire `<scratchpad>\s4-fts.ps1` (outil Write) puis lancer en détaché (outil PowerShell) :

```powershell
Set-Location C:\Dev\Frontend-Baikal
$log = "<scratchpad>\s4-fts.log"
"DEBUT $(Get-Date -Format s)" | Set-Content $log
deno run -A eval/run-eval.ts --tag s4-v2.3.0-fts --baseline eval/reports/baseline-v2.3.0.json *>> $log
deno run -A eval/run-eval.ts --golden eval/golden-set.synthetic.json --tag s4-v2.3.0-fts-synth --baseline eval/reports/baseline-v2.3.0-synth.json *>> $log
deno run -A eval/judge-groundedness.ts --from-report eval/reports/s4-v2.3.0-fts.json --passes 1 --chunks-only *>> $log
deno run -A eval/judge-groundedness.ts --from-report eval/reports/s4-v2.3.0-fts-synth.json --passes 1 --chunks-only *>> $log
"FIN $(Get-Date -Format s)" | Add-Content $log
```
Lancement : `Start-Process powershell -ArgumentList '-NoProfile','-ExecutionPolicy','Bypass','-File','<scratchpad>\s4-fts.ps1'`. Suivi : lire le log jusqu'à `FIN` (≈ 15 min).

- [ ] **Step 8 : Lire les résultats par classe et par question** (rapports `.md` + `.judged.json`) et écrire `task-1-chiffres.md` dans le registre SDD : tableau v2.3.0 → fts (critères, recall, C3 tous docs, MRR, p50, p95, coût, fidélité, citations) pour réel et synthétique ; liste des questions qui basculent (dans les deux sens) ; verdict sur **C3-004** (le CCTP TCE remonte-t-il ?), C6-002/C6-003, et les 3 questions de variance connues. Règle : une régression de plus d'une question hors variance connue sur une classe = rollback de la migration (SQL de rollback en tête du fichier) avant T2, et le fait est consigné.

---

### Task 2 : Ré-ingestion des 7 fichiers (S4.1, S4.2) et campagne « corpus »

**Files:**
- Create: `supabase/migrations/20260924110000_rag_archive_chunks_bessieres_sprint4.sql`
- Create: `supabase/migrations/20260924120000_rag_archive_chunks_app_sprint4.sql`
- Create (scratchpad, non versionnés) : `s4-copie-ccap.ps1`, `s4-statut-fichier.ps1`, `s4-corpus.ps1`

**Interfaces:**
- Consumes: FLUX 3 « copy 2 » `SQQlO1oZYBt9EU0r` (MCP n8n `execute_workflow` type `webhook`, `method: POST`, `body` = payload du Step 4), EF `ingest-documents` v8.1.0, `public.complete_ingestion_job(p_file_id, p_success, p_error_message, p_chunks_count)`, Storage `POST /storage/v1/object/copy`, `rag.update_fts` de T1 (les nouveaux chunks reçoivent le FTS pondéré à l'insertion).
- Produces: 7 fichiers ré-ingérés en v5.1.0 (L0 + L1, QQOQCCP, `chunk_local_id`), chunks legacy en `rejected` avec `metadata.archive`, rapports `eval/reports/s4-v2.3.0-corpus.*`.

- [ ] **Step 1 : Lire les identités exactes des 7 fichiers** (MCP `execute_sql`) — nécessaire au payload : `original_filename` réel (accents), `storage_path`, `storage_bucket`, `metadata`.

```sql
SELECT id, original_filename, display_name, storage_bucket, storage_path, mime_type, layer, org_id, project_id, created_by, app_id, metadata
FROM sources.files
WHERE id IN ('50011461-6815-447c-9674-cd591633db65','ae536bdf-4079-4b89-839f-1235e7968138','5cd5db31-b679-4b65-9c32-74579e549a55',
             '2ae83a91-5efb-4436-93cf-04c8d8e86653','672c4542-30af-43da-9d4d-86adfd5fd426','5ba512af-d68a-41f3-9578-aeb17a231a55',
             '8644dc88-1ded-4a95-97e8-27cc8e10754b')
ORDER BY file_size;
```

- [ ] **Step 2 : Recopier l'objet du CCAP** — écrire `<scratchpad>\s4-copie-ccap.ps1` (lit `eval/.env`, n'affiche jamais la clé) et l'exécuter (outil PowerShell) :

```powershell
Set-Location C:\Dev\Frontend-Baikal
$env_ = @{}
Get-Content eval/.env | Where-Object { $_ -match '^\s*([A-Z_]+)\s*=\s*(.*)\s*$' } | ForEach-Object { $env_[$Matches[1]] = $Matches[2].Trim('"') }
$url = $env_['SUPABASE_URL']; $key = $env_['SUPABASE_SERVICE_ROLE_KEY']
$body = @{
  bucketId = 'user-workspace'
  sourceKey = 'no-org/2a61b437-8e80-482f-89d7-6913002bd3de/1774398790180_Bessi_res_CCAP.pdf'
  destinationBucket = 'premium-sources'
  destinationKey = 'project/2a61b437-8e80-482f-89d7-6913002bd3de/1768438642018_Bessi_res_CCAP.pdf'
} | ConvertTo-Json
$r = Invoke-WebRequest -Method POST -Uri "$url/storage/v1/object/copy" -Headers @{ Authorization = "Bearer $key"; apikey = $key } -ContentType 'application/json' -Body $body -UseBasicParsing
"HTTP $($r.StatusCode) : $($r.Content)"
```
Attendu : HTTP 200 et `{"Key":"premium-sources/project/.../1768438642018_Bessi_res_CCAP.pdf"}`. Si l'API refuse la copie inter-buckets (HTTP 4xx), repli : `Invoke-WebRequest GET "$url/storage/v1/object/authenticated/user-workspace/<sourceKey>" -OutFile <scratchpad>\ccap.pdf` puis `Invoke-WebRequest POST "$url/storage/v1/object/premium-sources/<destinationKey>" -InFile <scratchpad>\ccap.pdf -ContentType 'application/pdf'` (mêmes en-têtes).

Vérification (MCP `execute_sql`) :
```sql
SELECT bucket_id, name, (metadata->>'size')::bigint AS octets FROM storage.objects
WHERE name = 'project/2a61b437-8e80-482f-89d7-6913002bd3de/1768438642018_Bessi_res_CCAP.pdf';
-- attendu : 1 ligne, premium-sources, 675931 octets
```

- [ ] **Step 3 : Écrire la migration d'archivage Bessières** (outil Write)

```sql
-- Sprint 4 (S4.2) : archivage des chunks legacy (pipeline de janvier 2026, sans L0) des 5 fichiers du
-- projet OC014 - Bessières avant leur ré-ingestion en FLUX 3 v5.1.0.
-- `rag.document_status` n'a pas de valeur 'archived' : status = 'rejected' (exclu de match_documents_v15 et
-- des stratégies ciblées ; lignes conservées, les citations restent résolubles).
-- `chunk_local_id` (colonne générée depuis metadata) est retiré pour que l'upsert (source_file_id, chunk_local_id)
-- de ingest-documents v8.1.0 n'écrase aucune ligne archivée et que rag.resolve_chunk_hierarchy ne rattache
-- aucun nouveau L1 à un ancien L0 (le rattachement se fait par chunk_local_id sans filtre de statut).
-- Rollback (un fichier) :
--   UPDATE rag.documents SET status = 'approved',
--     metadata = (metadata - 'archive') || CASE WHEN metadata->'archive'->>'chunk_local_id' IS NULL THEN '{}'::jsonb
--                ELSE jsonb_build_object('chunk_local_id', metadata->'archive'->>'chunk_local_id') END
--   WHERE source_file_id = '<id>' AND metadata->'archive'->>'raison' = 'sprint4-reingestion';
--   (puis passer les chunks v5.1.0 du même fichier en 'rejected' s'ils ont été insérés).
UPDATE rag.documents
SET status = 'rejected',
    metadata = (metadata - 'chunk_local_id')
             || jsonb_build_object('archive', jsonb_build_object(
                  'raison', 'sprint4-reingestion',
                  'le', now(),
                  'chunk_local_id', metadata->>'chunk_local_id',
                  'status_avant', status::text))
WHERE status = 'approved'
  AND source_file_id IN (
    '50011461-6815-447c-9674-cd591633db65', -- Acte d'Engagement (Bessières AE DBC Signé), 42 chunks
    'ae536bdf-4079-4b89-839f-1235e7968138', -- CCAP Bessières, 107
    '5cd5db31-b679-4b65-9c32-74579e549a55', -- Mémoire Technique (DBC - BESSIERES OPH 31), 171
    '2ae83a91-5efb-4436-93cf-04c8d8e86653', -- PGC-Bessières, 175
    '672c4542-30af-43da-9d4d-86adfd5fd426'  -- RICT-DCE, 41
  );
-- attendu : 536 lignes
```

- [ ] **Step 4 : G1 — soumettre la migration** (536 chunks passent en `rejected` ; la fenêtre sans ces documents en production dure le temps de la ré-ingestion, ≈ 30-60 min pour les 5). Attendre « ok » (ou régime « go de bout en bout »). Puis `apply_migration` (name `rag_archive_chunks_bessieres_sprint4`) et vérifier :

```sql
SELECT source_file_id, status, count(*), count(*) FILTER (WHERE chunk_local_id IS NULL) AS sans_local_id
FROM rag.documents WHERE source_file_id IN ('50011461-6815-447c-9674-cd591633db65','ae536bdf-4079-4b89-839f-1235e7968138',
  '5cd5db31-b679-4b65-9c32-74579e549a55','2ae83a91-5efb-4436-93cf-04c8d8e86653','672c4542-30af-43da-9d4d-86adfd5fd426')
GROUP BY 1, 2 ORDER BY 1, 2;
-- attendu : 5 lignes, toutes 'rejected', sans_local_id = count
```

- [ ] **Step 5 : Canari — ré-ingérer l'Acte d'Engagement** (le plus petit, 371 Ko) par MCP n8n `execute_workflow` :

```json
{
  "workflowId": "SQQlO1oZYBt9EU0r",
  "executionMode": "production",
  "inputs": { "type": "webhook", "webhookData": { "method": "POST", "body": {
    "success": true,
    "trace_id": "s4_ae_<horodatage>",
    "user_id": "2a61b437-8e80-482f-89d7-6913002bd3de",
    "org_id": "50c496ef-3af2-4aba-8950-e14680ceb895",
    "source_file_id": "50011461-6815-447c-9674-cd591633db65",
    "layer": "project",
    "status": "approved",
    "quality_level": "standard",
    "filename": "<original_filename lu au Step 1>",
    "extension": "pdf",
    "path": "project/2a61b437-8e80-482f-89d7-6913002bd3de/1768438506388_Bessi_res_AE_DBC_Sign_.pdf",
    "storage_bucket": "premium-sources",
    "processing_type": "document",
    "is_audio_transcript": false,
    "transcript_content": null,
    "audio_raw_path": null,
    "target_apps": ["arpet"],
    "target_projects": ["631734bc-ab38-4cb5-b0b0-0f5e7fe67a28"],
    "received_at": "<ISO 8601>",
    "document_title": "Acte d'Engagement",
    "category_slug": "bdf87560-6e1d-4bf9-84e3-620e94ee8b83",
    "filename_clean": "acte_d_engagement.pdf"
  } } }
}
```
Le corps reproduit exactement la sortie du nœud 1.2 de FLUX 1 (fait 7/8) ; `quality_level = standard` = valeur actuelle des chunks (parité). Suivre par `get_execution(workflowId, executionId)` (sans données) jusqu'à `finished`/`error` ; en cas d'erreur, relire avec `includeData: true, nodeNames: ["3.7 Edge Function Ingest", "3.8 Préparation Réponse"], truncateData: 2`.

- [ ] **Step 6 : Vérifier le canari** (MCP `execute_sql`, remplacer `<id>`) :

```sql
SELECT status, hierarchy_level, count(*) AS n,
       count(*) FILTER (WHERE parent_chunk_id IS NOT NULL) AS avec_parent,
       count(*) FILTER (WHERE embedding IS NULL) AS sans_embedding,
       count(*) FILTER (WHERE fts IS NULL) AS sans_fts,
       count(*) FILTER (WHERE qqoqccp IS NOT NULL) AS avec_qqoqccp,
       count(*) FILTER (WHERE array_length(comment_normes, 1) > 0) AS avec_normes,
       count(*) FILTER (WHERE array_length(qui_lots, 1) > 0) AS avec_lots,
       count(*) FILTER (WHERE fts::text ~ ':[0-9,]+A') AS fts_poids_a
FROM rag.documents WHERE source_file_id = '<id>' GROUP BY 1, 2 ORDER BY 1, 2;
-- attendu : lignes approved/0 et approved/1 (v5.1.0), rejected/1 (legacy) ; sans_embedding 0, sans_fts 0 ; L1 approuvés avec_parent = n

SELECT count(*) AS l1_orphelins FROM rag.documents
WHERE source_file_id = '<id>' AND status = 'approved' AND hierarchy_level = 1 AND parent_chunk_id IS NULL
  AND metadata->'enrichment'->'hierarchy'->>'parent_local_id' IS NOT NULL;        -- attendu 0

SELECT count(*) AS parent_archive FROM rag.documents c JOIN rag.documents p ON p.id = c.parent_chunk_id
WHERE c.source_file_id = '<id>' AND c.status = 'approved' AND p.status <> 'approved';   -- attendu 0

SELECT count(*) AS niveaux_2_plus FROM rag.documents
WHERE source_file_id = '<id>' AND status = 'approved' AND hierarchy_level >= 2;         -- attendu 0

SELECT processing_status, chunk_count, processed_at FROM sources.files WHERE id = '<id>';
-- attendu : completed, chunk_count = nombre de chunks approuvés, processed_at aujourd'hui
```
Si `processing_status`/`chunk_count` sont restés anciens (fait 7), corriger par l'API REST — script `<scratchpad>\s4-statut-fichier.ps1` (mêmes lignes de lecture d'`eval/.env` que le Step 2) :
```powershell
$body = @{ p_file_id = '<id>'; p_success = $true; p_error_message = $null; p_chunks_count = <n approuvés> } | ConvertTo-Json
Invoke-WebRequest -Method POST -Uri "$url/rest/v1/rpc/complete_ingestion_job" -Headers @{ Authorization = "Bearer $key"; apikey = $key } -ContentType 'application/json' -Body $body -UseBasicParsing | Select-Object StatusCode
```
Si `l1_orphelins > 0` : relancer `POST $url/rest/v1/rpc/resolve_chunk_hierarchy` avec `{ "p_source_file_id": "<id>" }` (même script) et recompter. Si le canari échoue (exécution en erreur, 0 chunk approuvé) : **s'arrêter**, appliquer le rollback du fichier (commentaire de la migration, via `apply_migration` name `rag_rollback_archive_ae_sprint4`), consigner et remonter à Eric.

- [ ] **Step 7 : Ré-ingérer les 4 autres fichiers Bessières, un par un, dans cet ordre, avec la vérification du Step 6 après chacun** : RICT (`672c4542-…`, path `…/1768438951931_RICT-DCE.pdf`, title `RICT`, filename_clean `rict.pdf`), PGC (`2ae83a91-…`, `…/1768443261120_PGC-Bessi_res.pdf`, `PGC`, `pgc.pdf`), CCAP (`ae536bdf-…`, `…/1768438642018_Bessi_res_CCAP.pdf`, `CCAP`, `ccap.pdf`), Mémoire Technique (`5cd5db31-…`, `…/1768438154005_DBC_-_BESSIERES_OPH_31.pdf`, `Mémoire Technique`, `memoire_technique.pdf`). Même `org_id`, `target_projects`, `category_slug` que le canari ; `filename` = `original_filename` du Step 1. Noter pour chaque fichier : durée d'exécution, L0/L1 obtenus, chunks legacy archivés.

- [ ] **Step 8 : Vérifier que la recherche ciblée et les stratégies L0 retrouvent les 5 documents** — `--smoke` ne cible qu'une question ; utiliser une campagne réduite :

```powershell
deno run -A eval/run-eval.ts --classes C1,C3,C4 --tag s4-v2.3.0-bessieres-verif --baseline eval/reports/baseline-v2.3.0.json
```
(16 questions, ≈ 2 min, en détaché comme les autres.) Attendu : C1-002, C1-003, C3-001, C4-002 citent respectivement Mémoire Technique / PGC / Mémoire Technique / Mémoire Technique (`ok_recall_doc = true`).

- [ ] **Step 9 : Écrire la migration d'archivage de la couche app** (outil Write) — même corps que le Step 3 avec l'en-tête « Sprint 4 (S4.1) : archivage des chunks pipeline 3.2.0 (dont les 24 chunks niveaux 2/3 du CCAG) des 2 fichiers de la couche application avant ré-ingestion » et la liste :

```sql
    '5ba512af-d68a-41f3-9578-aeb17a231a55', -- CCAG (couche app), 527 chunks dont 24 niveaux 2/3
    '8644dc88-1ded-4a95-97e8-27cc8e10754b'  -- Norme NFP03-001 (couche app), 544
-- attendu : 1071 lignes
```
G1 — soumettre (1 071 chunks ; fenêtre sans CCAG/NFP03-001 pour TOUS les projets ARPET pendant la ré-ingestion, ≈ 30-60 min pour les deux) ; `apply_migration` name `rag_archive_chunks_app_sprint4` ; vérification comme au Step 4 (2 lignes `rejected`, `sans_local_id = count`).

- [ ] **Step 10 : Ré-ingérer le CCAG puis la Norme NFP03-001** (un à la fois), payload du Step 5 avec `org_id: null`, `layer: "app"`, `target_projects: null`, `category_slug: "d16e955b-0876-429d-aa31-34914d524fea"`, `path` du fait 3, `document_title` `CCAG` / `Norme NFP03-001`, `filename_clean` `ccag.pdf` / `norme_nfp03_001.pdf`, `filename` = `original_filename` du Step 1. Vérification du Step 6 après chacun ; en plus pour le CCAG : `niveaux_2_plus` approuvés = 0 (purge S4.1 : les 24 anciens sont en `rejected`) et

```sql
SELECT count(*) FILTER (WHERE status = 'approved') AS approuves, count(*) FILTER (WHERE status = 'rejected') AS archives
FROM rag.documents WHERE layer = 'app';
-- attendu : archives = 1071 ; approuves = L0 + L1 des deux nouveaux jeux
SELECT count(*) FROM rag.documents d WHERE d.layer = 'app' AND d.status = 'approved' AND NOT (d.target_apps @> ARRAY['arpet']);  -- attendu 0
```

- [ ] **Step 11 : Contrôle anti-doublons global et commit des migrations**

```sql
SELECT source_file_id, count(*) AS approuves, count(DISTINCT chunk_local_id) AS ids_distincts,
       count(*) FILTER (WHERE chunk_local_id IS NULL) AS sans_id
FROM rag.documents WHERE status = 'approved' AND source_file_id IN ('50011461-6815-447c-9674-cd591633db65','ae536bdf-4079-4b89-839f-1235e7968138',
  '5cd5db31-b679-4b65-9c32-74579e549a55','2ae83a91-5efb-4436-93cf-04c8d8e86653','672c4542-30af-43da-9d4d-86adfd5fd426',
  '5ba512af-d68a-41f3-9578-aeb17a231a55','8644dc88-1ded-4a95-97e8-27cc8e10754b')
GROUP BY 1;
-- attendu : approuves = ids_distincts, sans_id = 0 pour les 7
SELECT layer, status, count(*) FROM rag.documents GROUP BY 1, 2 ORDER BY 1, 2;   -- nouveau tableau du corpus pour §7.6
```

```bash
git -C C:/Dev/Frontend-Baikal add supabase/migrations/20260924110000_rag_archive_chunks_bessieres_sprint4.sql supabase/migrations/20260924120000_rag_archive_chunks_app_sprint4.sql
git -C C:/Dev/Frontend-Baikal commit -m "feat(rag): archivage des chunks legacy des 7 fichiers ré-ingérés en FLUX 3 v5.1.0 (S4.1, S4.2)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

- [ ] **Step 12 : Campagne « corpus »** — `<scratchpad>\s4-corpus.ps1`, même squelette que T1 Step 7 avec les tags `s4-v2.3.0-corpus` / `s4-v2.3.0-corpus-synth` et **baseline = les rapports de T1** (`eval/reports/s4-v2.3.0-fts.json`, `…-fts-synth.json`) pour isoler l'effet du corpus. Lancer en détaché, attendre `FIN`.

- [ ] **Step 13 : Chiffres de tâche** (`task-2-chiffres.md`) : tableau fts → corpus (mêmes colonnes qu'en T1) ; les 15 questions réelles du fait 13 une par une (avant/après, document cité, page) ; les 14 synthétiques CCAP/AE ; recall, C3 « tous docs » ; latence (le v5.1.0 fait des L1 plus longs : surveiller p50 et tokens_in) ; état de S4.4 (`app_layer_weight` : lire dans `rag.query_logs` de la campagne la part de chunks `layer = 'app'` cités sur les questions projet non normatives, avant/après — si elle monte, proposer une valeur `search.app_layer_weight` à Eric, sinon rien). Règle : une classe en baisse de plus d'une question hors variance connue = analyse par question (extrait cité, page) avant toute décision ; le rollback d'un fichier est possible (commentaire de la migration).

---

### Task 3 : v2.4.0 — budget de réflexion Gemini, surcharges d'éval v2, harnais (R-S3a, R-S3d)

**Files:**
- Modify: `supabase/functions/baikal-retrieval/types.ts` (`LibrarianConfig`, `RequestBody.eval_overrides`)
- Modify: `supabase/functions/baikal-retrieval/config.ts` (`FALLBACK_LIBRARIAN`, `parseLibrarianConfig`) ; Test: `config.test.ts`
- Modify: `supabase/functions/baikal-retrieval/generation/gemini-chunks.ts` ; Test: `gemini-chunks.test.ts`
- Modify: `supabase/functions/baikal-retrieval/eval-overrides.ts` ; Test: `eval-overrides.test.ts`
- Modify: `supabase/functions/baikal-retrieval/index.ts` (en-tête ligne 2, log ligne ~176, surcharges ligne ~221)
- Modify: `eval/run-eval.ts` ; Test: `eval/run-eval.test.ts`
- Modify: `eval/judge-groundedness.ts`
- Create (si G3 = promotion) : `supabase/migrations/20260924130000_rag_librarian_gemini_flash.sql`

**Interfaces:**
- Consumes: `LibrarianConfig` (types.ts), `FeatureFlags` (types.ts:48), `applyEvalOverrides` (index.ts:221), `callRetrieval`/`evalEntry` (run-eval.ts:233/342), `usageFromGemini` (déjà compté `thoughtsTokenCount`).
- Produces: `LibrarianConfig.gemini_thinking_budget: number` ; `thinkingConfigFor(model: string, budget?: number)` ; `EvalOverrides = { llm_model?: string; gemini_thinking_budget?: number; enable_reranking?: boolean }` ; `applyEvalOverrides(librarian, features, overrides, callerKind) → { librarian, features, applied, ignored }` ; `buildEvalOverrides(args) → EvalOverridesArg | null` exportée de run-eval.ts ; options `--thinking-budget N`, `--enable-reranking` ; `meta.eval_overrides` dans le rapport JSON.

- [ ] **Step 1 : Tests `thinkingConfigFor(model, budget)`** — dans `gemini-chunks.test.ts`, remplacer le test « thinkingConfigFor : budget 0 sur flash, absent sur pro » par :

```ts
Deno.test("thinkingConfigFor : budget 0 par defaut sur flash, absent sur pro", () => {
  assertEquals(thinkingConfigFor('gemini-2.5-flash'), { thinkingBudget: 0 })
  assertEquals(thinkingConfigFor('gemini-2.5-flash-lite'), { thinkingBudget: 0 })
  assertEquals(thinkingConfigFor('gemini-2.5-pro'), undefined)
})

Deno.test("thinkingConfigFor : budget configure transmis tel quel sur flash, plancher 128 sur pro, valeurs invalides → 0", () => {
  assertEquals(thinkingConfigFor('gemini-2.5-flash', 256), { thinkingBudget: 256 })
  assertEquals(thinkingConfigFor('gemini-2.5-pro', 256), { thinkingBudget: 256 })
  assertEquals(thinkingConfigFor('gemini-2.5-pro', 64), { thinkingBudget: 128 })
  assertEquals(thinkingConfigFor('gemini-2.5-flash', -5), { thinkingBudget: 0 })
  assertEquals(thinkingConfigFor('gemini-2.5-flash', Number.NaN), { thinkingBudget: 0 })
  assertEquals(thinkingConfigFor('gemini-2.5-flash', 300.7), { thinkingBudget: 300 })
})

Deno.test("buildGeminiChunksBody : le budget de reflexion de la config est transmis", () => {
  const body = buildGeminiChunksBody("Quel delai ?", "=== CHUNK 1 ===\ntexte", "REGLES",
    { ...CFG, gemini_thinking_budget: 256 } as LibrarianConfig) as Record<string, any>
  assertEquals(body.generationConfig.thinkingConfig, { thinkingBudget: 256 })
})
```

- [ ] **Step 2 : Lancer, voir échouer** — `deno test -A supabase/functions/baikal-retrieval/generation/gemini-chunks.test.ts` → échec (`thinkingConfigFor` ignore le 2e argument ; `gemini_thinking_budget` n'existe pas sur `LibrarianConfig` → erreur de type).

- [ ] **Step 3 : Implémenter** — `types.ts` : dans `LibrarianConfig`, après `llm_model: string`, ajouter `gemini_thinking_budget: number   // Sprint 4 : budget de réflexion Gemini sur extraits (0 = coupée)`. `gemini-chunks.ts` :

```ts
// Budget de réflexion Gemini : 0 = réflexion coupée (défaut, budget de latence). Les modèles *-pro
// refusent 0 (pas de thinkingConfig) et imposent un plancher de 128 quand un budget est demandé.
export function thinkingConfigFor(model: string, budget = 0): { thinkingBudget: number } | undefined {
  const b = Number.isFinite(budget) ? Math.max(0, Math.floor(budget)) : 0
  if (/-pro\b/i.test(model)) return b > 0 ? { thinkingBudget: Math.max(128, b) } : undefined
  return { thinkingBudget: b }
}
```
et dans `buildGeminiChunksBody` : `const thinkingConfig = thinkingConfigFor(config.llm_model, config.gemini_thinking_budget)`. Mettre à jour le commentaire d'en-tête (« Réflexion coupée par défaut (thinkingBudget 0), configurable par `generation.gemini_thinking_budget` »). `config.ts` : `FALLBACK_LIBRARIAN` gagne `gemini_thinking_budget: 0` (après `llm_model`) ; `parseLibrarianConfig` gagne `gemini_thinking_budget: typeof generation.gemini_thinking_budget === 'number' ? generation.gemini_thinking_budget : FALLBACK_LIBRARIAN.gemini_thinking_budget,`.

- [ ] **Step 4 : Test de parsing** — dans `config.test.ts`, à côté du test existant sur `llm_model` (le lire pour reprendre son fixture) :

```ts
Deno.test("parseLibrarianConfig : gemini_thinking_budget lu en base, repli 0", () => {
  const avec = parseLibrarianConfig({ parameters: { generation: { gemini_thinking_budget: 256 } } } as any)
  assertEquals(avec.gemini_thinking_budget, 256)
  const sans = parseLibrarianConfig({ parameters: { generation: {} } } as any)
  assertEquals(sans.gemini_thinking_budget, 0)
})
```
(Adapter la forme de l'argument à la signature réelle de `parseLibrarianConfig` — lire le test existant de `llm_model` dans le même fichier et reprendre exactement sa manière d'appeler.) Lancer `deno test -A supabase/functions/baikal-retrieval/config.test.ts supabase/functions/baikal-retrieval/generation/gemini-chunks.test.ts` → PASS.

- [ ] **Step 5 : Tests des surcharges v2** — réécrire `eval-overrides.test.ts` :

```ts
import { assertEquals } from "https://deno.land/std@0.224.0/assert/mod.ts"
import { applyEvalOverrides } from "./eval-overrides.ts"
import type { FeatureFlags, LibrarianConfig } from "./types.ts"

const BASE = { llm_model: 'gpt-4o-mini', temperature: 0.3, gemini_thinking_budget: 0 } as LibrarianConfig
const FEATURES = { enable_reranking: false, cohere_top_n: 10, cohere_candidates: 24 } as FeatureFlags

Deno.test("service_role : llm_model surcharge, la config d'origine n'est pas mutee", () => {
  const r = applyEvalOverrides(BASE, FEATURES, { llm_model: 'gemini-2.5-flash' }, 'service')
  assertEquals(r.librarian.llm_model, 'gemini-2.5-flash')
  assertEquals(r.applied, ['llm_model'])
  assertEquals(BASE.llm_model, 'gpt-4o-mini')
})

Deno.test("service_role : gemini_thinking_budget entier 0..24576, sinon ignore", () => {
  assertEquals(applyEvalOverrides(BASE, FEATURES, { gemini_thinking_budget: 256 }, 'service').librarian.gemini_thinking_budget, 256)
  assertEquals(applyEvalOverrides(BASE, FEATURES, { gemini_thinking_budget: 256 }, 'service').applied, ['gemini_thinking_budget'])
  assertEquals(applyEvalOverrides(BASE, FEATURES, { gemini_thinking_budget: 0 }, 'service').applied, ['gemini_thinking_budget'])
  assertEquals(applyEvalOverrides(BASE, FEATURES, { gemini_thinking_budget: 30000 }, 'service').ignored, ['gemini_thinking_budget'])
  assertEquals(applyEvalOverrides(BASE, FEATURES, { gemini_thinking_budget: 12.5 }, 'service').ignored, ['gemini_thinking_budget'])
  assertEquals(applyEvalOverrides(BASE, FEATURES, { gemini_thinking_budget: '256' }, 'service').ignored, ['gemini_thinking_budget'])
})

Deno.test("service_role : enable_reranking booleen, les features d'origine ne sont pas mutees", () => {
  const r = applyEvalOverrides(BASE, FEATURES, { enable_reranking: true }, 'service')
  assertEquals(r.features.enable_reranking, true)
  assertEquals(r.features.cohere_candidates, 24)
  assertEquals(r.applied, ['enable_reranking'])
  assertEquals(FEATURES.enable_reranking, false)
  assertEquals(applyEvalOverrides(BASE, FEATURES, { enable_reranking: 'oui' }, 'service').ignored, ['enable_reranking'])
})

Deno.test("plusieurs surcharges : applied dans l'ordre llm_model, gemini_thinking_budget, enable_reranking", () => {
  const r = applyEvalOverrides(BASE, FEATURES, { enable_reranking: true, llm_model: 'gemini-2.5-flash', gemini_thinking_budget: 256 }, 'service')
  assertEquals(r.applied, ['llm_model', 'gemini_thinking_budget', 'enable_reranking'])
})

Deno.test("utilisateur / anonyme : toutes les surcharges ignorees et signalees", () => {
  const r = applyEvalOverrides(BASE, FEATURES, { llm_model: 'gemini-2.5-flash', gemini_thinking_budget: 256, enable_reranking: true }, 'user')
  assertEquals(r.librarian, BASE)
  assertEquals(r.features, FEATURES)
  assertEquals(r.ignored, ['llm_model', 'gemini_thinking_budget', 'enable_reranking'])
  assertEquals(applyEvalOverrides(BASE, FEATURES, { llm_model: 'x' }, 'anonymous').ignored, ['llm_model'])
})

Deno.test("valeur invalide de llm_model (espace, injection, trop longue) : ignoree meme en service_role", () => {
  assertEquals(applyEvalOverrides(BASE, FEATURES, { llm_model: 'gpt 4' }, 'service').ignored, ['llm_model'])
  assertEquals(applyEvalOverrides(BASE, FEATURES, { llm_model: 'a/../b' }, 'service').ignored, ['llm_model'])
  assertEquals(applyEvalOverrides(BASE, FEATURES, { llm_model: 'x'.repeat(70) }, 'service').ignored, ['llm_model'])
  assertEquals(applyEvalOverrides(BASE, FEATURES, { llm_model: 42 }, 'service').ignored, ['llm_model'])
})

Deno.test("absent ou non-objet : rien", () => {
  assertEquals(applyEvalOverrides(BASE, FEATURES, undefined, 'service'), { librarian: BASE, features: FEATURES, applied: [], ignored: [] })
  assertEquals(applyEvalOverrides(BASE, FEATURES, 'oui', 'service').applied, [])
})
```
Lancer → échec (signature à 3 arguments).

- [ ] **Step 6 : Implémenter `eval-overrides.ts`**

```ts
// ============================================================================
// baikal-retrieval - Surcharges d'évaluation (Sprint 3 S3.2, Sprint 4 v2)
// ============================================================================
// Le banc d'éval (service_role) mesure des variantes sans modifier config.agent_prompts :
// `eval_overrides: { llm_model, gemini_thinking_budget, enable_reranking }` dans le corps.
// Tout autre appelant : ignoré et journalisé — jamais un 4xx (ARPET n'envoie jamais ce champ).
// ============================================================================

import type { FeatureFlags, LibrarianConfig } from "./types.ts"

export interface EvalOverrides {
  llm_model?: string
  gemini_thinking_budget?: number
  enable_reranking?: boolean
}

const MODEL_NAME = /^[a-z0-9][a-z0-9.\-]{1,63}$/i
const THINKING_BUDGET_MAX = 24576   // plafond Gemini 2.5 Flash

export function applyEvalOverrides(
  librarian: LibrarianConfig,
  features: FeatureFlags,
  overrides: unknown,
  callerKind: 'service' | 'user' | 'anonymous',
): { librarian: LibrarianConfig; features: FeatureFlags; applied: string[]; ignored: string[] } {
  if (!overrides || typeof overrides !== 'object') return { librarian, features, applied: [], ignored: [] }
  const o = overrides as Record<string, unknown>
  const applied: string[] = []
  const ignored: string[] = []
  let lib = librarian
  let feat = features
  const service = callerKind === 'service'

  if ('llm_model' in o) {
    const v = o.llm_model
    if (service && typeof v === 'string' && MODEL_NAME.test(v)) { lib = { ...lib, llm_model: v }; applied.push('llm_model') }
    else ignored.push('llm_model')
  }
  if ('gemini_thinking_budget' in o) {
    const v = o.gemini_thinking_budget
    if (service && typeof v === 'number' && Number.isInteger(v) && v >= 0 && v <= THINKING_BUDGET_MAX) {
      lib = { ...lib, gemini_thinking_budget: v }; applied.push('gemini_thinking_budget')
    } else ignored.push('gemini_thinking_budget')
  }
  if ('enable_reranking' in o) {
    const v = o.enable_reranking
    if (service && typeof v === 'boolean') { feat = { ...feat, enable_reranking: v }; applied.push('enable_reranking') }
    else ignored.push('enable_reranking')
  }
  return { librarian: lib, features: feat, applied, ignored }
}
```
`types.ts:29` : `eval_overrides?: { llm_model?: string; gemini_thinking_budget?: number; enable_reranking?: boolean }`. `index.ts` ligne ~221 :

```ts
          // Sprint 3/4 : surcharges du banc (service_role seulement) — modèle, budget de réflexion, reranking
          const ov = applyEvalOverrides(config.librarian, config.features, eval_overrides, caller.kind)
          config.librarian = ov.librarian
          config.features = ov.features
          if (ov.applied.length) console.log(`[eval] surcharges appliquées: ${ov.applied.join(', ')} (llm_model=${config.librarian.llm_model}, thinking=${config.librarian.gemini_thinking_budget}, rerank=${config.features.enable_reranking})`)
          if (ov.ignored.length) console.warn(`[eval] surcharges ignorées (${caller.kind}): ${ov.ignored.join(', ')}`)
```
En-tête ligne 2 → `// baikal-retrieval v2.4.0 - "Agentic RAG" + Sprint 4 (corpus v5.1.0, FTS pondéré, budget de réflexion Gemini, surcharges d'éval v2)` ; log ligne ~176 → `=== v2.4.0 Sprint 4 ===`. Si `config` est `const` et `features` en lecture seule, `deno check` le dira : passer alors par `const cfg = { ...config, librarian: ov.librarian, features: ov.features }` et remplacer les usages en aval (chercher `config.` dans `start()`), en le signalant dans le rapport.

- [ ] **Step 7 : Lancer la suite EF et `deno check`** — `deno test -A supabase/functions/baikal-retrieval/` → 152 + 5 nouveaux (7 tests dans eval-overrides.test.ts contre 4 avant, +2 gemini-chunks, +1 config) = **160 PASS attendus** (noter le vrai chiffre) ; `deno check supabase/functions/baikal-retrieval/index.ts` → 0 erreur.

- [ ] **Step 8 : Harnais — test `buildEvalOverrides`** dans `eval/run-eval.test.ts` :

```ts
import { buildEvalOverrides } from "./run-eval.ts"

Deno.test("buildEvalOverrides : rien sans option, sinon seulement les options fournies", () => {
  assertEquals(buildEvalOverrides({}), null)
  assertEquals(buildEvalOverrides({ 'llm-model': 'gemini-2.5-flash' }), { llm_model: 'gemini-2.5-flash' })
  assertEquals(buildEvalOverrides({ 'llm-model': 'gemini-2.5-flash', 'thinking-budget': '256' }), { llm_model: 'gemini-2.5-flash', gemini_thinking_budget: 256 })
  assertEquals(buildEvalOverrides({ 'enable-reranking': true }), { enable_reranking: true })
  assertEquals(buildEvalOverrides({ 'thinking-budget': 'abc' }), null)
})
```
(Fusionner l'import avec la ligne d'import existante de `./run-eval.ts`.) Lancer `deno test -A eval/` → échec (export absent).

- [ ] **Step 9 : Implémenter dans `run-eval.ts`**

```ts
export interface EvalOverridesArg { llm_model?: string; gemini_thinking_budget?: number; enable_reranking?: boolean }

/** Surcharges du banc depuis les options CLI (--llm-model, --thinking-budget N, --enable-reranking). */
export function buildEvalOverrides(args: Record<string, unknown>): EvalOverridesArg | null {
  const o: EvalOverridesArg = {}
  if (typeof args['llm-model'] === 'string') o.llm_model = args['llm-model'] as string
  if (typeof args['thinking-budget'] === 'string') {
    const n = parseInt(args['thinking-budget'] as string, 10)
    if (Number.isFinite(n)) o.gemini_thinking_budget = n
  }
  if (args['enable-reranking'] === true) o.enable_reranking = true
  return Object.keys(o).length ? o : null
}
```
Puis : `callRetrieval(question, ctx, conversationId, cfg, bearer, overrides: EvalOverridesArg | null)` — corps : `...(overrides ? { eval_overrides: overrides } : {})` ; `evalEntry(..., overrides, prices)` et ses deux appels internes passent `overrides` ; dans `main` : `const overrides = buildEvalOverrides(args)` remplace `const llmModel = …` ; les trois usages de `llmModel` (smoke, `evalEntry`, log `▶ Éval`, en-tête markdown `Modèle de génération (surcharge)`, `meta.llm_model_override`) deviennent `overrides?.llm_model ?? null`, et `meta` gagne `eval_overrides: overrides` ; en-tête markdown : `> Surcharges du banc : ${overrides ? JSON.stringify(overrides) : 'aucune (config DB)'}`. Commentaire d'usage en tête de fichier : ajouter `--thinking-budget 256` et `--enable-reranking`. `judge-groundedness.ts` ligne 178 :

```ts
  const passesArg = typeof args.passes === 'string' ? parseInt(args.passes, 10) : (args.passes === true ? Number.NaN : 1)
```
(un `--passes` nu est désormais signalé « invalide, 1 passage » au lieu d'être silencieux). Lancer `deno test -A eval/` → 11 PASS ; `deno check eval/run-eval.ts eval/judge-groundedness.ts` → 0 erreur.

- [ ] **Step 10 : Commit v2.4.0**

```bash
git -C C:/Dev/Frontend-Baikal add supabase/functions/baikal-retrieval/types.ts supabase/functions/baikal-retrieval/config.ts supabase/functions/baikal-retrieval/config.test.ts supabase/functions/baikal-retrieval/generation/gemini-chunks.ts supabase/functions/baikal-retrieval/generation/gemini-chunks.test.ts supabase/functions/baikal-retrieval/eval-overrides.ts supabase/functions/baikal-retrieval/eval-overrides.test.ts supabase/functions/baikal-retrieval/index.ts eval/run-eval.ts eval/run-eval.test.ts eval/judge-groundedness.ts
git -C C:/Dev/Frontend-Baikal commit -m "feat(rag): v2.4.0 — budget de réflexion Gemini configurable, surcharges d'éval v2 (budget, reranking), banc --thinking-budget/--enable-reranking (R-S3a, R-S3d)

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

- [ ] **Step 11 : G2 — déployer** sur « ok » (ou régime « go de bout en bout ») : `npx supabase functions deploy baikal-retrieval --project-ref odspcxgafcqxjzrarsqf` (racine Baikal). Fumée : `deno run -A eval/run-eval.ts --smoke` (modèle DB) puis `deno run -A eval/run-eval.ts --smoke --llm-model gemini-2.5-flash --thinking-budget 256` → réponse non vide, `model=gemini-2.5-flash` ; vérifier dans les journaux (`query_logs`, `source = 'function_logs'`) la ligne `[eval] surcharges appliquées: llm_model, gemini_thinking_budget (… thinking=256 …)`.

- [ ] **Step 12 : Échantillon flash + budget 256** — `<scratchpad>\s4-flash-echantillon.ps1` (détaché) :

```powershell
Set-Location C:\Dev\Frontend-Baikal
$log = "<scratchpad>\s4-flash-echantillon.log"
"DEBUT $(Get-Date -Format s)" | Set-Content $log
deno run -A eval/run-eval.ts --classes C3,C5,C6 --llm-model gemini-2.5-flash --thinking-budget 256 --tag s4-v2.4.0-flash256-ech --baseline eval/reports/s4-v2.3.0-corpus.json *>> $log
deno run -A eval/run-eval.ts --golden eval/golden-set.synthetic.json --classes C3,C5 --llm-model gemini-2.5-flash --thinking-budget 256 --tag s4-v2.4.0-flash256-ech-synth --baseline eval/reports/s4-v2.3.0-corpus-synth.json *>> $log
"FIN $(Get-Date -Format s)" | Add-Content $log
```
(11 questions réelles + 18 synthétiques, ≈ 5 min.) Puis :

```sql
SELECT id, model, (counts->>'runaway')::int AS runaway, (counts->>'tokens_out')::int AS tokens_out, latency_ms, created_at
FROM rag.query_logs WHERE created_at > now() - interval '30 minutes' AND model LIKE 'gemini%' ORDER BY id;
```
(Si `latency_ms` n'est pas une colonne, lire la latence dans le rapport JSON.) **Règle d'arrêt** : `runaway` = 0 sur les 29 réponses ET aucune réponse tronquée (`answer` se terminant sans phrase complète) ET p50 de l'échantillon ≤ p50 des mêmes questions dans `s4-v2.3.0-corpus` + 1 s → Step 13 ; sinon **stop** : flash reste non promu, consigner les ids/questions en boucle et la latence dans `task-3-chiffres.md`, passer à T4.

- [ ] **Step 13 : Campagnes complètes flash 256** (seulement si la règle du Step 12 est satisfaite) — `<scratchpad>\s4-flash256.ps1`, squelette de T1 Step 7 avec `--llm-model gemini-2.5-flash --thinking-budget 256`, tags `s4-v2.4.0-flash256` / `-synth`, baseline `s4-v2.3.0-corpus(.json|-synth.json)`, juge `--passes 1 --chunks-only` sur les deux. Chiffres dans `task-3-chiffres.md` : critères par classe, fidélité, p50/p95, coût/req (grille `PRICES_DEFAULT`, hypothèse tarifaire à rappeler), `runaway` total sur les 95 réponses, questions basculées.

- [ ] **Step 14 : G3 — décision d'Eric sur chiffres.** Critères de promotion proposés : `runaway` = 0 sur les deux campagnes ; critères réel ≥ corpus − 1 et synthétique ≥ corpus − 1 (variance) ; fidélité ≥ corpus ; p50 réel ≤ p50 corpus + 1 s ; coût documenté (attendu ×2-3). Si promotion : migration `20260924130000_rag_librarian_gemini_flash.sql` :

```sql
-- Sprint 4 (G3, décision d'Eric du <date>) : gemini-2.5-flash avec budget de réflexion 256 devient le modèle
-- de génération sur extraits (campagnes s4-v2.4.0-flash256 : <chiffres>). Rollback : remettre
-- 'gpt-4o-mini' et 0.
UPDATE config.agent_prompts
SET parameters = jsonb_set(jsonb_set(parameters, '{generation,llm_model}', to_jsonb('gemini-2.5-flash'::text), true),
                           '{generation,gemini_thinking_budget}', to_jsonb(256), true)
WHERE agent_type = 'librarian_v3' AND app_id = 'arpet' AND org_id IS NULL AND is_active;
```
G1 (apply), vérification `SELECT parameters->'generation' FROM config.agent_prompts WHERE agent_type='librarian_v3' AND app_id='arpet' AND org_id IS NULL AND is_active;`, commit `feat(rag): gemini-2.5-flash + réflexion 256 promu sur chiffres (Sprint 4, G3)`. Sinon : rien en base, le modèle reste gpt-4o-mini ; la décision et ses chiffres vont en §7.6.

---

### Task 4 : Cohere sur clé (R-S3b) et campagne « cohere »

**Files:**
- Create (si G4 = activation) : `supabase/migrations/20260924140000_rag_features_cohere.sql`

**Interfaces:**
- Consumes: `--enable-reranking` (T3), `search/reranker.ts` (`rerankIfEnabled`, `candidateCount`, `keepTargetedFirst`, `COHERE_API_KEY`), `features.cohere_top_n` 10 / `cohere_candidates` 24 (repli code, `features` NULL en base).
- Produces: rapports `eval/reports/s4-v2.4.0-cohere.*` ; migration `features` si activation.

- [ ] **Step 1 : Précondition — la clé est posée.** `npx supabase secrets list --project-ref odspcxgafcqxjzrarsqf` doit lister `COHERE_API_KEY` (empreinte seulement). Si absente : demander à Eric de la poser (`npx supabase secrets set COHERE_API_KEY=… --project-ref odspcxgafcqxjzrarsqf`, valeur jamais dans le chat) et attendre ; si Eric reporte, la tâche est consignée « non exécutée, clé absente » en §7.6 et on passe à T5. Une fois posée, aucun redéploiement n'est nécessaire (les secrets sont injectés à l'exécution) — vérifier par une fumée : `deno run -A eval/run-eval.ts --smoke --enable-reranking` et, dans les journaux, l'absence de `Reranking enabled but COHERE_API_KEY not set`.

- [ ] **Step 2 : Campagnes « cohere »** — `<scratchpad>\s4-cohere.ps1`, squelette de T1 Step 7 avec `--enable-reranking` (modèle = config DB, c'est-à-dire le résultat de G3), tags `s4-v2.4.0-cohere` / `-synth`, baseline = le dernier état de référence (rapports `s4-v2.4.0-flash256*` si G3 a promu flash, sinon `s4-v2.3.0-corpus*`), juge sur les deux.

- [ ] **Step 3 : Chiffres** (`task-4-chiffres.md`) : les 4 questions ciblées (C1-002, C5-002, SC2-006, SC5-005) une par une (extraits cités avant/après, page, réponse) ; critères par classe ; p50/p95 (attendu +150-300 ms) ; fidélité ; part des réponses où un extrait ciblé a été préservé (`targeted` dans `sources`) ; coût Cohere estimé (nombre d'appels = nombre de requêtes non conversationnelles).

- [ ] **Step 4 : G4 — décision d'Eric** (seuil du brainstorming : ≥ 2 des 4 questions ciblées réglées, p50 +300 ms max, aucune classe en baisse de plus d'une question). Si activation : `20260924140000_rag_features_cohere.sql` :

```sql
-- Sprint 4 (G4, décision d'Eric du <date>) : reranking Cohere activé en production
-- (campagnes s4-v2.4.0-cohere : <chiffres>). Rollback : enable_reranking = false.
UPDATE config.agent_prompts
SET parameters = jsonb_set(parameters, '{features}',
      COALESCE(parameters->'features', '{}'::jsonb)
      || '{"enable_reranking": true, "cohere_top_n": 12, "cohere_candidates": 24}'::jsonb, true)
WHERE agent_type = 'librarian_v3' AND app_id = 'arpet' AND org_id IS NULL AND is_active;
```
G1 (apply), vérification `SELECT parameters->'features' …`, commit `feat(rag): reranking Cohere activé sur chiffres (Sprint 4, G4)`. Si activation : les deux mineurs R-S3c « `candidateCount` non appliqué à cross-ref/targeted » et « enfants ciblés orphelins après rerank » passent en tête des reports Sprint 5 (§7.6).

---

### Task 5 : Baseline v2.4.0, docs de fin de sprint, revue finale, push

**Files:**
- Create: `eval/reports/baseline-v2.4.0.json`, `.md`, `.judged.json`, `baseline-v2.4.0-synth.json`, `.md`, `.judged.json`
- Modify: ARPET `docs/SPEC_RAG_OPTIM_V1.md` (tableau §7 : Sprints 2, 3, 4 ✅ avec dates et rapports ; nouvelle §7.6 après §7.5)
- Modify: ARPET `CLAUDE.md` (bloc « État courant » lignes ~219-225 : branche, RAG prod v2.4.0, Sprint 4 ✅, prochaine étape Sprint 5)
- Modify: Baikal `CLAUDE.md` (« RAG Pipeline (baikal-retrieval v2.3.0) » → v2.4.0 avec le budget de réflexion et les surcharges v2 dans la ligne génération ; structure de fichiers ; « Known Issues » : retirer « Quatre fichiers du projet Bessières n'ont aucun chunk L0… ré-ingestion prévue au Sprint 4 » et « gemini-2.5-flash… essai d'un budget de réflexion au Sprint 4 » selon G3, retirer « Cohere dormant » selon G4, ajouter une ligne « `fts` pondéré (normes/lots A, localisations/titre B, contenu D) par `rag.update_fts`, trigger sur content/comment_normes/qui_lots/qqoqccp/metadata »)
- Modify: mémoire `C:\Users\epude\.claude\projects\C--Dev-Frontend-Baikal\memory\` (nouveau `sprint4-rag-etat.md` + ligne dans `MEMORY.md`)

- [ ] **Step 1 : Baseline v2.4.0** — `<scratchpad>\s4-baseline.ps1` (détaché), squelette de T1 Step 7 **sans surcharge** (l'état de production après G3/G4), tags `baseline-v2.4.0` / `baseline-v2.4.0-synth`, baseline de comparaison `eval/reports/baseline-v2.3.0(.json|-synth.json)`, juge `--passes 1 --chunks-only` sur les deux → fichiers `.judged.json`.

- [ ] **Step 2 : Revue finale de branche** (`opus`) sur `59b8774..HEAD` du dépôt Baikal : migrations (rollback documenté, filtres `status = 'approved'`, aucune écriture hors des 7 fichiers), `eval-overrides.ts` (service_role seulement, aucune surcharge acceptée d'un utilisateur), `gemini-chunks.ts` (pro/flash), `index.ts` (features réassignées avant tout usage de `config.features`), harnais. Vague de correction si besoin, tests + `deno check` verts, commit `fix(rag): revue finale Sprint 4`.

- [ ] **Step 3 : §7.6 de la spec ARPET** — même structure que §7.5 : en-tête (date, commit Baikal, migrations appliquées : `rag_fts_entites_qqoqccp`, `rag_archive_chunks_bessieres_sprint4`, `rag_archive_chunks_app_sprint4`, + `rag_librarian_gemini_flash` / `rag_features_cohere` si promus), « Corpus après Sprint 4 » (tableau couche / chunks approuvés / L0 / archivés, depuis T2 Step 11), « Campagnes » (tableau v2.3.0 → fts → corpus → flash256 (échantillon et complet) → cohere → baseline v2.4.0, colonnes de §7.5), « Baseline v2.4.0 vs v2.3.0 » réel et synthétique, « Classement des échecs restants », « Enseignements » (au moins : effet réel du FTS pondéré sur C3-004/C6 ; effet de la ré-ingestion v5.1.0 sur les 15 questions Bessières ; flash avec réflexion : boucles ou non, coût ; Cohere : les 4 questions, latence ; ce que le harnais a appris), « Décisions » (G3, G4, `app_layer_weight`), « Reports Sprint 5 » (mineurs R-S3c non traités, avec priorité si Cohere actif). Tableau §7 : lignes Sprint 2, 3, 4 à ✅ avec dates (2026-09-21, 2026-09-23, <date>) et chemins des rapports.

- [ ] **Step 4 : `CLAUDE.md` ARPET, `CLAUDE.md` Baikal, mémoire** — modifications listées dans **Files** ; chiffres copiés des rapports, jamais de tête.

- [ ] **Step 5 : Commits (chemins explicites) et push**

```bash
git -C C:/Dev/Frontend-Baikal add eval/reports/baseline-v2.4.0.json eval/reports/baseline-v2.4.0.md eval/reports/baseline-v2.4.0.judged.json eval/reports/baseline-v2.4.0-synth.json eval/reports/baseline-v2.4.0-synth.md eval/reports/baseline-v2.4.0-synth.judged.json CLAUDE.md docs/superpowers/plans/2026-09-24-sprint4-rag.md
git -C C:/Dev/Frontend-Baikal commit -m "feat(eval): baseline v2.4.0 figée + docs de fin de Sprint 4

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
git -C C:/Dev/Frontend-ARPET add docs/SPEC_RAG_OPTIM_V1.md CLAUDE.md
git -C C:/Dev/Frontend-ARPET commit -m "docs(rag): Sprint 4 clos — §7.6 résultats v2.4.0 (corpus v5.1.0, FTS pondéré, flash + réflexion, Cohere), état courant

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```
Push `origin/main` des deux dépôts **sur accord d'Eric** (ou régime « go de bout en bout ») ; `master` d'ARPET jamais poussé ici.

---

## Auto-revue du plan (faite le 2026-09-24)

- **Couverture spec** : S4.1 (T2 Steps 9-11 : ré-ingestion CCAG/NFP03-001, archivage par statut, anti-doublons, purge des 24 niveaux 2/3 = archivés en `rejected` et contrôle `niveaux_2_plus` = 0), S4.2 (T2 Steps 3-8, 5 fichiers dont le CCAP), S4.3 (T1), S4.4 (déjà en place — T2 Step 13 le re-vérifie), R-S3a (T3), R-S3b (T4), R-S3c (traités : `--passes` nu ; reportés explicitement en §7.6 : les autres), R-S3d (T3 Step 9, motifs de refus surveillés à chaque `task-N-chiffres.md`), critère de succès du sprint (C3/C6 mesurés à chaque campagne, re-run complet = baseline v2.4.0).
- **Placeholders** : les seuls `<…>` sont des valeurs lues en base au Step 1 de T2 (`original_filename`), des horodatages, le scratchpad de la session et les chiffres des campagnes (inconnus par construction).
- **Cohérence des noms** : `gemini_thinking_budget` (types, config, gemini-chunks, eval-overrides, run-eval, migration G3) ; `enable_reranking` (features, eval-overrides, run-eval, migration G4) ; `applyEvalOverrides(librarian, features, overrides, callerKind)` (eval-overrides.ts, test, index.ts) ; `buildEvalOverrides(args)` (run-eval.ts, test) ; tags de campagne `s4-v2.3.0-fts`, `s4-v2.3.0-corpus`, `s4-v2.4.0-flash256-ech`, `s4-v2.4.0-flash256`, `s4-v2.4.0-cohere`, `baseline-v2.4.0` (+ `-synth`).
